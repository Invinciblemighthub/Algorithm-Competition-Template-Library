## [返回目录](http://scpc.fun/discussion-detail/1405)

<style>
a[id], h2, h3, h4 { scroll-margin-top: 140px; }
</style>

---

## 本章目录

- [7.1 高精度运算](#sec-7-1) **必学**
  - [7.1.1 高精度减法](#sec-7-1-1) **必学**
  - [7.1.2 高精度除法与取模](#sec-7-1-2) **进阶**
  - [7.1.3 高精度快速幂](#sec-7-1-3) **选学**

---

<a id="ch7"></a>

## 七、高精度

本板块涵盖高精度运算，适用于数据范围超出 `long long` 的场景。



<a id="sec-7-1"></a>

### 7.1 高精度运算
- **适用场景**：处理大数运算（加减乘除、快速幂）。
- **核心思想**：用数组存储每一位数字，模拟手工计算。

```cpp
// 高精度结构体（支持加减乘除）
struct BigInt {
    vector<int> d;  // 低位在前
    bool negative = false;
    
    BigInt() {}
    BigInt(long long x) {
        if (x < 0) { negative = true; x = -x; }
        while (x) { d.push_back(x % 10); x /= 10; }
        if (d.empty()) d.push_back(0);
    }
    BigInt(const string& s) {
        int start = 0;
        if (s[0] == '-') { negative = true; start = 1; }
        for (int i = s.size() - 1; i >= start; i--) d.push_back(s[i] - '0');
        while (d.size() > 1 && d.back() == 0) d.pop_back();
    }
    
    int size() const { return d.size(); }
    
    // 比较
    bool operator<(const BigInt& o) const {
        if (negative != o.negative) return negative;
        if (d.size() != o.d.size()) return negative ? d.size() > o.d.size() : d.size() < o.d.size();
        for (int i = d.size() - 1; i >= 0; i--)
            if (d[i] != o.d[i]) return negative ? d[i] > o.d[i] : d[i] < o.d[i];
        return false;
    }
    
    // 加法
    BigInt operator+(const BigInt& o) const {
        if (negative != o.negative) {
            // 转为减法
            // 省略：需实现减法后调用
        }
        BigInt res;
        int carry = 0;
        for (int i = 0; i < max(d.size(), o.d.size()) || carry; i++) {
            int sum = carry;
            if (i < d.size()) sum += d[i];
            if (i < o.d.size()) sum += o.d[i];
            res.d.push_back(sum % 10);
            carry = sum / 10;
        }
        res.negative = negative;
        return res;
    }
    
    // 乘法
    BigInt operator*(const BigInt& o) const {
        BigInt res;
        res.d.resize(d.size() + o.d.size(), 0);
        for (int i = 0; i < d.size(); i++) {
            int carry = 0;
            for (int j = 0; j < o.d.size() || carry; j++) {
                long long cur = res.d[i + j] + carry;
                if (j < o.d.size()) cur += 1LL * d[i] * o.d[j];
                res.d[i + j] = cur % 10;
                carry = cur / 10;
            }
        }
        while (res.d.size() > 1 && res.d.back() == 0) res.d.pop_back();
        res.negative = negative != o.negative;
        return res;
    }
    
    string toStr() const {
        string s;
        if (negative) s += '-';
        for (int i = d.size() - 1; i >= 0; i--) s += char('0' + d[i]);
        return s;
    }
};
```

---

<a id="sec-7-1-1"></a>

### 7.1.1 高精度减法

**存储约定**：以下独立函数模板使用 `vector<int>` 存储**十进制大整数**，**低位在前**（`A[0]` 存个位，`A.back()` 存最高位），默认表示**非负整数**。选手可按需裁剪（有些题只要求正整数）。

```cpp
// 比较 A 和 B：返回 -1 (A<B), 0 (A==B), 1 (A>B)
int cmp_big(const vector<int>& A, const vector<int>& B) {
    if (A.size() != B.size()) return A.size() < B.size() ? -1 : 1;
    for (int i = (int)A.size() - 1; i >= 0; i--)
        if (A[i] != B[i]) return A[i] < B[i] ? -1 : 1;
    return 0;
}

// 高精度减法：返回 A - B（要求 A >= B）
vector<int> sub_big(const vector<int>& A, const vector<int>& B) {
    vector<int> C;
    int t = 0;
    for (int i = 0; i < (int)A.size(); i++) {
        t = A[i] - t;
        if (i < (int)B.size()) t -= B[i];
        C.push_back((t + 10) % 10);
        t = t < 0 ? 1 : 0;      // 向上一位借位标记
    }
    // 去掉前导零（低位在前 → 去掉尾部零）
    while (C.size() > 1 && C.back() == 0) C.pop_back();
    return C;
}

// 通用大整数减法：自动比较 A、B 大小，返回 A - B（带符号），结果为 pair<负数标志, 绝对值digits>
pair<bool, vector<int>> sub_any(const vector<int>& A, const vector<int>& B) {
    int c = cmp_big(A, B);
    if (c >= 0) return {false, sub_big(A, B)};      // A >= B
    else         return {true,  sub_big(B, A)};     // A < B，结果为负
}
```

---

<a id="sec-7-1-2"></a>

### 7.1.2 高精度除法与取模

高精度除法通常写成"**高精度 ÷ 单精度 long long**"形式（竖式除法，O(n)）。如果题目是"高精度 ÷ 高精度"，直接用"倍数枚举 + 高精度减法"复杂度会高达 $O(n^2 \times 9)$，一般不会考；如果真遇到，可用"二分商 + 高精度乘法比较"或手写长除法。

```cpp
// ============= 高精度 A（vector<int>低位在前） ÷ 单精度 b（long long） =============
// 返回 pair<商C, 余数r>
pair<vector<int>, long long> div_mod_big(const vector<int>& A, long long b) {
    vector<int> C;       // 商，注意：除法从高位算起 → C 也是高位在前！需翻转后才能和加减乘混用
    long long r = 0;
    for (int i = (int)A.size() - 1; i >= 0; i--) {
        r = r * 10 + A[i];
        C.push_back(r / b);
        r %= b;
    }
    // 现在 C 是高位在前，去掉前导零（即头部的零）
    reverse(C.begin(), C.end());          // 翻为低位在前
    while (C.size() > 1 && C.back() == 0) C.pop_back();
    return {C, r};
}

// 快速把一个字符串（"12345"）转为低位在前 vector 存储
vector<int> str2big(const string& s) {
    vector<int> A;
    for (int i = (int)s.size() - 1; i >= 0; i--) A.push_back(s[i] - '0');
    return A;
}

// 低位在前 vector 转回字符串
string big2str(const vector<int>& A) {
    string s;
    for (int i = (int)A.size() - 1; i >= 0; i--) s += char('0' + A[i]);
    if (s.empty()) s = "0";
    return s;
}
```

---

<a id="sec-7-1-3"></a>

### 7.1.3 高精度快速幂

**实用化版本**：**底数为高精度 vector，指数为 long long**（竞赛中不会考"指数也是高精度"的幂，那种题要么直接用扩展欧拉定理降幂，要么根本不出）。

需要先复用同一节内已写过的高精度乘法独立函数（不是 BigInt 类），所以先贴一个"纯 vector 版高精度乘法"供配套使用。

```cpp
// ================ 配套工具：高精度乘法（纯 vector 版，不依赖 BigInt 类）================
vector<int> mul_big(const vector<int>& A, const vector<int>& B) {
    vector<int> C(A.size() + B.size(), 0);
    for (int i = 0; i < (int)A.size(); i++)
        for (int j = 0; j < (int)B.size(); j++)
            C[i + j] += A[i] * B[j];
    int carry = 0;
    for (int i = 0; i < (int)C.size(); i++) {
        int sum = C[i] + carry;
        C[i] = sum % 10;
        carry = sum / 10;
    }
    while (C.size() > 1 && C.back() == 0) C.pop_back();
    return C;
}

// ================ 高精度快速幂：A^b（A 为高精度底数，b 为 long long 指数）================
// 结果仍然是 vector<int> 低位在前
// 若 b=0 返回 "1"；若 b<0 返回 {0}（一般竞赛不会让底数为分数幂）
vector<int> pow_big(vector<int> A, long long b) {
    vector<int> res; res.push_back(1);            // 初始 res = 1
    if (b < 0) { res.assign(1, 0); return res; }
    while (b > 0) {
        if (b & 1) res = mul_big(res, A);
        A = mul_big(A, A);
        b >>= 1;
    }
    return res;
}
```

**使用说明**：
- 如果底数是 2~10 等小数字，可以用 `str2big("9999...")` 先转成 vector。
- 结果位数是 b × log10(A的十进制位数)，2^(10^5) 大约 3 万位，所以请确保内存 / 时限足够。
- **扩展欧拉定理应用**：若题目要求 A^B mod p（B 是超大整数），不应该用高精度幂再取模——而应使用 [4.7 扩展欧拉定理](http://scpc.fun/discussion-detail/1409#sec-4-7)（先用扩展欧拉把 B 降为 mod φ(p) + φ(p)，变成 long long 之后再用普通 `quickpow`）。

---


