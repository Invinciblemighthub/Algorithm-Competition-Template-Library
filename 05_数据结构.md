## [返回目录](http://scpc.fun/discussion-detail/1405)

<style>
a[id], h2, h3, h4 { scroll-margin-top: 140px; }
</style>

---

## 本章目录

- [5.1 并查集](#sec-5-1) **必学**
- [5.2 树状数组](#sec-5-2) **必学**
- [5.3 线段树](#sec-5-3) **必学**
- [5.4 二维前缀和与二维ST表](#sec-5-4) **进阶**
- [5.5 平衡树（FHQ Treap）](#sec-5-5) **选学**
- [5.6 字典树（Trie）](#sec-5-6) **必学**
- [5.7 01Trie](#sec-5-7) **进阶**
- [5.8 主席树](#sec-5-8) **选学**
- [5.9 对顶堆](#sec-5-9) **进阶**

---

<a id="ch5"></a>

## 五、数据结构

本板块涵盖竞赛常用数据结构，包括并查集、树状数组、线段树和二维数据结构。



<a id="sec-5-1"></a>

### 5.1 并查集
- **适用场景**：维护集合的合并与查询，判断连通性。
- **时间复杂度**：均摊 $O(\alpha(n))$，接近 $O(1)$
- **核心思想**：路径压缩 + 按秩合并（或按大小合并）。

**变体**：带权并查集（维护到根的距离）、种类并查集（扩展域，处理敌人/朋友关系）、可撤销并查集。

```cpp
// 基础并查集（路径压缩+按大小合并）
struct DSU {
    vector<int> p, sz;
    DSU(int n) : p(n + 1), sz(n + 1, 1) {
        iota(p.begin(), p.end(), 0);
    }
    int find(int x) {
        return p[x] == x ? x : p[x] = find(p[x]);
    }
    void unite(int a, int b) {
        a = find(a); b = find(b);
        if (a == b) return;
        if (sz[a] < sz[b]) swap(a, b);
        p[b] = a;
        sz[a] += sz[b];
    }
    bool connected(int a, int b) { return find(a) == find(b); }
    int size(int x) { return sz[find(x)]; }
};

// 带权并查集（维护到根的距离）
struct WeightedDSU {
    vector<int> p, sz;
    vector<long long> dist;  // dist[x] = x到根的距离
    WeightedDSU(int n) : p(n + 1), sz(n + 1, 1), dist(n + 1, 0) {
        iota(p.begin(), p.end(), 0);
    }
    int find(int x) {
        if (p[x] == x) return x;
        int root = find(p[x]);
        dist[x] += dist[p[x]];  // 路径压缩时更新距离
        return p[x] = root;
    }
    // 合并：a到b的距离为d
    bool unite(int a, int b, long long d) {
        int ra = find(a), rb = find(b);
        if (ra == rb) return dist[a] - dist[b] == d;  // 检查一致性
        if (sz[ra] < sz[rb]) {
            dist[ra] = d - dist[a] + dist[b];
            p[ra] = rb;
            sz[rb] += sz[ra];
        } else {
            dist[rb] = dist[a] - dist[b] - d;
            p[rb] = ra;
            sz[ra] += sz[rb];
        }
        return true;
    }
};

// 种类并查集（扩展域，处理敌人关系）
// 例：食物链问题，将节点x拆分为x, x+n, x+2n分别表示三种关系
// 朋友：unite(x, y), unite(x+n, y+n), unite(x+2n, y+2n)
// 敌人：unite(x, y+n), unite(x+n, y+2n), unite(x+2n, y)

// 并查集+离线处理（删边转加边）
// 将删除操作逆序处理，变为加边操作

// 并查集+有序集合（合并区间）
set<int> st;  // 维护未合并的相邻位置
auto it = st.lower_bound(L);
while (it != st.end() && *it <= R) {
    dsu.unite(*it, *it + 1);
    it = st.erase(it);
}
```

**相关题目**：
- [Evil Morty](http://scpc.fun/problem/9299)（第14题，并查集+离线删边转加边）
- [Cube Stacking](http://scpc.fun/problem/10112)（第31题，带权并查集）
- [食物链](http://scpc.fun/problem/10115)（第32题，种类并查集）
- [营地](http://scpc.fun/problem/10436)（第39题，带权并查集判定一致性）
- [摧毁路线](http://scpc.fun/problem/10465)（第47题，并查集+离线处理）
- [Connected Components](http://scpc.fun/problem/CF-292-D)（第55题，前后缀并查集）
- [搭配购买](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P1455)（第68题，并查集+分组背包）
- [关押犯人](http://scpc.fun/problem/10110)（第93题，种类并查集/二分图判定）
- [家谱](http://scpc.fun/problem/10111)（第94题，并查集+map）
- [团伙](http://scpc.fun/problem/10114)（第95题，种类并查集）
- [朋友](http://scpc.fun/problem/10448)（第105题，并查集+按大小合并）
- [萌萌哒](http://scpc.fun/problem/10447)（第104题，并查集+区间合并）
- [小苯的蓄水池](http://scpc.fun/problem/11111)（第118题，并查集+有序集合）
- [最小化字符串](http://scpc.fun/problem/CF-1675E)（第120题，并查集+贪心）
- [口袋的天空](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P1195)（第156题，并查集=Kruskal变体）

---

<a id="sec-5-2"></a>

### 5.2 树状数组
- **适用场景**：单点修改+区间查询，或区间修改+单点查询（差分树状数组）。
- **时间复杂度**：修改和查询均为 $O(\log n)$

**核心操作**：`lowbit(x) = x & (-x)`，向前缀和方向更新/查询。

**变体**：
- 单点修改+区间查询（标准版）
- 区间修改+单点查询（差分版）
- 区间修改+区间查询（双树状数组）
- 二维树状数组
- 树状数组求逆序对
- 树状数组求第k小（二分查找）

```cpp
// 标准树状数组（单点修改+区间查询）
struct BIT {
    int n;
    vector<long long> c;
    BIT(int n) : n(n), c(n + 1, 0) {}
    
    void add(int idx, long long val) {
        for (; idx <= n; idx += idx & (-idx)) c[idx] += val;
    }
    
    long long sum(int idx) {
        long long res = 0;
        for (; idx > 0; idx -= idx & (-idx)) res += c[idx];
        return res;
    }
    
    long long rangeSum(int l, int r) {
        return sum(r) - sum(l - 1);
    }
};

// 差分树状数组（区间修改+单点查询）
// 对 [l,r] 加 v：add(l, v), add(r+1, -v)
// 查询 a[x]：sum(x)

// 区间修改+区间查询（双树状数组）
struct BIT2 {
    int n;
    vector<long long> s1, s2;
    BIT2(int n) : n(n), s1(n + 1, 0), s2(n + 1, 0) {}
    
    void add(vector<long long>& s, int idx, long long val) {
        for (; idx <= n; idx += idx & (-idx)) s[idx] += val;
    }
    
    long long sum(vector<long long>& s, int idx) {
        long long res = 0;
        for (; idx > 0; idx -= idx & (-idx)) res += s[idx];
        return res;
    }
    
    // 对 [l,r] 加 v
    void rangeAdd(int l, int r, long long v) {
        add(s1, l, v); add(s1, r + 1, -v);
        add(s2, l, v * (l - 1)); add(s2, r + 1, -v * r);
    }
    
    // 查询 [1,x] 的前缀和
    long long prefixSum(int x) {
        return sum(s1, x) * x - sum(s2, x);
    }
    
    // 查询 [l,r] 的区间和
    long long rangeSum(int l, int r) {
        return prefixSum(r) - prefixSum(l - 1);
    }
};

// 树状数组求第k小（二进制查找）
int kth(int k) {
    int pos = 0;
    int step = 1;
    while ((step << 1) <= n) step <<= 1;
    for (; step; step >>= 1) {
        int nxt = pos + step;
        if (nxt <= n && c[nxt] < k) {
            pos = nxt;
            k -= c[nxt];
        }
    }
    return pos + 1;
}

// 二维树状数组
void add2d(int x, int y, int val) {
    for (int i = x; i <= n; i += i & (-i))
        for (int j = y; j <= m; j += j & (-j))
            bit[i][j] += val;
}

int sum2d(int x, int y) {
    int res = 0;
    for (int i = x; i > 0; i -= i & (-i))
        for (int j = y; j > 0; j -= j & (-j))
            res += bit[i][j];
    return res;
}
// 子矩阵 (x1,y1)-(x2,y2) 的和
int query2d(int x1, int y1, int x2, int y2) {
    return sum2d(x2, y2) - sum2d(x1-1, y2) - sum2d(x2, y1-1) + sum2d(x1-1, y1-1);
}

// 树状数组+离散化求逆序对
long long countInv(vector<int>& a) {
    vector<int> vals = a;
    sort(vals.begin(), vals.end());
    vals.erase(unique(vals.begin(), vals.end()), vals.end());
    int n = vals.size();
    BIT bit(n);
    long long inv = 0;
    for (int i = 0; i < (int)a.size(); i++) {
        int rk = lower_bound(vals.begin(), vals.end(), a[i]) - vals.begin() + 1;
        inv += i - bit.sum(rk);  // 已加入的比a[i]大的个数
        bit.add(rk, 1);
    }
    return inv;
}

// 树状数组+离线查询（HH的项链模式）
// 对查询按右端点排序，维护每个颜色最后出现的位置
// 扫描右端点，在当前位置+1，在前一个位置-1
```

**相关题目**：
- [刘雨几の恼](http://scpc.fun/problem/9307)（第18题，树状数组模板）
- [三元上升子序列](http://scpc.fun/problem/10107)（第28题，树状数组+离散化）
- [出题人的手环](http://scpc.fun/problem/10108)（第29题，树状数组+逆序对）
- [火柴排队](http://scpc.fun/problem/10109)（第30题，树状数组+逆序对）
- [小石的妹子](http://scpc.fun/problem/10201)（第36题，树状数组+离散化，维护最大值）
- [情人节的电灯泡](http://scpc.fun/problem/10202)（第37题，二维树状数组）
- [小魂和他的数列](http://scpc.fun/problem/10203)（第38题，树状数组+离散化，K元上升子序列）
- [中位数](http://scpc.fun/problem/10459)（第42题，树状数组求第k小）
- [HH的项链](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P1972)（第69题，树状数组+离线查询）
- [采花](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P4113)（第73题，树状数组+离线查询）
- [【模板】树状数组 1](http://scpc.fun/problem/10103)（第89题，单点修改+区间查询）
- [【模板】树状数组 2](http://scpc.fun/problem/10104)（第90题，区间修改+单点查询）
- [【模板】树状数组 3](http://scpc.fun/problem/10105)（第91题，区间修改+区间查询）
- [逆序对](http://scpc.fun/problem/10106)（第92题，树状数组+离散化）
- [multiset](http://scpc.fun/problem/CF-1354-D)（第122题，二分+树状数组）

---

<a id="sec-5-3"></a>

### 5.3 线段树
- **适用场景**：区间修改、区间查询，支持更复杂的操作（如区间赋值、区间最值等）。
- **时间复杂度**：修改和查询均为 $O(\log n)$
- **核心思想**：将区间 $[1,n]$ 组织成完全二叉树，利用懒标记（lazy propagation）实现区间修改。

```cpp
// 线段树模板（区间GCD为例，也支持区间和/最值）
const int MAXN = 1e5 + 5;
int tree[4 * MAXN];
int arr[MAXN];
int n, m;

int gcd(int a, int b) { return b == 0 ? a : gcd(b, a % b); }

void build(int node, int start, int end) {
    if (start == end) {
        tree[node] = arr[start];
        return;
    }
    int mid = (start + end) / 2;
    build(2 * node, start, mid);
    build(2 * node + 1, mid + 1, end);
    tree[node] = gcd(tree[2 * node], tree[2 * node + 1]);  // 按需替换为max/min/sum
}

int query(int node, int start, int end, int l, int r) {
    if (r < start || l > end) return 0;  // GCD的幺元为0，sum为0，max为-INF
    if (l <= start && end <= r) return tree[node];
    int mid = (start + end) / 2;
    return gcd(query(2 * node, start, mid, l, r),
               query(2 * node + 1, mid + 1, end, l, r));
}

// 带懒标记的线段树（区间加法+区间求和）
long long tree2[4 * MAXN], lazy[4 * MAXN];

void pushDown(int node, int start, int end) {
    if (lazy[node]) {
        int mid = (start + end) / 2;
        tree2[2*node] += lazy[node] * (mid - start + 1);
        tree2[2*node+1] += lazy[node] * (end - mid);
        lazy[2*node] += lazy[node];
        lazy[2*node+1] += lazy[node];
        lazy[node] = 0;
    }
}

void updateRange(int node, int start, int end, int l, int r, long long val) {
    if (r < start || l > end) return;
    if (l <= start && end <= r) {
        tree2[node] += val * (end - start + 1);
        lazy[node] += val;
        return;
    }
    pushDown(node, start, end);
    int mid = (start + end) / 2;
    updateRange(2*node, start, mid, l, r, val);
    updateRange(2*node+1, mid+1, end, l, r, val);
    tree2[node] = tree2[2*node] + tree2[2*node+1];
}

long long queryRange(int node, int start, int end, int l, int r) {
    if (r < start || l > end) return 0;
    if (l <= start && end <= r) return tree2[node];
    pushDown(node, start, end);
    int mid = (start + end) / 2;
    return queryRange(2*node, start, mid, l, r) + queryRange(2*node+1, mid+1, end, l, r);
}
```

**相关题目**：
- [gcd区间](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P1890)（第159题，线段树区间GCD）

**易错点：**
- **数组开 4 倍大小**（n ≤ 4e5 建议开 2e6）。保守开法：`4 * (n + 10)`。
- **下传 lazy 标记**：必须先下传（push_down）到左右子节点后再递归修改/查询儿子，不要直接改父节点。
- **标记永久化**：如果不熟练，直接用普通lazy（更易写对）。
- 单点/区间修改后别忘了 **push_up**（把儿子的值合并到父节点）。
- 主席树每一次修改**新建 $O(\log n)$ 个节点**，空间通常开 **4n + n log n**。

---

<a id="sec-5-4"></a>

### 5.4 二维前缀和与二维ST表
- **适用场景**：二维平面上的区间查询。

```cpp
// 二维前缀和
for (int i = 1; i <= n; i++)
    for (int j = 1; j <= m; j++)
        pre[i][j] = pre[i-1][j] + pre[i][j-1] - pre[i-1][j-1] + a[i][j];
// 子矩阵和
ll sum = pre[x2][y2] - pre[x1-1][y2] - pre[x2][y1-1] + pre[x1-1][y1-1];

// 二维ST表/单调队列（理想的正方形：求n×n子矩阵max-min的最大值最小化）
// 先对每行做滑动窗口求max/min，再对列做滑动窗口
for (int i = 0; i < a; i++) {
    deque<int> dqMax, dqMin;
    for (int j = 0; j < b; j++) {
        // 维护行窗口的max/min
        while (!dqMax.empty() && dqMax.front() <= j - n) dqMax.pop_front();
        while (!dqMax.empty() && mat[i][dqMax.back()] <= mat[i][j]) dqMax.pop_back();
        dqMax.push_back(j);
        // 同理维护dqMin
        if (j >= n - 1) {
            rowMax[i][j-n+1] = mat[i][dqMax.front()];
            rowMin[i][j-n+1] = mat[i][dqMin.front()];
        }
    }
}
// 再对列做同样的滑动窗口
```

**相关题目**：
- [领地选择](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P2004)（第70题，二维前缀和）
- [理想的正方形](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P2216)（第71题，二维ST表/单调队列）

---

<a id="sec-5-5"></a>

### 5.5 平衡树（FHQ Treap）
- **适用场景**：需要支持插入、删除、排名、前驱后继等操作的动态有序集合。相比普通 Treap 无需旋转，实现简单。
- **时间复杂度**：期望 $O(\log n)$

**空间复杂度**：$O(n)$
- **核心思想**：每个节点存储一个随机优先级，维护堆性质。通过 split（按值/按大小）和 merge 两个核心操作实现所有功能。split 将树按值分成两棵子树；merge 将两棵子树按优先级合并。由于无需旋转，代码量小且不易写错，是竞赛中常用的平衡树实现。

**模板代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

struct FHQTreap {
    static const int INF = 0x3f3f3f3f;
    struct Node {
        int val, rnd, sz;
        int ch[2];
    };
    vector<Node> t;       // 下标 0 作为空节点
    int root = 0;
    mt19937 rng;

    FHQTreap() {
        t.push_back({0, 0, 0, 0, 0}); // 占位，使有效节点从 1 开始
        rng.seed(chrono::steady_clock::now().time_since_epoch().count());
    }

    int newNode(int v) {
        t.push_back({v, (int)rng(), 1, 0, 0});
        return (int)t.size() - 1;
    }

    void pushup(int p) {
        t[p].sz = t[t[p].ch[0]].sz + t[t[p].ch[1]].sz + 1;
    }

    // 按值分裂：val <= v 进 x 子树，val > v 进 y 子树
    void split(int p, int v, int &x, int &y) {
        if (!p) { x = y = 0; return; }
        if (t[p].val <= v) {
            x = p;
            split(t[p].ch[1], v, t[p].ch[1], y);
            pushup(x);
        } else {
            y = p;
            split(t[p].ch[0], v, x, t[p].ch[0]);
            pushup(y);
        }
    }

    // 按堆性质合并，要求 x 中所有值 <= y 中所有值
    int merge(int x, int y) {
        if (!x || !y) return x | y;
        if (t[x].rnd > t[y].rnd) {
            t[x].ch[1] = merge(t[x].ch[1], y);
            pushup(x);
            return x;
        } else {
            t[y].ch[0] = merge(x, t[y].ch[0]);
            pushup(y);
            return y;
        }
    }

    void insert(int v) {
        int x, y;
        split(root, v - 1, x, y);
        int z = newNode(v);
        root = merge(merge(x, z), y);
    }

    // 删除一个值为 v 的节点（若存在多个只删一个）
    void erase(int v) {
        int x, y, z;
        split(root, v, x, z);        // x: <= v, z: > v
        split(x, v - 1, x, y);       // y: == v
        if (y) y = merge(t[y].ch[0], t[y].ch[1]); // 丢弃根，合并子树
        root = merge(merge(x, y), z);
    }

    // 查询 v 的排名（小于 v 的个数 + 1）
    int rank(int v) {
        int x, y;
        split(root, v - 1, x, y);
        int r = t[x].sz + 1;
        root = merge(x, y);
        return r;
    }

    // 查询排名为 k 的值
    int kth(int k) {
        int p = root;
        while (true) {
            int ls = t[t[p].ch[0]].sz;
            if (k <= ls) p = t[p].ch[0];
            else if (k == ls + 1) return t[p].val;
            else { k -= ls + 1; p = t[p].ch[1]; }
        }
    }

    // 前驱：严格小于 v 的最大值
    int prev(int v) {
        int x, y;
        split(root, v - 1, x, y);
        int res = t[x].sz ? kth_helper(x, t[x].sz) : -INF;
        root = merge(x, y);
        return res;
    }

    // 后继：严格大于 v 的最小值
    int next(int v) {
        int x, y;
        split(root, v, x, y);
        int res = t[y].sz ? kth_helper(y, 1) : INF;
        root = merge(x, y);
        return res;
    }

private:
    int kth_helper(int p, int k) {
        while (true) {
            int ls = t[t[p].ch[0]].sz;
            if (k <= ls) p = t[p].ch[0];
            else if (k == ls + 1) return t[p].val;
            else { k -= ls + 1; p = t[p].ch[1]; }
        }
    }
};

int main() {
    quick;
    FHQTreap tr;
    int n;
    cin >> n;
    while (n--) {
        int op, x;
        cin >> op >> x;
        if (op == 1) tr.insert(x);
        else if (op == 2) tr.erase(x);
        else if (op == 3) cout << tr.rank(x) << endl;
        else if (op == 4) cout << tr.kth(x) << endl;
        else if (op == 5) cout << tr.prev(x) << endl;
        else if (op == 6) cout << tr.next(x) << endl;
    }
    return 0;
}
```

**使用说明**：直接定义 `FHQTreap tr;` 即可使用。`insert/erase` 支持重复值（每个值独立成节点）；`rank` 返回 1-based 排名；`kth` 返回排名为 k 的值；`prev/next` 返回严格前驱后继，无解时分别返回 `-INF/INF`。`vector` 动态分配节点，无需手动管理内存；随机种子使用 `chrono` 高精度时钟，避免被卡。

---

<a id="sec-5-6"></a>

### 5.6 字典树（Trie）
- **适用场景**：高效存储和检索字符串集合，支持前缀匹配、词频统计等。
- **时间复杂度**：插入/查询 $O(L)$，$L$ 为字符串长度

**空间复杂度**：$O(N \cdot L \cdot |\Sigma|)$
- **核心思想**：将字符串的公共前缀合并到树的路径中，每个节点代表一个字符。从根到某节点的路径构成一个字符串前缀。利用公共前缀压缩存储，查询时只需沿路径下行。

**模板代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

struct Trie {
    static const int ALPHABET = 26;
    struct Node {
        int next[ALPHABET];
        int cnt;   // 以该节点结尾的完整单词数
        int pass;  // 经过该节点的单词数（用于前缀统计）
    };
    vector<Node> t;

    Trie() {
        t.push_back({{}, 0, 0}); // 根节点下标 0
    }

    void insert(const string &s) {
        int p = 0;
        t[p].pass++;
        for (char c : s) {
            int idx = c - 'a';
            if (!t[p].next[idx]) {
                t.push_back({{}, 0, 0});
                t[p].next[idx] = (int)t.size() - 1;
            }
            p = t[p].next[idx];
            t[p].pass++;
        }
        t[p].cnt++;
    }

    // 查询字符串 s 是否作为完整单词存在
    bool search(const string &s) {
        int p = 0;
        for (char c : s) {
            int idx = c - 'a';
            if (!t[p].next[idx]) return false;
            p = t[p].next[idx];
        }
        return t[p].cnt > 0;
    }

    // 查询是否存在以 s 为前缀的单词
    bool startsWith(const string &s) {
        int p = 0;
        for (char c : s) {
            int idx = c - 'a';
            if (!t[p].next[idx]) return false;
            p = t[p].next[idx];
        }
        return t[p].pass > 0;
    }

    // 统计以 s 为前缀的单词个数
    int countPrefix(const string &s) {
        int p = 0;
        for (char c : s) {
            int idx = c - 'a';
            if (!t[p].next[idx]) return 0;
            p = t[p].next[idx];
        }
        return t[p].pass;
    }
};

int main() {
    quick;
    Trie tr;
    int n;
    cin >> n;
    while (n--) {
        int op;
        string s;
        cin >> op >> s;
        if (op == 1) tr.insert(s);
        else if (op == 2) cout << (tr.search(s) ? "YES" : "NO") << endl;
        else if (op == 3) cout << (tr.startsWith(s) ? "YES" : "NO") << endl;
        else if (op == 4) cout << tr.countPrefix(s) << endl;
    }
    return 0;
}
```

**使用说明**：默认支持小写字母 `a-z`，如需支持其他字符集，修改 `ALPHABET` 与下标映射即可。`insert` 支持重复插入（`cnt` 累加）；`search` 判断完整单词是否存在；`startsWith` 判断前缀是否存在；`countPrefix` 统计某前缀的单词数量。使用 `vector` 模拟节点数组，自动扩容，避免开固定大数组浪费空间。

---

<a id="sec-5-7"></a>

### 5.7 01Trie
- **适用场景**：维护一组整数，支持插入、删除、求异或最大值/最小值。常用于解决异或相关的问题。
- **时间复杂度**：插入/查询 $O(\log V)$，$V$ 为值域

**空间复杂度**：$O(N \cdot \log V)$
- **核心思想**：将每个整数的二进制表示从高位到低位插入 Trie。查询异或最大值时，从高位开始贪心选择与当前位不同的分支（使该位异或结果为 1），从而最大化结果。

**模板代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

struct Trie01 {
    static const int BITS = 31; // 适用于 int 范围内的非负整数
    struct Node {
        int ch[2];
        int cnt; // 经过该节点的数的个数
    };
    vector<Node> t;

    Trie01() {
        t.push_back({{0, 0}, 0}); // 根节点下标 0
    }

    void insert(int x) {
        int p = 0;
        t[p].cnt++;
        for (int i = BITS - 1; i >= 0; i--) {
            int b = (x >> i) & 1;
            if (!t[p].ch[b]) {
                t.push_back({{0, 0}, 0});
                t[p].ch[b] = (int)t.size() - 1;
            }
            p = t[p].ch[b];
            t[p].cnt++;
        }
    }

    // 删除一个 x（需保证 x 存在）
    void remove(int x) {
        int p = 0;
        t[p].cnt--;
        for (int i = BITS - 1; i >= 0; i--) {
            int b = (x >> i) & 1;
            p = t[p].ch[b];
            t[p].cnt--;
        }
    }

    // 查询 x 与集合中某数异或的最大值
    int queryMaxXor(int x) {
        int p = 0;
        int res = 0;
        for (int i = BITS - 1; i >= 0; i--) {
            int b = (x >> i) & 1;
            int want = b ^ 1; // 期望走相反分支使该位异或为 1
            if (t[p].ch[want] && t[t[p].ch[want]].cnt > 0) {
                res |= (1 << i);
                p = t[p].ch[want];
            } else {
                p = t[p].ch[b];
            }
        }
        return res;
    }

    // 查询 x 与集合中某数异或的最小值
    int queryMinXor(int x) {
        int p = 0;
        int res = 0;
        for (int i = BITS - 1; i >= 0; i--) {
            int b = (x >> i) & 1;
            int want = b; // 期望走相同分支使该位异或为 0
            if (t[p].ch[want] && t[t[p].ch[want]].cnt > 0) {
                p = t[p].ch[want];
            } else {
                res |= (1 << i);
                p = t[p].ch[b ^ 1];
            }
        }
        return res;
    }
};

int main() {
    quick;
    Trie01 tr;
    int n;
    cin >> n;
    while (n--) {
        int op, x;
        cin >> op >> x;
        if (op == 1) tr.insert(x);
        else if (op == 2) tr.remove(x);
        else if (op == 3) cout << tr.queryMaxXor(x) << endl;
        else if (op == 4) cout << tr.queryMinXor(x) << endl;
    }
    return 0;
}
```

**使用说明**：默认处理 31 位非负整数（int 范围）。若值可能为负，可统一加上偏移量转为非负再处理，或扩展为带符号位处理。`remove` 前请确保该值已存在，否则会导致 `cnt` 错乱。`queryMaxXor/queryMinXor` 要求集合非空。额外提供了 `queryMinXor` 便于求异或最小值。

---

<a id="sec-5-8"></a>

### 5.8 主席树
- **适用场景**：静态区间第 k 小查询，也可用于区间不同数个数等历史版本查询问题。
- **时间复杂度**：建树 $O(n \log n)$，查询 $O(\log n)$

**空间复杂度**：$O(n \log n)$
- **核心思想**：可持久化权值线段树。对原序列的每个前缀建一棵权值线段树，利用前缀和思想，第 $i$ 棵树由第 $i-1$ 棵树新增一个值得到。查询区间 $[l,r]$ 第 k 小等价于在第 $r$ 棵树和第 $l-1$ 棵树上做差：左右子树权值计数之差决定走向。

**模板代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

struct ChairmanTree {
    int n;                  // 离散化后值域大小
    vector<int> lc, rc, sum;
    vector<int> root;       // root[i] = 插入前 i 个元素后的版本
    int idx = 0;

    void init(int _n, int totalUpdates) {
        n = _n;
        // 预分配空间：每条链新增 log(n) 个节点
        int nodes = (totalUpdates + 1) * (20);
        lc.assign(nodes, 0);
        rc.assign(nodes, 0);
        sum.assign(nodes, 0);
        root.assign(totalUpdates + 1, 0);
        idx = 0;
        build(root[0], 1, n);
    }

    int newNode() {
        return ++idx;
    }

    void build(int &p, int l, int r) {
        p = newNode();
        if (l == r) return;
        int mid = (l + r) >> 1;
        build(lc[p], l, mid);
        build(rc[p], mid + 1, r);
    }

    int update(int pre, int l, int r, int pos) {
        int cur = newNode();
        lc[cur] = lc[pre];
        rc[cur] = rc[pre];
        sum[cur] = sum[pre] + 1;
        if (l == r) return cur;
        int mid = (l + r) >> 1;
        if (pos <= mid) lc[cur] = update(lc[pre], l, mid, pos);
        else rc[cur] = update(rc[pre], mid + 1, r, pos);
        return cur;
    }

    // 在版本 u（前 l-1 个元素）与版本 v（前 r 个元素）上查询第 k 小
    int query(int u, int v, int l, int r, int k) {
        if (l == r) return l;
        int mid = (l + r) >> 1;
        int leftCnt = sum[lc[v]] - sum[lc[u]];
        if (k <= leftCnt) return query(lc[u], lc[v], l, mid, k);
        else return query(rc[u], rc[v], mid + 1, r, k - leftCnt);
    }

    // 区间 [l, r] 第 k 小（位置 1-indexed）
    int query(int l, int r, int k) {
        return query(root[l - 1], root[r], 1, n, k);
    }
};

int main() {
    quick;
    cin.tie(nullptr);

    int n, q;
    cin >> n >> q;
    vector<int> a(n + 1);
    for (int i = 1; i <= n; i++) cin >> a[i];

    // 离散化
    vector<int> b(a.begin() + 1, a.end());
    sort(b.begin(), b.end());
    b.erase(unique(b.begin(), b.end()), b.end());
    int m = (int)b.size();
    auto getRank = [&](int x) {
        return (int)(lower_bound(b.begin(), b.end(), x) - b.begin()) + 1;
    };

    ChairmanTree ct;
    ct.init(m, n);
    for (int i = 1; i <= n; i++) {
        ct.root[i] = ct.update(ct.root[i - 1], 1, m, getRank(a[i]));
    }

    while (q--) {
        int l, r, k;
        cin >> l >> r >> k;
        int rk = ct.query(l, r, k);     // 离散化后的排名
        cout << b[rk - 1] << endl;      // 还原为原值
    }
    return 0;
}
```

**使用说明**：必须先对原序列离散化，将值域压缩到 $[1, m]$。`init(m, n)` 初始化值域大小与更新次数（一般为序列长度 $n$）。随后依次对每个前缀调用 `update` 生成新版本。查询 `query(l, r, k)` 返回的是离散化后的排名，需用原离散化数组还原为真实值。注意预分配空间约为 $n \log n$，模板中按 `20` 倍估算（对 $n \le 2 \times 10^5$ 足够），数据更大时需调大。该模板仅支持静态（无修改）区间第 k 小；带修改需结合树状数组套主席树或整体二分。

**易错点：**
- **空间不足是头号 WA/TLE 原因**：n ≤ 2e5 时建议开 `MAXN * 20`（约4e6）。
- 每次修改必须**新建节点**，不能直接修改老版本的节点。
- 离散化前记得 `sort + unique`，求下标用 `lower_bound`。
- 多组数据时，记得把 `cnt = 0; root[0] = 0;`，并按需清空 tree/lson/rson（或循环重置每棵根）。

---

<a id="sec-5-9"></a>

### 5.9 对顶堆
- **适用场景**：动态维护数据流的中位数，支持插入元素和查询中位数。
- **时间复杂度**：插入 $O(\log n)$，查询 $O(1)$

**空间复杂度**：$O(n)$
- **核心思想**：使用一个大根堆维护前半部分（较小的一半），一个小根堆维护后半部分（较大的一半）。始终保持两个堆的大小差不超过 1，且大根堆大小 $\ge$ 小根堆大小。此时大根堆堆顶即为中位数（元素个数为偶数时为下中位数）。

**模板代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

struct MedianHeap {
    priority_queue<int> lo;                              // 大根堆：较小的一半
    priority_queue<int, vector<int>, greater<int>> hi;   // 小根堆：较大的一半

    void insert(int x) {
        if (lo.empty() || x <= lo.top()) lo.push(x);
        else hi.push(x);
        // 调整使两堆大小差不超过 1，且 lo.size() >= hi.size()
        if (lo.size() > hi.size() + 1) {
            hi.push(lo.top()); lo.pop();
        } else if (hi.size() > lo.size()) {
            lo.push(hi.top()); hi.pop();
        }
    }

    // 返回中位数：奇数个返回中间值，偶数个返回下中位数
    int getMedian() {
        return lo.top();
    }

    // 返回中位数（偶数个时返回两中位数的平均值）
    double getMedianAvg() {
        if (lo.size() == hi.size()) return (lo.top() + hi.top()) / 2.0;
        return lo.top();
    }

    size_t size() { return lo.size() + hi.size(); }
};

int main() {
    quick;
    cin.tie(nullptr);

    MedianHeap mh;
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        int x;
        cin >> x;
        mh.insert(x);
        cout << mh.getMedian() << endl;
    }
    return 0;
}
```

**使用说明**：直接 `MedianHeap mh;` 后调用 `insert` 插入元素、`getMedian` 查询中位数即可。约定当总个数为奇数时返回正中间的数，偶数时 `getMedian` 返回下中位数（大根堆堆顶），`getMedianAvg` 返回上下中位数的平均值。该结构仅支持插入与查询，不支持任意删除；若需支持删除可用懒删除（额外记录待删集合）或改用平衡树/多重集合。注意插入第一个元素时需走 `lo` 分支，模板已处理 `lo.empty()` 的边界情况。

---


