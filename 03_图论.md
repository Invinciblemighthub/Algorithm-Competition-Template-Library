## [返回目录](http://scpc.fun/discussion-detail/1405)

<style>
a[id], h2, h3, h4 { scroll-margin-top: 140px; }
</style>

---

## 本章目录

- [3.1 最短路](#sec-3-1) **必学**
  - [3.1.1 Bellman-Ford](#sec-3-1-1) **进阶**
  - [3.1.2 Floyd全源最短路](#sec-3-1-2) **进阶**
- [3.2 最小生成树（kruskal算法）](#sec-3-2) **必学**
  - [3.2.1 Prim算法](#sec-3-2-1) **进阶**
- [3.3 二分图匹配](#sec-3-3) **进阶**
  - [3.3.1 KM算法](#sec-3-3-1) **选学**
  - [3.3.2 KM(费用流等价实现)](#sec-3-3-2) **选学**
- [3.4 LCA最近公共祖先](#sec-3-4) **必学**
- [3.5 树的直径与重心](#sec-3-5) **选学**
- [3.6 拓扑排序](#sec-3-6) **必学**
- [3.7 贡献法](#sec-3-7) **进阶**
- [3.8 差分约束](#sec-3-8) **进阶**
- [3.9 2-SAT](#sec-3-9) **选学**
- [3.10 最小环](#sec-3-10) **选学**
- [3.11 网络流与最小割](#sec-3-11) **进阶**
- [3.12 SCC强连通分量](#sec-3-12) **进阶**
- [3.13 割点割边与双连通分量](#sec-3-13) **进阶**
- [3.14 欧拉路径与回路](#sec-3-14) **选学**
- [3.15 Kruskal 重构树](#sec-3-15) **选学**
- [3.16 启发式合并 / DSU on Tree（树上子树查询）](#sec-3-16) **进阶**

---


<a id="ch3"></a>

## 三、图论

本板块涵盖图论经典算法，包括最短路、最小生成树、二分图匹配、LCA、树的直径与重心、拓扑排序和贡献法。



<a id="sec-3-1"></a>

### 3.1 最短路
- **适用场景**：求图中两点间的最短路径。

**核心算法**：
- Dijkstra（非负权图，单源最短路）：$O((n+m) \log n)$
- BFS（无权图）：$O(n+m)$
- 0-1 BFS（边权为0或1的图，用双端队列）

```cpp
// Dijkstra模板（堆优化）
vector<long long> dijkstra(int s, vector<vector<pair<int,long long>>>& g, int n) {
    vector<long long> dist(n + 1, 4e18);
    priority_queue<pair<long long,int>, vector<pair<long long,int>>, greater<>> pq;
    dist[s] = 0;
    pq.push({0, s});
    while (!pq.empty()) {
        auto [d, u] = pq.top(); pq.pop();
        if (d != dist[u]) continue;
        for (auto [v, w] : g[u]) {
            if (dist[v] > d + w) {
                dist[v] = d + w;
                pq.push({dist[v], v});
            }
        }
    }
    return dist;
}

// BFS求无权图最短路及路径计数
vector<int> dist(n + 1, -1), cnt(n + 1, 0);
queue<int> q;
dist[1] = 0; cnt[1] = 1; q.push(1);
while (!q.empty()) {
    int u = q.front(); q.pop();
    for (int v : g[u]) {
        if (dist[v] == -1) {
            dist[v] = dist[u] + 1;
            cnt[v] = cnt[u];
            q.push(v);
        } else if (dist[v] == dist[u] + 1) {
            cnt[v] = (cnt[v] + cnt[u]) % MOD;
        }
    }
}

// 0-1 BFS（边权为0或1，用双端队列）
deque<pair<int,int>> dq;
dq.push_front({sx, sy});
while (!dq.empty()) {
    auto [x, y] = dq.front(); dq.pop_front();
    // 边权为0的转移：push_front
    // 边权为1的转移：push_back
}
```

**相关题目**：
- [代价转移](http://scpc.fun/problem/10464)（第46题，图论+最短路建模）
- [树上最短路·单源](http://scpc.fun/problem/10458)（第108题，树遍历）
- [树上最短路·全源](http://scpc.fun/problem/10457)（第107题，全源查询）
- [树上最短路·到点集](http://scpc.fun/problem/10455)（第40题，最短路+DP）
- [最短路计数](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P1144)（第155题，BFS最短路计数）
- [Telephone Lines S](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P1948)（第67题，二分+最短路）

**易错点（所有最短路）：**
- Dijkstra **小根堆**存的是 `{距离, 节点编号}`，取出时距离大于已记录 `dist[u]` 直接跳过。
- SPFA：**判负环用入队次数** ≥ n 次，不要 ≤ n（n个节点最长路径n-1条边，入队n次说明有负环）。
- 负权图 **不能用 Dijkstra**（必须 Bellman-Ford / SPFA）。
- 多组数据：`memset(dist, 0x3f, sizeof(dist))` 或循环；INF 不要太接近 INT_MAX，避免加起来溢出。

---

<a id="sec-3-1-1"></a>

### 3.1.1 Bellman-Ford
- **适用场景**：带负权边的单源最短路；**检测图中是否存在负权环**。Dijkstra 无法处理负权边，但 Bellman-Ford 可以（代价是复杂度更高）。常用于 SPFA 写挂时对拍，或题目明确要求"第 k 步最短路"等严格有限轮松弛的场景。
- **核心思想**：对每条边做 n-1 轮松弛；若第 n 轮还能继续松弛，说明存在从源点可达的负权环。
- **时间复杂度**：$O(nm)$

```cpp
// Bellman-Ford：单源最短路 + 负环检测
// g 为边列表：{u, v, w} 表示 u -> v 有向边权 w；无向图则正反各加一条
// 返回值：true 表示存在可达负环（dist[]无意义）；false 表示 dist[1..n] 为从 s 出发的最短路
bool bellman_ford(int n, int s, vector<tuple<int,int,long long>>& edges, vector<long long>& dist) {
    dist.assign(n + 1, 4e18);
    dist[s] = 0;
    for (int i = 0; i < n - 1; i++) {           // 第 1..n-1 轮：正常松弛
        bool updated = false;
        for (auto& [u, v, w] : edges) {
            if (dist[u] != 4e18 && dist[v] > dist[u] + w) {
                dist[v] = dist[u] + w;
                updated = true;
            }
        }
        if (!updated) break;                      // 一轮内没更新，提前退出
    }
    // 第 n 轮：还能松弛 => 存在负权环
    for (auto& [u, v, w] : edges) {
        if (dist[u] != 4e18 && dist[v] > dist[u] + w) return true;
    }
    return false;
}
```

**使用说明**：
- 求 **第 k 步以内最短路**（最多走 k 条边）：把外层循环改成 `for (int i = 0; i < k; i++)`，每轮用临时数组 `ndist` 保证不会"同一轮中多次复用同一条边"。
- "存在负环（非源点可达）但只想求到各点最短路"仍可返回 `dist`，只有位于负环上/负环能到达的点 `dist` 会继续被错误松弛。
- 实战中绝大多数情况用 **SPFA（3.8 差分约束节已提供模板）** 替代 Bellman-Ford，复杂度期望更优；Bellman-Ford 主要用于"步数限制最短路"或严格对拍。

---

<a id="sec-3-1-2"></a>

### 3.1.2 Floyd全源最短路
- **适用场景**：求图中**任意两点之间**的最短路（All-Pairs Shortest Paths）。适用于数据规模较小（一般 $n \le 500$，配合 O3 可过 700~800）。亦可用于传递闭包、可达性判断。
- **核心思想**：动态规划，$f(k,i,j)$ 表示经过**编号不超过 k 的中间节点**时从 i 到 j 的最短路；枚举 k 作为中转点做松弛：$d[i][j] = \min(d[i][j], d[i][k] + d[k][j])$。
- **时间复杂度**：$O(n^3)$

```cpp
// ============= Floyd 全源最短路 =============
// g[i][j]：i -> j 的**初始边权**；无边则设为 INF（4e18 级别）；g[i][i]=0
// 传参使用引用，函数内原地更新
void floyd(int n, vector<vector<long long>>& d) {
    for (int k = 1; k <= n; k++)            // 注意：k（中转点）必须在最外层
        for (int i = 1; i <= n; i++)
            if (d[i][k] != 4e18)            // 剪枝：i 到不了 k 则跳过
                for (int j = 1; j <= n; j++)
                    d[i][j] = min(d[i][j], d[i][k] + d[k][j]);
}

// ============= Floyd 传递闭包（可达性） =============
// reach[i][j] = true 表示 i 能到达 j（仅判断连通性，非最短路）
void floyd_reach(int n, vector<vector<bool>>& reach) {
    for (int k = 1; k <= n; k++)
        for (int i = 1; i <= n; i++)
            for (int j = 1; j <= n; j++)
                reach[i][j] = reach[i][j] || (reach[i][k] && reach[k][j]);
}
```

**注意事项**：
- 循环顺序必须是 **k → i → j**，写成 i → j → k 就错了。
- 若只需要"判断两点最短路是否 ≤ 某阈值"，可以手动展开内循环 + 使用 `bitset` 加速 64 倍（传递闭包）。
- 若图中带**负权边但无负环**，Floyd 依然正确；若图中存在**负权环**，则环上所有点对自身的 `d[i][i]` 会变成负数，可据此检测。
- **带负权的无向图最小环**请见 3.10 节（使用 Floyd 的变种解法）。

---

<a id="sec-3-2"></a>

### 3.2 最小生成树（kruskal算法）
- **适用场景**：在连通图中选取 $n-1$ 条边使图连通且边权之和最小。
- **时间复杂度**：Kruskal $O(m \log m)$，Prim $O(n^2)$
- **核心思想**：Kruskal算法按边权排序，用并查集判断是否成环。

```cpp
// Kruskal模板
struct Edge { int u, v, w; };
bool operator<(const Edge& a, const Edge& b) { return a.w < b.w; }

int fa[MAXN];
int find(int x) { return fa[x] == x ? x : fa[x] = find(fa[x]); }

int kruskal(vector<Edge>& edges, int n) {
    sort(edges.begin(), edges.end());
    for (int i = 1; i <= n; i++) fa[i] = i;
    int ans = 0, cnt = 0;
    for (auto& e : edges) {
        int fu = find(e.u), fv = find(e.v);
        if (fu != fv) {
            fa[fu] = fv;
            ans += e.w;
            if (++cnt == n - 1) break;
        }
    }
    return cnt == n - 1 ? ans : -1;  // -1表示图不连通
}
```

**相关题目**：
- [买礼物](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P1194)（第66题，最小生成树建图）
- [繁忙的都市](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P2330)（第160题，最小生成树最大边）
- [局域网](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P2820)（第162题，最小生成树求删边最大）

---

<a id="sec-3-2-1"></a>

### 3.2.1 Prim算法
- **适用场景**：**稠密完全图**的最小生成树（例如邻接矩阵存图、点少边多）。Kruskal 对边排序适合稀疏图；Prim 在稠密图上比 Kruskal 常数更小（无需排序、无需并查集）。
- **核心思想**：从任意起点开始，每次在"已加入连通块的点集 S"到"剩余点集 V\S"之间选择权最小的跨边，把对应点加入 S；重复 n-1 次。
- **时间复杂度**：邻接矩阵版 $O(n^2)$；堆优化版 $O(m \log n)$（与 Kruskal 相当，通常直接用 Kruskal 即可，因此模板只给最常用的 $O(n^2)$ 邻接矩阵版）。

```cpp
// Prim O(n^2) 邻接矩阵版
// w[i][j]：i 与 j 之间的边权；无边则为 INF（4e18 级别）；w[i][i]=0
// 返回值：最小生成树的总边权；若图不连通返回 -1
long long prim(int n, vector<vector<long long>>& w) {
    vector<long long> dist(n + 1, 4e18);   // dist[u]：点 u 到当前连通块 S 的最短边权
    vector<bool> inS(n + 1, false);        // inS[u]：u 是否已加入连通块 S
    dist[1] = 0;                           // 任选起点 1
    long long ans = 0;
    int cnt = 0;
    for (int i = 1; i <= n; i++) {         // 共 n 次：每次选 1 个加入 S
        // 1. 找不在 S 中且 dist 最小的点 u
        int u = -1;
        long long mn = 4e18;
        for (int v = 1; v <= n; v++) {
            if (!inS[v] && dist[v] < mn) { mn = dist[v]; u = v; }
        }
        if (u == -1) return -1;            // 找不到 => 原图不连通
        // 2. 把 u 加入 S，累加这条最短跨边的权值
        inS[u] = true;
        ans += mn;
        cnt++;
        // 3. 用 u 做中转，更新剩余点到 S 的最短边权
        for (int v = 1; v <= n; v++) {
            if (!inS[v] && w[u][v] < dist[v])
                dist[v] = w[u][v];
        }
    }
    return cnt == n ? ans : -1;
}
```

**使用说明**：
- 起点可以任选（MST总权唯一，与起点无关）。
- 若要求输出**具体选了哪 n-1 条边**，额外开 `from[]` 数组记录"每个点加入S时，是通过哪条边"——当 `dist[v] > w[u][v]` 更新时同时令 `from[v]=u`，最终遍历 `from[2..n]` 即可。
- 稀疏图直接用 Kruskal（3.2 节模板）即可，无需 Prim。

---

<a id="sec-3-3"></a>

### 3.3 二分图匹配
- **适用场景**：二分图最大匹配问题。
- **时间复杂度**：匈牙利算法 $O(nm)$
- **核心思想**：对每个左部点，尝试用DFS寻找增广路。

```cpp
// 匈牙利算法模板
vector<int> adj[MAXN], matchR(MAXN);
vector<bool> vis(MAXN);

bool dfs(int u) {
    for (int v : adj[u]) {
        if (!vis[v]) {
            vis[v] = true;
            if (matchR[v] == -1 || dfs(matchR[v])) {
                matchR[v] = u;
                return true;
            }
        }
    }
    return false;
}

int hungarian(int n, int m) {
    matchR.assign(m + 1, -1);
    int ans = 0;
    for (int i = 1; i <= n; i++) {
        vis.assign(m + 1, false);
        if (dfs(i)) ans++;
    }
    return ans;
}
```

**相关题目**：
- [填坑](http://scpc.fun/problem/10033)（第1题，二分图匹配）
- [二分图匹配](http://scpc.fun/problem/9186)（第5题，二分图匹配模板）

---

<a id="sec-3-3-1"></a>

### 3.3.1 KM算法
- **适用场景**：二分图**最大权完美匹配**（左右两部点数相同、要求每个左部点都匹配到右部点，总权值最大）。在"指派问题""任务分配"等场景常见。
- **核心思想**：给每个左右部点分配**顶标** $lx[i]$、$ly[j]$，保证 $lx[i] + ly[j] \ge w[i][j]$；每次在顶标满足的**相等子图**（即 $lx[i]+ly[j]=w[i][j]$ 的边构成的子图）上用 BFS 寻找增广路；若找不到则整体下调一部分顶标、上调另一部分顶标（通过最小松弛量 `slack`），继续迭代。
- **时间复杂度**：$O(n^3)$（BFS 写法），左部右部各 $n$ 个点。

```cpp
// ============= KM O(n^3) 邻接矩阵 BFS 版 =============
// 左右部点数均为 n（要求完美匹配，左右必须同部数 n）
// w[i][j]：左 i -> 右 j 的边权；要求边权 long long；若无边可设为 -INF
// 用法：调用 km(n, w) 返回最大权完美匹配总权；match[j] 存右 j 匹配的左部点编号

const long long KMINF = 4e18;

long long km(int n, vector<vector<long long>>& w, vector<int>& match) {
    vector<long long> lx(n + 1, -KMINF), ly(n + 1, 0);  // 顶标
    vector<int> vy(n + 1), pre(n + 1);                   // vy[j] = 右j本轮是否访问过；pre[j] = 交错树父
    vector<long long> slack(n + 1);
    match.assign(n + 1, 0);
    // 初始化左部顶标 = 出边最大权
    for (int i = 1; i <= n; i++)
        for (int j = 1; j <= n; j++)
            lx[i] = max(lx[i], w[i][j]);

    auto bfs_aug = [&](int u) {             // 对左u BFS增广
        int a, y = 0, y_prev = 0;
        long long d;
        fill(vy.begin(), vy.end(), 0);
        fill(slack.begin(), slack.end(), KMINF);
        match[0] = u;                        // 虚拟点0匹配到u（入口）
        while (match[y]) {
            a = match[y];
            d = KMINF;
            vy[y] = 1;
            for (int j = 1; j <= n; j++) {
                if (!vy[j]) {
                    long long t = lx[a] + ly[j] - w[a][j];
                    if (slack[j] > t) { slack[j] = t; pre[j] = y; }
                    if (slack[j] < d)      { d = slack[j];   y_prev = j; }
                }
            }
            for (int j = 0; j <= n; j++) {
                if (vy[j]) { lx[match[j]] -= d; ly[j] += d; }
                else slack[j] -= d;
            }
            y = y_prev;
        }
        while (y) { match[y] = match[pre[y]]; y = pre[y]; }  // 取反增广路
    };

    for (int i = 1; i <= n; i++) bfs_aug(i);
    long long ans = 0;
    for (int j = 1; j <= n; j++)
        if (match[j]) ans += w[match[j]][j];
    return ans;
}
```

**使用注意事项**：
- **转换求最小权**：把所有权值取负后调用 km，结果再取负。
- **左右部点数不等 / 不要求完美匹配**：补虚点使得 $n' = \max(n_L, n_R)$，补边权设为 0（求最大权时）或 INF（求最小权时）。例如"左3右5，最大化匹配权"→ $n'=5$，左部补 2 个虚拟点，所有补边权=0。
- 当边权非常大（超过 $10^9$）或存在负值时，注意 `KMINF` 的绝对值要**远大于**所有可能边权之和，避免顶标越界。

---

<a id="sec-3-3-2"></a>

### 3.3.2 KM(费用流等价实现)
- **适用场景**：二分图加权匹配的通用实现。与严格KM不同，它**天然支持左右部点数不等、非完全二分图、非完美匹配**。当数据规模较小（$n,m$ 不超过几千到几万）或 KM 边界条件复杂时，费用流写法更稳妥、不易写错。

**建模方法**：
1. 新建**超级源 S=0**、**超级汇 T = nL + nR + 1**。
2. S 向每个左部点 $i$ 连：容量 1，费用 0。
3. 每个右部点 $j'$ 向 T 连：容量 1，费用 0。
4. 原图每对 $(i, j)$ 有权 $w$（要最大化）：左 $i$ → 右 $j'$，容量 1，费用 $-w$（取负后最小费用 = 最大权）。
5. 跑 S→T 的**最小费用最大流**，总费用取反就是二分图最大权匹配值。
- **时间复杂度**：SPFA版 $O(F \cdot E)$（本题中最大流 $F \le \min(n_L, n_R)$）。

```cpp
// ============= 最小费用流（SPFA连续最短路版）=============
// 同时求最大流与最小费用
struct MCMF {
    struct Edge { int to, cap, nxt; long long cost; };
    vector<Edge> e;
    vector<int> head, inq, pre_e;
    vector<long long> dist;
    int n, S, T;
    MCMF(int n_, int S_, int T_) : n(n_), S(S_), T(T_) {
        head.assign(n + 2, -1); inq.resize(n + 2);
        pre_e.assign(n + 2, -1); dist.resize(n + 2);
    }
    void add_edge(int u, int v, int cap, long long cost) {
        e.push_back({v, cap, head[u], cost}); head[u] = e.size() - 1;
        e.push_back({u, 0,   head[v], -cost}); head[v] = e.size() - 1;
    }
    bool spfa() {
        fill(dist.begin(), dist.end(), KMINF);
        fill(inq.begin(), inq.end(), 0);
        queue<int> q;
        q.push(S); dist[S] = 0; inq[S] = 1;
        while (!q.empty()) {
            int u = q.front(); q.pop(); inq[u] = 0;
            for (int i = head[u]; ~i; i = e[i].nxt) {
                int v = e[i].to;
                if (e[i].cap && dist[v] > dist[u] + e[i].cost) {
                    dist[v] = dist[u] + e[i].cost;
                    pre_e[v] = i;
                    if (!inq[v]) { q.push(v); inq[v] = 1; }
                }
            }
        }
        return dist[T] != KMINF;
    }
    pair<int, long long> work() {
        int flow = 0; long long cost = 0;
        while (spfa()) {
            int f = INT_MAX;
            for (int u = T; u != S; u = e[pre_e[u]^1].to) f = min(f, e[pre_e[u]].cap);
            for (int u = T; u != S; u = e[pre_e[u]^1].to) {
                e[pre_e[u]].cap -= f; e[pre_e[u]^1].cap += f;
            }
            flow += f; cost += 1LL * f * dist[T];
        }
        return {flow, cost};
    }
};

// ============= 用 MCMF 实现二分图加权匹配 =============
// nL 左部点数 1..nL；nR 右部点数 nL+1..nL+nR
// w[i][j] 左i -> 右j 的边权；无边则不要加边
// 返回：{匹配数, 最大匹配总权}
pair<int, long long> km_mcmf(int nL, int nR, vector<tuple<int,int,long long>>& edges) {
    int S = 0, T = nL + nR + 1;
    MCMF mcmf(T, S, T);
    for (int i = 1; i <= nL; i++) mcmf.add_edge(S, i, 1, 0);          // S → 左
    for (int j = 1; j <= nR; j++) mcmf.add_edge(nL + j, T, 1, 0);      // 右 → T
    for (auto& [i, j, val] : edges)
        mcmf.add_edge(i, nL + j, 1, -val);                             // 左 → 右（费用取负）
    auto [flow, cost] = mcmf.work();
    return {flow, -cost};                                              // 费用取反 = 最大权
}
```

**对比 3.3.1 纯KM**：
- 纯 KM 在 $n \le 800$ 的完全二分图上依然能过（$O(n^3)$）；费用流写法在 $n \ge 300$ 的完全图会 TLE。
- 费用流好处是**通用**（非完美/非完全/左右不等部统统不用特判），建模清晰。在非完全二分图或 $n \le 200$ 的场景下优先选本模板。

---

<a id="sec-3-4"></a>

### 3.4 LCA最近公共祖先
- **适用场景**：树上一对节点的最近公共祖先查询。
- **时间复杂度**：预处理 $O(n \log n)$，查询 $O(\log n)$
- **核心思想**：倍增法，预处理每个节点向上跳 $2^j$ 步的祖先。

```cpp
// 倍增LCA模板
const int LOG = 20;
vector<vector<pair<int,long long>>> g;
vector<int> depth;
vector<long long> dist;
vector<vector<int>> up;

void dfs(int u, int p) {
    up[u][0] = p;
    for (int i = 1; i < LOG; i++)
        up[u][i] = up[up[u][i-1]][i-1];
    for (auto [v, w] : g[u]) {
        if (v == p) continue;
        depth[v] = depth[u] + 1;
        dist[v] = dist[u] + w;
        dfs(v, u);
    }
}

int lca(int u, int v) {
    if (depth[u] < depth[v]) swap(u, v);
    int diff = depth[u] - depth[v];
    for (int i = 0; i < LOG; i++)
        if (diff & (1 << i)) u = up[u][i];
    if (u == v) return u;
    for (int i = LOG - 1; i >= 0; i--) {
        if (up[u][i] != up[v][i]) {
            u = up[u][i];
            v = up[v][i];
        }
    }
    return up[u][0];
}

// 树上两点距离 = dist[u] + dist[v] - 2 * dist[lca(u,v)]
```

**相关题目**：
- [区间LCA](http://scpc.fun/problem/10454)（第2题，LCA+DFN序列+ST表）
- [祖孙询问](http://scpc.fun/problem/10121)（第33题，LCA判定祖孙关系）
- [距离](http://scpc.fun/problem/10122)（第34题，LCA求树上距离）
- [树上最短路·LCA在线查询](http://scpc.fun/problem/10456)（第41题，LCA在线查询）

---

<a id="sec-3-5"></a>

### 3.5 树的直径与重心
- **适用场景**：
- 直径：树中最长路径
- 重心：使最大子树最小的节点
- **时间复杂度**：$O(n)$

```cpp
// 树的直径（两次BFS）
pair<int, long long> bfs(int src, vector<vector<pair<int, long long>>>& g, int n) {
    vector<long long> dist(n + 1, -1);
    queue<int> q;
    q.push(src);
    dist[src] = 0;
    int far = src;
    long long maxd = 0;
    while (!q.empty()) {
        int u = q.front(); q.pop();
        for (auto [v, w] : g[u]) {
            if (dist[v] == -1) {
                dist[v] = dist[u] + w;
                q.push(v);
                if (dist[v] > maxd) { maxd = dist[v]; far = v; }
            }
        }
    }
    return {far, maxd};
}
// 用法：int u = bfs(1).first; long long diameter = bfs(u).second;

// 树的重心（使各子树大小最大值最小的节点）
int sz[MAXN];
void dfs(int u, int fa) {
    sz[u] = 1;
    for (int v : g[u]) {
        if (v != fa) {
            dfs(v, u);
            sz[u] += sz[v];
        }
    }
}
// 对于节点u，最大子树 = max(max(sz[v]), n - sz[u])
```

**相关题目**：
- [树的最长路径](http://scpc.fun/problem/ACWING-1072)（第154题，树的直径两次BFS）
- [会议](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P1395)（第158题，树的重心+换根DP）

---

<a id="sec-3-6"></a>

### 3.6 拓扑排序
- **适用场景**：DAG（有向无环图）的节点排序，常用于依赖关系处理。
- **时间复杂度**：$O(n + m)$
- **核心思想**：BFS实现（Kahn算法），每次取入度为0的节点入队。

```cpp
// 拓扑排序模板（BFS/Kahn算法）
vector<int> topoSort(int n, vector<vector<int>>& g, vector<int>& in) {
    queue<int> q;
    for (int i = 1; i <= n; i++)
        if (in[i] == 0) q.push(i);
    vector<int> order;
    while (!q.empty()) {
        int u = q.front(); q.pop();
        order.push_back(u);
        for (int v : g[u]) {
            if (--in[v] == 0) q.push(v);
        }
    }
    return order;  // 若order.size() < n 则有环
}

// 拓扑排序+DP（如最大食物链计数）
vector<int> dp(n + 1, 0);
for (int i = 1; i <= n; i++)
    if (in[i] == 0) { dp[i] = 1; q.push(i); }
while (!q.empty()) {
    int u = q.front(); q.pop();
    for (int v : g[u]) {
        dp[v] = (dp[v] + dp[u]) % MOD;
        if (--in[v] == 0) q.push(v);
    }
}
```

**相关题目**：
- [最大食物链计数](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P4017)（第72题，拓扑排序+DP）
- [信息传递](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P2661)（第161题，拓扑排序求最小环）

---

<a id="sec-3-7"></a>

### 3.7 贡献法

- **适用场景**：不直接计算全局答案，而是逐个元素计算其对答案的贡献后累加。
- **时间复杂度**：取决于具体问题，通常为 $O(n)$ 或 $O(n \log n)$。
- **核心思想**：将全局答案拆解为每个基本元素（节点、边、位置等）的独立贡献之和。关键在于找出"每个元素被统计了多少次"，而非暴力枚举所有方案。

**通用贡献法框架：**

```cpp
// 通用贡献法思路：
//   step1: 确定答案的"基本元素"（如节点、边、位置）
//   step2: 计算每个基本元素对答案的贡献（即被统计的次数）
//   step3: 累加所有贡献得到全局答案
//
// 典型应用场景：
//   1. 树上路径和：每个节点对答案的贡献 = w[u] * 经过u的路径数
//   2. 区间计数：每个元素对满足条件的区间数的贡献
//   3. 组合计数：每个选择对总方案数的贡献
```

- **完整模板：树的路径和**

给定一棵 $n$ 个节点的树，每个节点有权值 $w[u]$，求所有有序路径（包括单点路径 $(u,u)$）的权值和。路径权值 = 路径上所有节点权值之和。

对于节点 $u$，经过 $u$ 的有序路径数 $= n^2 - \sum sz_v^2$，其中 $sz_v$ 为 $u$ 的各子树大小（包括上方子树 $n - sz[u]$）。

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 100010;

int n;
long long w[MAXN];
vector<int> G[MAXN];
int sz[MAXN];
long long ans = 0;

void dfs(int u, int fa) {
    sz[u] = 1;
    long long sum_sq = 0;            // sum(sz[v]^2)
    for (int v : G[u]) {
        if (v == fa) continue;
        dfs(v, u);
        sz[u] += sz[v];
        sum_sq += (long long)sz[v] * sz[v];
    }
    int up = n - sz[u];              // 上方子树大小
    sum_sq += (long long)up * up;
    long long cnt = (long long)n * n - sum_sq;   // 经过 u 的路径数
    ans += w[u] * cnt;
}

int main() {
    quick;
    cin >> n;
    for (int i = 1; i <= n; i++) cin >> w[i];
    for (int i = 1; i < n; i++) {
        int u, v; cin >> u >> v;
        G[u].push_back(v);
        G[v].push_back(u);
    }
    dfs(1, 0);
    cout << ans << endl;
    return 0;
}
```

**相关题目**：
- [树的路径和](http://scpc.fun/problem/10470)（第109题，贡献法+DFS，每个节点计算路径贡献）

---

<a id="sec-3-8"></a>

### 3.8 差分约束

- **适用场景**：给定一组形如 $x_i - x_j \leq c$ 的不等式约束，求一组可行解或判断无解。常用于求最大/最小值问题。
- **时间复杂度**：$O(nm)$（SPFA），$O((n+m)\log n)$（Dijkstra）
- **核心思想**：将每个不等式 $x_i - x_j \leq c$ 转化为图中从 $j$ 到 $i$ 权值为 $c$ 的有向边，然后建立超级源点向所有点连边权为 $0$ 的边，跑最短路。若存在负环则无解；否则 $dist[i]$ 就是一组可行解。求最大值用最短路（不等式化为 $\leq$），求最小值用最长路（不等式化为 $\geq$）。

- **模板代码：**

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 100010;              // 最大点数(含超级源点0)
const int MAXM = 500010;              // 最大边数
const long long INF = 0x3f3f3f3f3f3f3f3fLL;

int head[MAXN], nxt[MAXM], to[MAXM], tot;
long long w[MAXM];

void add(int u, int v, long long c) {
    to[++tot] = v;
    w[tot] = c;
    nxt[tot] = head[u];
    head[u] = tot;
}

int n, m;
long long dist[MAXN];
int pre[MAXN];                         // pre[u]: 源点到u的最短路边数, 用于判负环
bool inq[MAXN];                        // inq[u]: u是否在队列中

// SPFA: 求从s出发的最短路, 同时判负环
// 返回 true: 无负环(有解); false: 存在负环(无解)
bool spfa(int s) {
    queue<int> q;
    for (int i = 0; i <= n; i++) {
        dist[i] = INF;
        pre[i] = 0;
        inq[i] = false;
    }
    dist[s] = 0;
    inq[s] = true;
    q.push(s);
    int N = n + 1;                     // 总点数(含超级源点)
    while (!q.empty()) {
        int u = q.front(); q.pop();
        inq[u] = false;
        for (int i = head[u]; i; i = nxt[i]) {
            int v = to[i];
            if (dist[v] > dist[u] + w[i]) {
                dist[v] = dist[u] + w[i];
                pre[v] = pre[u] + 1;   // 最短路边数
                if (pre[v] >= N) return false;  // 边数 >= 总点数, 存在负环
                if (!inq[v]) {
                    inq[v] = true;
                    q.push(v);
                }
            }
        }
    }
    return true;
}

int main() {
    quick;
    cin >> n >> m;
    // 超级源点 0 向每个点连边权 0 的边, 保证所有点可达
    for (int i = 1; i <= n; i++) add(0, i, 0);
    // 不等式 x_u - x_v <= c  →  建边 v -> u, 权值 c
    for (int k = 0; k < m; k++) {
        int u, v; long long c;
        cin >> u >> v >> c;            // 约束: x_u - x_v <= c
        add(v, u, c);
    }
    if (!spfa(0)) {
        cout << "NO"<<endl;                // 存在负环, 无解
    } else {
        // dist[i] 即为一组可行解, 满足所有 x_u - x_v <= c
        for (int i = 1; i <= n; i++)
            cout << dist[i] << " \n"[i == n];
    }
    return 0;
}
```

**使用说明：**
- `add(u, v, c)` 建立从 $u$ 到 $v$ 权值为 $c$ 的有向边（链式前向星建图）
- 不等式 $x_u - x_v \leq c$ 转化为 `add(v, u, c)`（注意方向是 $v \to u$）
- 超级源点 $0$ 向所有点连边权 $0$ 的边，保证从源点能到达每个点
- `spfa(0)` 返回 `true` 表示有解，`dist[i]` 即为 $x_i$ 的一组可行解（满足所有 $\leq$ 约束）；返回 `false` 表示存在负环、无解
- 求最大值：用最短路（不等式化为 $\leq$，如本模板）
- 求最小值：用最长路（不等式化为 $\geq$，将松弛改为 `dist[v] < dist[u] + w[i]` 并判正环）
- 常见约束转化（求最短路时）：$x_u - x_v \geq c \Rightarrow$ `add(u, v, -c)`；$x_u = x_v \Rightarrow$ 同时建 `add(u, v, 0)` 和 `add(v, u, 0)`
- 判负环原理：最短路边数 `pre[v]` 达到总点数 $N$ 时说明路径上出现重复点，即存在负环

**建模思路与常见套路：**

- **不等式转图论边**：每个形如 $x_j - x_i \leq c$ 的约束，连一条有向边 $i \rightarrow j$，边权 $c$。
- **建立超级源点 S**：从 S 向所有点连权值 0 的边（或直接把所有点初始距离设为0并入队），保证图连通。
- **最大值 vs 最小值**：
  - 求 **$x_t - x_s$ 最大值** → 跑从 $s$ 出发的**最短路**（所有约束都是上界）。
  - 求 **$x_t - x_s$ 最小值** → 把不等式变形为 $x_i - x_j \geq -c$（即 $x_j - x_i \leq c$），或统一改为**最长路**（此时约束为下界，边权取反后跑最短路等价）。
- **负环 = 无解**：SPFA 中某点入队次数 ≥ n+1 → 存在负环 → 不等式组无解。
- **绝对条件转约束**：若有 $x_i \geq a$ 或 $x_i \leq a$，令 $x_0=0$（虚拟变量），则：
  - $x_i \leq a \Rightarrow x_i - x_0 \leq a$，连边 $0 \rightarrow i$，权 $a$。
  - $x_i \geq a \Rightarrow x_0 - x_i \leq -a$，连边 $i \rightarrow 0$，权 $-a$。

---

<a id="sec-3-9"></a>

### 3.9 2-SAT

- **适用场景**：给定 $n$ 个布尔变量和若干形如 $(a \lor b)$、$(\neg a \lor b)$ 等的析取约束，判断是否存在合法赋值。
- **时间复杂度**：$O(n+m)$
- **核心思想**：每个变量 $x$ 拆成两个点 $x_0$（false）和 $x_1$（true）。对于约束 $a \lor b$，等价于 $\neg a \Rightarrow b$ 和 $\neg b \Rightarrow a$，建两条有向边。用 Tarjan 求 SCC，若 $x_0$ 和 $x_1$ 在同一 SCC 则无解；否则按 SCC 编号逆序赋值（编号小的取 true）。

- **模板代码：**

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 2000010;

int head[MAXN], nxt[MAXN], to[MAXN], tot;
void add(int u, int v) {
    to[++tot] = v;
    nxt[tot] = head[u];
    head[u] = tot;
}

int n, m;
int dfn[MAXN], low[MAXN], scc[MAXN], timer_, scnt;
int stk_[MAXN], top_;
bool instk[MAXN];

void tarjan(int u) {
    dfn[u] = low[u] = ++timer_;
    stk_[++top_] = u;
    instk[u] = true;
    for (int i = head[u]; i; i = nxt[i]) {
        int v = to[i];
        if (!dfn[v]) {
            tarjan(v);
            low[u] = min(low[u], low[v]);
        } else if (instk[v]) {
            low[u] = min(low[u], dfn[v]);
        }
    }
    if (dfn[u] == low[u]) {
        scnt++;
        while (true) {
            int x = stk_[top_--];
            instk[x] = false;
            scc[x] = scnt;
            if (x == u) break;
        }
    }
}

int main() {
    quick;
    cin >> n >> m;
    // 变量 x (1..n): x_0(false) = 2*x, x_1(true) = 2*x+1
    for (int k = 0; k < m; k++) {
        int a, va, b, vb;            // 约束: (x_a = va) ∨ (x_b = vb), va,vb ∈ {0,1}
        cin >> a >> va >> b >> vb;
        // ¬(a=va) -> (b=vb) : (a=va^1) -> (b=vb)
        add(2 * a + (va ^ 1), 2 * b + vb);
        // ¬(b=vb) -> (a=va) : (b=vb^1) -> (a=va)
        add(2 * b + (vb ^ 1), 2 * a + va);
    }
    // Tarjan 求SCC (节点编号 2..2n+1)
    for (int i = 2; i <= 2 * n + 1; i++)
        if (!dfn[i]) tarjan(i);
    // 判定: 若 x_0 与 x_1 在同一 SCC 则无解
    for (int i = 1; i <= n; i++) {
        if (scc[2 * i] == scc[2 * i + 1]) {
            cout << "IMPOSSIBLE"<<endl;
            return 0;
        }
    }
    // 赋值: SCC编号小的取 true → x_1(true) 的 SCC 编号 < x_0(false) 时, x = true
    cout << "POSSIBLE"<<endl;
    for (int i = 1; i <= n; i++)
        cout << (scc[2 * i + 1] < scc[2 * i]) << " \n"[i == n];
    return 0;
}
```

**使用说明：**
- 变量 $x$（$1..n$）拆为 $x_0$（false，节点 $2x$）和 $x_1$（true，节点 $2x+1$）
- 输入格式：每行 `a va b vb` 表示约束 $(x_a = v_a) \lor (x_b = v_b)$，其中 $v_a, v_b \in \{0,1\}$
- 该格式覆盖四种子句：$(a \lor b)$→`a 1 b 1`，$(\neg a \lor b)$→`a 0 b 1`，$(a \lor \neg b)$→`a 1 b 0`，$(\neg a \lor \neg b)$→`a 0 b 0`
- 析取 $a \lor b$ 等价于 $\neg a \Rightarrow b$ 与 $\neg b \Rightarrow a$，各建一条有向边
- 判定：若某变量 $x$ 的 $x_0$ 与 $x_1$ 落在同一 SCC，输出 `IMPOSSIBLE`
- 赋值：按 SCC 编号逆序，编号小的取 true，即 $x = (scc[2x+1] < scc[2x])$
- 注意：节点数较多时递归 Tarjan 可能爆栈，可改为迭代实现或手动扩栈

**建模思路与常见套路：**

每个布尔变量 $x$ 对应两个点：$2x$ 表示"$x$ 为假"，$2x+1$ 表示"$x$ 为真"。对每个蕴含式 $a \Rightarrow b$ 连**有向边** $a \rightarrow b$（同时建**逆否命题**边 $\neg b \rightarrow \neg a$）。

- **基础蕴含式：**
  - $a \lor b$（至少一个为真）：$\neg a \Rightarrow b$ 且 $\neg b \Rightarrow a$ → 连两条边。
  - $a \rightarrow b$（若a则b）：直接连 $a \rightarrow b$ + $\neg b \rightarrow \neg a$。
  - $a \oplus b$（恰好一个为真，异或）：$a \Leftrightarrow \neg b$ → 连 $a \to \neg b$、$b \to \neg a$ 及其逆否。
  - $a = b$（两者同值）：$a \Leftrightarrow b$ → 连互相蕴含 + 逆否，共4条边。
- **求解：** 跑 Tarjan SCC，若 $\exists x, x$ 和 $\neg x$ 在同一 SCC → 无解；否则选 SCC 拓扑序更靠后的取值作为该变量的真值。
- **常见应用**："A选或B选"、"A和B不能相邻"、"开/关状态约束"等二元决策问题。

---

<a id="sec-3-10"></a>

### 3.10 最小环

- **适用场景**：求图中边权和最小的环（至少 $3$ 个点的环）。
- **时间复杂度**：$O(n^3)$（Floyd），$O(nm\log n)$（Dijkstra 枚举）
- **核心思想**：Floyd 算法在枚举中转点 $k$ 时，在用 $k$ 松弛之前，检查所有 $i,j < k$ 的边是否与 $k$ 构成环：$ring = dist[i][j] + w[j][k] + w[k][i]$。取所有环的最小值即为答案。

- **模板代码：**

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 510;
const int INF = 0x3f3f3f3f;

int n, m;
int g[MAXN][MAXN];   // 原始邻接矩阵(不参与松弛)
int d[MAXN][MAXN];   // Floyd 最短路矩阵

int main() {
    quick;
    cin >> n >> m;
    memset(g, 0x3f, sizeof(g));
    for (int i = 1; i <= n; i++) g[i][i] = 0;
    for (int k = 0; k < m; k++) {
        int u, v, w; cin >> u >> v >> w;
        // 无向图取双向 min; 有向图改为 g[u][v] = min(g[u][v], w);
        g[u][v] = g[v][u] = min(g[u][v], w);
    }
    memcpy(d, g, sizeof(g));
    int ans = INF;
    for (int k = 1; k <= n; k++) {
        // 在用 k 松弛之前, 检查所有 i < j < k 是否与 k 构成环
        for (int i = 1; i < k; i++)
            for (int j = i + 1; j < k; j++)
                if (d[i][j] < INF && g[j][k] < INF && g[k][i] < INF)
                    ans = min(ans, d[i][j] + g[j][k] + g[k][i]);
        // 用 k 松弛 (标准 Floyd)
        for (int i = 1; i <= n; i++)
            for (int j = 1; j <= n; j++)
                if (d[i][k] + d[k][j] < d[i][j])
                    d[i][j] = d[i][k] + d[k][j];
    }
    if (ans >= INF) cout << "No solution."<<endl;
    else cout << ans << endl;
    return 0;
}
```

**使用说明：**
- `g[i][j]` 保留原始边权（不参与松弛），`d[i][j]` 为 Floyd 最短路矩阵
- 枚举中转点 $k$，在用 $k$ 松弛之前，检查所有 $i < j < k$ 的点对：环长 $= d[i][j] + g[j][k] + g[k][i]$
- 必须在松弛前检查，否则 $d[i][j]$ 可能已经过 $k$，导致环不简单
- 无向图建双向边；有向图改为只建 `g[u][v]`（此时环方向为 $i \to \dots \to j \to k \to i$）
- 要求边权为正（保证最短路为简单路径，环不重边）；若不存在环输出 `No solution.`
- 适用于 $n$ 较小（$\leq 500$）的稠密图；稀疏图可用 Dijkstra 枚举每个点作源 $O(nm\log n)$

---

<a id="sec-3-11"></a>

### 3.11 网络流与最小割

- **适用场景**：最大流、二分图匹配、最小割建模（最大流等于最小割）。
- **时间复杂度**：$O(n^2 m)$（一般图），$O(m\sqrt{n})$（二分图匹配）。
- **核心思想**：基于层次图（BFS 分层）+ DFS 增广。每轮 BFS 建立分层图后，DFS 只在层次图中找增广路，配合当前弧优化避免重复遍历已饱和边。最大流等于最小割（最大流-最小割定理）。

- **模板代码：**

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 10010;
const int MAXM = 200010;
const int INF  = 0x3f3f3f3f;

int head[MAXN], nxt[MAXM], to[MAXM], cap[MAXM], tot = 1;
int  dep[MAXN], cur[MAXN];          // cur: 当前弧优化
int  que[MAXN], hh, tt;

inline void add_edge(int u, int v, int c) {
    nxt[++tot] = head[u]; to[tot] = v; cap[tot] = c; head[u] = tot;
    nxt[++tot] = head[v]; to[tot] = u; cap[tot] = 0; head[v] = tot; // 反向边初始容量为 0
}

// BFS 分层，建立层次图
bool bfs(int s, int t, int n) {
    for (int i = 1; i <= n; i++) dep[i] = -1, cur[i] = head[i];
    hh = tt = 0; que[tt++] = s; dep[s] = 0;
    while (hh < tt) {
        int u = que[hh++];
        for (int i = head[u]; i; i = nxt[i]) {
            int v = to[i];
            if (cap[i] > 0 && dep[v] == -1) {
                dep[v] = dep[u] + 1;
                que[tt++] = v;
                if (v == t) return true;
            }
        }
    }
    return dep[t] != -1;
}

// DFS 增广
int dfs(int u, int t, int flow) {
    if (u == t || flow == 0) return flow;
    int rest = flow, add = 0;
    for (int& i = cur[u]; i && rest; i = nxt[i]) {   // 当前弧优化: i 用引用
        int v = to[i];
        if (cap[i] > 0 && dep[v] == dep[u] + 1) {
            add = dfs(v, t, min(rest, cap[i]));
            if (add == 0) dep[v] = -1;               // 该点无法再增广, 剪枝
            cap[i]     -= add;
            cap[i ^ 1] += add;                        // 反向边
            rest       -= add;
        }
    }
    return flow - rest;
}

// Dinic 主流程
int dinic(int s, int t, int n) {
    int maxflow = 0, flow;
    while (bfs(s, t, n))
        while ((flow = dfs(s, t, INF)) > 0)
            maxflow += flow;
    return maxflow;
}

int main() {
    quick;
    int n, m, s, t;
    cin >> n >> m >> s >> t;
    for (int i = 0; i < m; i++) {
        int u, v, c; cin >> u >> v >> c;
        add_edge(u, v, c);
    }
    cout << dinic(s, t, n) << endl;
    return 0;
}
```

**使用说明：**
- `tot` 初值为 1，保证每条正向边 `i` 与反向边 `i ^ 1` 成对存储
- `add_edge(u, v, c)` 加有向边；无向图请调用 `add_edge(u, v, c); add_edge(v, u, c);`
- 最小割：求完最大流后，从 $s$ 出发沿 `cap > 0` 的边 BFS，能到达的顶点集 $S$ 即为割的 $S$ 部，剩余点为 $T$ 部
- 二分图最大匹配：源点连左部点容量 1，右部点连汇点容量 1，原图边容量 1，最大流即为最大匹配数
- **建模思路与常见套路（核心！）**：

- **最大流（供需匹配类）**：
  - 源 $S$ → 供应方（容量为供应量），需求方 → 汇 $T$（容量为需求量），供应到需求有边直接连（容量∞或上限）。
  - 二分图最大匹配：$S$ 连左部容量1，右部连 $T$ 容量1，存在边的左右连容量1 → 最大流就是匹配数。
- **最小割（二选一、划分集合类）**：
  - 若选 A 集损失 $c_1$、选 B 集损失 $c_2$，两者同时选加罚 $w$ → 典型最小割（S连A、B连T，A-B间连w双向）。
  - **最大权闭合子图**：正权点连S（权为点值），负权点连T（权为点值绝对值），依赖关系连 ∞ 边 → 答案 = 正权总和 - 最小割。
  - **文理分科模型**：两人同科有奖励 → 新建虚点连S/T，虚点向两人连 ∞，对应同文/同理的额外收益。
- **最小费用最大流（有成本的匹配）**：
  - 图同上，但**每条边同时带费用**（如每输送一单位物品花多少代价）。
  - 求"最大流前提下总费用最小" → 跑 SSP（连续最短路+势函数消负环）或 Primal-Dual。
- **网络流数组大小建议**：链式前向星通常开到 **4×边数**（每条正边+反向边，且建模时常有多层虚点），不要吝啬。

---

<a id="sec-3-12"></a>

### 3.12 SCC强连通分量

- **适用场景**：有向图强连通分量缩点，将 DAG 化以进行拓扑 DP。
- **时间复杂度**：$O(n + m)$。
- **核心思想**：DFS 过程中维护时间戳 `dfn` 与能回溯到的最早祖先 `low`。若 `dfn[u] == low[u]`，则 $u$ 及栈中在其之后的顶点构成一个 SCC。缩点后整图变为 DAG。

- **模板代码：**

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 10010;

int n, m;
vector<int> G[MAXN];
int dfn[MAXN], low[MAXN], timer = 0;
int stk[MAXN], top = 0;
bool instk[MAXN];
int scc[MAXN], scccnt = 0;        // scc[i]: 顶点 i 所属 SCC 编号
int sz[MAXN];                      // sz[i]: 第 i 个 SCC 的大小

void tarjan(int u) {
    dfn[u] = low[u] = ++timer;
    stk[++top] = u; instk[u] = true;
    for (int v : G[u]) {
        if (!dfn[v]) {
            tarjan(v);
            low[u] = min(low[u], low[v]);
        } else if (instk[v]) {
            low[u] = min(low[u], dfn[v]);
        }
    }
    if (dfn[u] == low[u]) {
        ++scccnt;
        while (true) {
            int x = stk[top--];
            instk[x] = false;
            scc[x] = scccnt;
            sz[scccnt]++;
            if (x == u) break;
        }
    }
}

int main() {
    quick;
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int u, v; cin >> u >> v;
        G[u].push_back(v);
    }
    for (int i = 1; i <= n; i++)
        if (!dfn[i]) tarjan(i);
    // 缩点建新图 (DAG)
    vector<pair<int,int>> edges;
    for (int u = 1; u <= n; u++)
        for (int v : G[u])
            if (scc[u] != scc[v])
                edges.push_back({scc[u], scc[v]});
    sort(edges.begin(), edges.end());
    edges.erase(unique(edges.begin(), edges.end()), edges.end());
    cout << "SCC 数量: " << scccnt << endl;
    return 0;
}
```

**使用说明：**
- `dfn[u]` 为 DFS 时间戳，`low[u]` 为 $u$ 经至多一条非树边能回溯到的最早 `dfn`
- `dfn[u] == low[u]` 时弹出栈中点形成一个 SCC
- 缩点：把同 SCC 内顶点合并为一个新顶点，重边去重后得到 DAG
- 缩点后常按 SCC 编号逆序进行拓扑 DP（SCC 编号递减即拓扑序逆序）

**易错点：**
- **邻接表 G/链式前向星清空**必须清零 head / tot 或 G.clear()。
- 正图反图别搞反。Kosaraju 两次 DFS 别忘先正后反（或两次顺序按 tarjan 序）。
- Tarjan DFS 栈：栈中节点 low == dfn 时出栈到自身，不要漏掉 `instack` 标记的同步更新。

---

<a id="sec-3-13"></a>

### 3.13 割点割边与双连通分量

- **适用场景**：无向图割点、桥（割边）、边双连通分量（E-BCC）、点双连通分量（V-BCC）。
- **时间复杂度**：$O(n + m)$。
- **核心思想**：
- **割点**：若根节点有两棵及以上子树，或非根节点 $u$ 存在子节点 $v$ 使 `low[v] >= dfn[u]`，则 $u$ 为割点。
- **桥**：若 `low[v] > dfn[u]`，则边 $(u, v)$ 为桥。
- **边双**：删去所有桥后每个连通块即为一个边双连通分量。
- **点双**：割点连接若干点双，每个点双内任意两点间存在两条不相交路径。

**模板代码（割点与桥）：**

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 20010;

int n, m;
vector<pair<int,int>> G[MAXN];   // (邻接顶点, 边编号) 处理重边
int dfn[MAXN], low[MAXN], timer = 0;
bool iscut[MAXN], isbridge[200010];
int stk[MAXN], top = 0;

void tarjan(int u, int fa_edge) {
    dfn[u] = low[u] = ++timer;
    int son = 0;
    for (auto [v, eid] : G[u]) {
        if (eid == fa_edge) continue;          // 跳过来时的边 (处理重边)
        if (!dfn[v]) {
            son++;
            tarjan(v, eid);
            low[u] = min(low[u], low[v]);
            if (fa_edge == 0 && son > 1) iscut[u] = true;      // 根节点割点判定
            if (fa_edge != 0 && low[v] >= dfn[u]) iscut[u] = true; // 非根割点判定
            if (low[v] > dfn[u])  isbridge[eid] = true;        // 桥判定
        } else {
            low[u] = min(low[u], dfn[v]);
        }
    }
}

int main() {
    quick;
    cin >> n >> m;
    for (int i = 1; i <= m; i++) {
        int u, v; cin >> u >> v;
        G[u].push_back({v, i});
        G[v].push_back({u, i});
    }
    for (int i = 1; i <= n; i++)
        if (!dfn[i]) tarjan(i, 0);
    cout << "割点: ";
    for (int i = 1; i <= n; i++) if (iscut[i]) cout << i << ' ';
    cout << "\n桥: ";
    for (int i = 1; i <= m; i++) if (isbridge[i]) cout << i << ' ';
    return 0;
}
```

**边双连通分量（E-BCC）实现要点：**
- 求出所有桥后，标记桥不参与 DFS，每个连通块即一个边双
- 或在 Tarjan 中 `low[v] > dfn[u]` 时不再向下合并，用并查集合并其余点

**使用说明：**
- 割点判定：根节点要求子树数 $\geq 2$；非根节点要求子节点 $v$ 满足 `low[v] >= dfn[u]`
- 桥判定：`low[v] > dfn[u]`（严格大于），重边时需用边编号判父边而非顶点编号
- 边双缩点后整图变为森林（树），常用于把一般图问题转化为树问题
- 点双分量需另用栈维护边/点，较复杂，常用于圆方树建模

---

<a id="sec-3-14"></a>

### 3.14 欧拉路径与回路

- **适用场景**：求一笔画路径（每条边恰好经过一次）。
- **时间复杂度**：$O(n + m)$。

**判定条件**：
- **有向图欧拉回路**：所有顶点入度 = 出度，且基图连通
- **有向图欧拉路径**：恰有一个顶点出度 - 入度 = 1（起点），一个顶点入度 - 出度 = 1（终点），其余入度 = 出度
- **无向图欧拉回路**：所有顶点度数为偶数，且连通
- **无向图欧拉路径**：恰有两个奇度顶点（起点和终点）
- **核心思想**：从任一合法起点出发 DFS，走到无路可走时回退并将当前点加入答案栈，最终逆序输出即为欧拉路径/回路。需配合当前弧优化保证 $O(n+m)$。

**模板代码（有向图，字典序最小）：**

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 100010;

int n, m;
int head[MAXN];
struct Edge { int to, nxt; } e[200010];
int tot = 0;
int cur[MAXN];                       // 当前弧优化
int outd[MAXN], ind[MAXN];
stack<int> ans;

inline void add_edge(int u, int v) {
    e[++tot] = {v, head[u]}; head[u] = tot;
}

void hierholzer(int u) {
    for (int& i = cur[u]; i; ) {     // 引用, 自动推进
        int v = e[i].to;
        i = e[i].nxt;                // 先推进 (删边), 避免回头
        hierholzer(v);
    }
    ans.push(u);                     // 无路可走时入栈
}

int main() {
    quick;
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int u, v; cin >> u >> v;
        add_edge(u, v);
        outd[u]++; ind[v]++;
    }
    // 判定起点
    int start = 1, cnt = 0;
    for (int i = 1; i <= n; i++) {
        if (outd[i] - ind[i] == 1) { start = i; cnt++; }
        else if (ind[i] - outd[i] == 1) cnt++;
        else if (ind[i] != outd[i]) { cout << "No"<<endl; return 0; }
    }
    if (cnt != 0 && cnt != 2) { cout << "No"<<endl; return 0; }
    // 字典序最小: 邻接表按终点从小到大排序 (建边时即排)
    for (int u = 1; u <= n; u++) {
        vector<int> vs;
        for (int i = head[u]; i; i = e[i].nxt) vs.push_back(e[i].to);
        sort(vs.begin(), vs.end());
        head[u] = 0;
        for (int i = (int)vs.size() - 1; i >= 0; i--) add_edge(u, vs[i]);
    }
    for (int i = 1; i <= n; i++) cur[i] = head[i];
    hierholzer(start);
    while (!ans.empty()) { cout << ans.top() << ' '; ans.pop(); }
    return 0;
}
```

**使用说明：**
- 答案在栈中，需逆序输出（先入栈的是终点，最后入栈的是起点）
- 当前弧优化 `for (int& i = cur[u]; i; )` 必须先推进 `i` 再递归，避免回溯后重走已访问边
- 字典序最小：邻接表按终点升序排序后再跑
- 无向图版本：每条边建两条边并成对存储（同 Dinic），用 `used[]` 数组标记或修改 `cur` 指针；判父边需用边编号
- 自环允许；重边允许，按独立边处理

---
<a id="sec-3-15"></a>

### 3.15 Kruskal 重构树

- **适用场景**：求无向图中两点路径上**最大边权的最小值 / 最小边权的最大值**（瓶颈路问题）；子图中所有边权 ≤ X 的节点连通性查询；Kruskal 重构树后变成树结构，直接用 LCA 求瓶颈。
- **时间复杂度**：建重构树 $O(m \log m)$（排序+并查集）；单次查询 $O(\log n)$（LCA）。
- **建议上界**：$n, m \leq 2 \times 10^5$（1秒时限）。
- **核心思想**：
在 Kruskal 最小生成树过程中，不直接连边，而是**新建一个虚拟节点**，权值为当前边权，将两个连通块的根挂到虚拟节点下作为儿子。最终形成一棵 **2n-1 个节点的二叉（或多叉）树**，具有以下性质：
- 叶子是原节点，内部节点代表"连接两个连通块的那条边"。
- 最小生成树中，两点 **u, v 路径上最大边权的最小值 = LCA(u, v) 的权值**（Kruskal 按从小到大加边）。
- 同理，**最大边权的最大值**：按边权从大到小做 Kruskal，LCA 权值即为答案。

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 400010;           // 注意：重构树节点数 = 2n - 1，开两倍

struct Edge { int u, v, w; bool operator<(const Edge& r) const { return w < r.w; } };

int n, m, Q;
Edge e[MAXN];

// ===== 并查集（用于Kruskal） =====
int fa[MAXN];
int find(int x) { return x == fa[x] ? x : fa[x] = find(fa[x]); }

// ===== 重构树 =====
int cnt;                           // 重构树当前节点编号（从n+1开始递增）
int val[MAXN];                     // 内部节点的权值 = 边权
vector<int> T[MAXN];               // 重构树邻接表

// ===== LCA（倍增） =====
const int LOG = 20;
int dep[MAXN], up[MAXN][LOG + 1];

void dfs_lca(int u, int father) {
    up[u][0] = father;
    for (int i = 1; i <= LOG; i++) up[u][i] = up[up[u][i - 1]][i - 1];
    for (int v : T[u]) {
        if (v == father) continue;
        dep[v] = dep[u] + 1;
        dfs_lca(v, u);
    }
}
int lca(int u, int v) {
    if (dep[u] < dep[v]) swap(u, v);
    int d = dep[u] - dep[v];
    for (int i = 0; i <= LOG; i++) if (d >> i & 1) u = up[u][i];
    if (u == v) return u;
    for (int i = LOG; i >= 0; i--)
        if (up[u][i] != up[v][i]) { u = up[u][i]; v = up[v][i]; }
    return up[u][0];
}

// ===== 建 Kruskal 重构树 =====
void build_kruskal_tree() {
    sort(e + 1, e + 1 + m);
    cnt = n;                                // 虚拟节点编号从 n+1 开始
    for (int i = 1; i <= 2 * n; i++) fa[i] = i;  // 初始化并查集（注意范围）
    for (int i = 1; i <= m; i++) {
        int u = find(e[i].u), v = find(e[i].v);
        if (u == v) continue;               // 已连通则跳过
        int nd = ++cnt;                     // 新建虚拟节点
        val[nd] = e[i].w;
        T[nd].push_back(u); T[nd].push_back(v);
        fa[u] = nd; fa[v] = nd;
    }
    // 最后 cnt 是重构树的根；可能有多个连通块 → 用超级根0统一连，或逐个DFS
    for (int i = 1; i <= cnt; i++)
        if (find(i) == i) {                 // 每个连通块的根
            dep[i] = 1;
            dfs_lca(i, 0);
        }
}

int main() {
    quick;
    cin >> n >> m;
    for (int i = 1; i <= m; i++) cin >> e[i].u >> e[i].v >> e[i].w;
    build_kruskal_tree();
    cin >> Q;
    while (Q--) {
        int u, v; cin >> u >> v;
        if (find(u) != find(v)) { cout << -1 << endl; continue; }
        cout << val[lca(u, v)] << endl;     // 答案：LCA的权值
    }
    return 0;
}
```
- **常见套路**：
- **瓶颈路**：直接如上，查询 LCA 权值。
- **"询问 V ≤ X 时的连通性"**：对每个询问 X，只保留权值 ≤ X 的边 — 只需从虚根向下跳到最后一个 `val[u] ≤ X` 的祖先，其儿子集合就是连通块。
- **最大生成树版本**（最小边权的最大值）：把边按权值**从大到小**排序，再做同样的重构树，LCA 权值即为"路径上最小边权的最大值"。
- **子树信息维护**：因为重构树的每个内部节点的子树都对应 Kruskal 合并到该边时的连通块，可以通过 DFS 序转化为区间问题，用主席树 / 树状数组维护。

---

<a id="sec-3-16"></a>

### 3.16 启发式合并 / DSU on Tree（树上子树查询）

- **适用场景**：给一棵有根树，每个点有颜色 / 权值，**对每个节点 u 求其子树内的统计信息**（子树内颜色种数、出现次数最多的颜色、权值和排名、满足条件的对数等）。
- **时间复杂度**：$O(n \log n)$（每个节点被拷贝的次数是其到根路径上"轻边"的数量，最多 $\log n$ 条）。
- **建议上界**：$n \leq 5 \times 10^5$（1秒时限，常数较小）。

- **核心思想**（DSU on Tree，又称"树上并查集启发式"）：
1. 先做一次 DFS 计算每个节点的大小，标记重儿子（即 `son[u]` = 子树最大的那个儿子）。
2. 再做第二次 DFS（`dfs_solve(u)`）：
   - **先递归处理所有轻儿子**，但处理完**立刻清空**全局累加器（保留干净状态）。
   - **最后处理重儿子**，处理完**不清空**全局累加器。
   - 再把**所有轻儿子的子树暴力枚举**，一个个点加入全局累加器，得到 u 的完整答案。
   - 如果 u 本身是**某个父亲的轻儿子**，递归返回前清空全局累加器；若是重儿子则保留。

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 200010;

int n;
int col[MAXN];                      // 每个节点的颜色
vector<int> G[MAXN];

// ---------- 第一次DFS：算大小 + 标记重儿子 ----------
int sz[MAXN], son[MAXN];
void dfs_size(int u, int fa) {
    sz[u] = 1; son[u] = 0;
    int mx = 0;
    for (int v : G[u]) {
        if (v == fa) continue;
        dfs_size(v, u);
        sz[u] += sz[v];
        if (sz[v] > mx) { mx = sz[v]; son[u] = v; }
    }
}

// ---------- 全局累加器（按问题修改，这里是"颜色计数 + 总颜色种数"） ----------
int cnt[MAXN];                     // 每种颜色出现次数
int kinds = 0;                     // 全局：当前不同颜色种数
int ans[MAXN];                     // 答案数组：ans[u] = u子树内颜色种数

// ---------- 加入 / 删除 一个节点的信息 ----------
void add(int u, int fa, int delta) {     // delta = +1 加入, -1 删除
    if (cnt[col[u]] == 0 && delta == +1) kinds++;
    cnt[col[u]] += delta;
    if (cnt[col[u]] == 0 && delta == -1) kinds--;
    for (int v : G[u]) {
        if (v == fa) continue;
        add(v, u, delta);
    }
}

// ---------- 第二次DFS：求解 ----------
void dfs_solve(int u, int fa, bool keep) {
    // 1. 先处理所有轻儿子，处理完立刻清空（keep=false）
    for (int v : G[u]) {
        if (v == fa || v == son[u]) continue;
        dfs_solve(v, u, false);
    }
    // 2. 最后处理重儿子，保留累加器（keep=true）
    if (son[u]) dfs_solve(son[u], u, true);
    // 3. 把 u 自己加入
    if (cnt[col[u]] == 0) kinds++;
    cnt[col[u]]++;
    // 4. 再暴力把所有轻儿子的子树加入（它们的答案之前算过，但全局累加器现在没有它们）
    for (int v : G[u]) {
        if (v == fa || v == son[u]) continue;
        add(v, u, +1);
    }
    // 5. 现在全局累加器是 u 子树的完整信息 → 记录答案
    ans[u] = kinds;
    // 6. 如果当前 u 是某个父亲的轻儿子（keep=false），离开前清空
    if (!keep) {
        if (--cnt[col[u]] == 0) kinds--;
        for (int v : G[u]) {
            if (v == fa) continue;
            add(v, u, -1);
        }
    }
}

int main() {
    quick;
    cin >> n;
    for (int i = 1; i <= n; i++) cin >> col[i];
    for (int i = 1; i < n; i++) {
        int u, v; cin >> u >> v;
        G[u].push_back(v);
        G[v].push_back(u);
    }
    dfs_size(1, 0);
    dfs_solve(1, 0, false);
    int Q; cin >> Q;
    while (Q--) {
        int u; cin >> u;
        cout << ans[u] << endl;
    }
    return 0;
}
```

**常见扩展（修改全局累加器 + add 函数即可适配）**：
- **子树中出现次数最多的颜色**：额外维护 `mx`（最大次数），每次 `cnt[c]++` 时更新 `mx`。
- **子树中排名第 k 小**：用树状数组 / 值域分块替代 `cnt` 数组。
- **子树中满足 count[c] ≥ k 的颜色数**：`cnt` 数组 + 额外维护 `geq[k]`（出现次数≥k 的颜色数量）。
- **易错点**：
- 第二次 DFS 的第四步 `add(v, u, +1)` 一定要对所有**轻儿子**递归加入（它们的递归返回时被清空了），不要漏掉。
- 若答案需要**取模 / 去重**，全局累加器的加减要对称（`add(+1)` 和 `add(-1)` 必须严格互逆），否则不清空的重儿子那部分会出错。
- 遇到 $n$ 很大（3e5以上）时，`add` 函数递归层数可能较深——若怕爆栈可用非递归 DFS 序展开（栈模拟）。

---



