## [返回目录](http://scpc.fun/discussion-detail/1405)

<style>
a[id], h2, h3, h4 { scroll-margin-top: 140px; }
</style>

---

## 本章目录

- [1.1 快速幂](#sec-1-1) **必学**
- [1.2 二分答案与二分查找](#sec-1-2) **必学**
  - [1.2.1 三分法](#sec-1-2-1) **选学**
- [1.3 差分与前缀和](#sec-1-3) **必学**
  - [1.3.1 树上差分](#sec-1-3-1) **进阶**
- [1.4 离散化](#sec-1-4) **必学**
- [1.5 排序](#sec-1-5) **必学**
- [1.6 贪心](#sec-1-6) **必学**
  - [1.6.1 反悔贪心](#sec-1-6-1) **进阶**
- [1.7 DFS与BFS](#sec-1-7) **必学**
- [1.8 滑动窗口与双指针](#sec-1-8) **必学**
- [1.9 ST表](#sec-1-9) **进阶**
- [1.10 位运算](#sec-1-10) **必学**
- [1.11 CDQ分治](#sec-1-11) **进阶**
- [1.12 整体二分](#sec-1-12) **进阶**
- [1.13 Meet-in-the-Middle](#sec-1-13) **进阶**
- [1.14 模拟退火与爬山](#sec-1-14) **选学**
- [1.15 单调栈](#sec-1-15) **必学**
- [1.16 01BFS（双端队列最短路）](#sec-1-16) **进阶**

---

<a id="ch1"></a>

## 一、基础算法

本板块涵盖竞赛中最基础的算法技巧，包括快速幂、二分、差分前缀和、离散化、排序、贪心、搜索、滑动窗口、ST表和位运算。

<a id="sec-1-1"></a>

### 1.1 快速幂
- **适用场景**：计算 $a^b \bmod p$，在数论题中广泛使用。
- **时间复杂度**：$O(\log b)$
- **核心思想**：将指数 $b$ 按二进制拆分，利用平方倍增的思想，每次将底数平方，若当前二进制位为1则乘入答案。

```cpp
// 快速幂模板
long long quickpow(long long a, long long b, long long mod) {
    long long res = 1;
    a %= mod;
    while (b > 0) {
        if (b & 1) res = res * a % mod;
        a = a * a % mod;
        b >>= 1;
    }
    return res;
}

// 不取模版本（注意溢出风险）
long long quickpow_ll(long long a, long long b) {
    long long res = 1;
    while (b > 0) {
        if (b & 1) res = res * a;
        a = a * a;
        b >>= 1;
    }
    return res;
}
```

**相关题目**：
- [打扫教室](http://scpc.fun/problem/9165)（第4题，快速幂求 $\sum_{i=1}^n i^{m-1}$）
- [分糖果](http://scpc.fun/problem/9036)（第75题，快速幂 + 大数取模）
- [幻梦选gal](http://scpc.fun/problem/SCPC-1208D)（第142题，快速幂求 $\sum \lfloor n/2^k \rfloor$）

**易错点：**
- $a$ 为负数或超过 $mod$ 时，**先取模再加模**：`a = (a % mod + mod) % mod;`
- 取模过程中乘法溢出 int：统一改用 long long；或 `__int128` 辅助。
- 指数 $b=0$、$a^0 = 1$；但 $a=0, b=0$ 无意义，按题意特判。

---

<a id="sec-1-2"></a>

### 1.2 二分答案与二分查找
- **适用场景**：求最大/最小值问题，且答案具有单调性（即存在一个临界点，使得一侧满足条件、另一侧不满足）。
- **时间复杂度**：$O(n \log V)$，其中 $V$ 为值域大小。
- **核心思想**：二分枚举答案，用 `check(mid)` 函数判定当前答案是否可行，根据判定结果调整上下界。

```cpp
// 二分答案模板（求满足条件的最大值）
int lo = lo_bound, hi = hi_bound, ans = -1;
while (lo <= hi) {
    int mid = (lo + hi) / 2;
    if (check(mid)) {       // mid 可行，尝试更大
        ans = mid;
        lo = mid + 1;
    } else {                // mid 不可行，缩小
        hi = mid - 1;
    }
}

// 二分答案模板（求满足条件的最小值）
int lo = lo_bound, hi = hi_bound, ans = -1;
while (lo <= hi) {
    int mid = (lo + hi) / 2;
    if (check(mid)) {       // mid 可行，尝试更小
        ans = mid;
        hi = mid - 1;
    } else {                // mid 不可行，放大
        lo = mid + 1;
    }
}

// STL二分查找
// lower_bound: 返回 >= val 的第一个位置
// upper_bound: 返回 > val 的第一个位置
int pos = lower_bound(a.begin(), a.end(), val) - a.begin();
```

**分数规划（二分答案+判定）**：二分答案比率 $x$，将问题转化为判定 $\sum(a_i - x \cdot b_i) \geq 0$ 是否成立。

```cpp
// 分数规划模板：求 max(sum(a)/sum(b))
bool check(double x) {
    vector<double> c(n);
    for (int i = 0; i < n; i++) c[i] = a[i] - x * b[i];
    sort(c.begin(), c.end(), greater<double>());
    double sum = 0;
    for (int i = 0; i < k; i++) sum += c[i];
    return sum >= 0;
}
// 二分：lo=0, hi=1e9, 迭代60次
```

**相关题目**：
- [数列分段 II](http://scpc.fun/problem/10407)（第97题，二分最大段和）
- [跳石头比赛](http://scpc.fun/problem/10412)（第98题，二分最小跳跃距离）
- [分巧克力](http://scpc.fun/problem/10414)（第99题，二分边长 + 贪心判定）
- [分数规划 I](http://scpc.fun/problem/10469)（第51题，贪心+二分答案）
- [分数规划 II](http://scpc.fun/problem/10468)（第50题，二分答案+背包判定）
- [分数规划 III](http://scpc.fun/problem/10467)（第49题，前缀和+二分答案）
- [千星奇域](http://scpc.fun/problem/10435)（第102题，二分+区间合并）
- [喵卡喵斯的游戏](http://scpc.fun/problem/11779)（第119题，二分+二维前缀和）
- [小凡偷吃巧克力](http://scpc.fun/problem/SCPC-0999M)（第123题，二分）
- [不要浪费](http://scpc.fun/problem/SCPC-1026F)（第125题，二分）
- [不想再加训了](http://scpc.fun/problem/SCPC-1186I)（第138题，二分）
- [Friends and Subsequences](http://scpc.fun/problem/CF-689-D)（第121题，二分+ST表）
- [multiset](http://scpc.fun/problem/CF-1354-D)（第122题，二分+树状数组）
- [Telephone Lines S](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P1948)（第67题，二分+最短路）
- [猫咪国的弹珠机](http://scpc.fun/problem/10431)（第101题，并查集+二分+DFS+最短路）

**易错点：**
- 整数二分清空**边界写法**固定一种，别混用：
  - **求最小的合法值（下界）**：`while (l < r) { m = l + r >> 1; if (ok(m)) r = m; else l = m + 1; } → return l;`
  - **求最大的合法值（上界）**：`while (l < r) { m = (l + r + 1) >> 1; if (ok(m)) l = m; else r = m - 1; } → return l;`
  - 混用写法 +1/-1 放反 → 轻则死循环重则 WA。
- 浮点二分直接用 $r - l > EPS$ 判断，注意 EPS 选 1e-8 或 1e-10。
- check 函数写对边界，尤其是"刚好等于算不算合法"。

---

<a id="sec-1-2-1"></a>

### 1.2.1 三分法
- **适用场景**：求**单峰/单谷函数**的极值点（最大值或最小值）。常用于：分数规划、几何最值、凸函数优化。
- **核心思想**：在区间 [l, r] 内取两个三分点 m1, m2，比较 f(m1) 与 f(m2) 的大小：
- 若 f(m1) < f(m2)（求最大值时），极值点在 [m1, r] 内 → 令 l = m1
- 否则极值点在 [l, m2] 内 → 令 r = m2
- 重复直到 r - l < EPS（实数）或迭代次数足够（整数）。
- **时间复杂度**：$O(\log_{1.5}(\frac{r-l}{\text{EPS}}))$；整数版每轮区间缩小约 2/3。

```cpp
// ============= 三分法 - 实数版（求极大值）=============
// f(x) 必须是严格单峰函数；若求极小值把比较方向反过来即可
double ternary_search_max(double l, double r, function<double(double)> f) {
    const double EPS = 1e-10;
    while (r - l > EPS) {
        double m1 = l + (r - l) / 3;
        double m2 = r - (r - l) / 3;
        if (f(m1) < f(m2)) l = m1;     // 极大值在右侧
        else                r = m2;     // 极大值在左侧
    }
    return (l + r) / 2;                 // 返回极值点 x*；f((l+r)/2) 即为极值
}

// ============= 三分法 - 整数版（求极小值）=============
// 适用于离散单峰函数，要求 l <= 极值点 <= r
long long ternary_search_min_int(long long l, long long r, function<long long(long long)> f) {
    while (r - l > 3) {
        long long m1 = l + (r - l) / 3;
        long long m2 = r - (r - l) / 3;
        if (f(m1) < f(m2)) r = m2;      // 极小值在左侧（注意方向与求最大值相反）
        else                l = m1;
    }
    long long ans = LLONG_MAX;
    for (long long x = l; x <= r; x++) ans = min(ans, f(x));   // 剩下小区间暴力扫
    return ans;
}
```

**使用注意**：
- 函数必须是**严格单峰**（不能有平台/多峰），否则三分会卡在平台。
- 整数版要"留 3 个点最后暴力扫"，避免死循环。
- 三分法常与**二分答案**配合：外层二分一个值，内层用三分检查。

---

<a id="sec-1-3"></a>

### 1.3 差分与前缀和
- **适用场景**：区间修改、区间查询。差分用于高效区间加法，前缀和用于快速区间求和。
- **时间复杂度**：修改 $O(1)$，查询 $O(1)$（前缀和）或 $O(\log n)$（树状数组维护差分）。
- **核心思想**：
- 一维前缀和：$S[i] = S[i-1] + a[i]$，区间和 $= S[r] - S[l-1]$
- 一维差分：对 $[l,r]$ 加 $v$，只需 $d[l] \mathrel{+}= v, d[r+1] \mathrel{-}= v$，再前缀和还原
- 二维前缀和：$S[i][j] = S[i-1][j] + S[i][j-1] - S[i-1][j-1] + a[i][j]$
- 二阶差分：对 $[l,r]$ 加等差数列，用二阶差分 $d2[l] \mathrel{+}= d, d2[mid] \mathrel{-}= 2d, d2[r] \mathrel{+}= d$

```cpp
// 一维前缀和
for (int i = 1; i <= n; i++) pre[i] = pre[i-1] + a[i];
// 区间和 [l, r]
ll sum = pre[r] - pre[l-1];

// 一维差分（区间加法）
void add_range(int l, int r, int v) {
    diff[l] += v;
    diff[r + 1] -= v;
}
// 还原
for (int i = 1; i <= n; i++) {
    diff[i] += diff[i - 1];
    a[i] += diff[i];
}

// 二维前缀和
for (int i = 1; i <= n; i++)
    for (int j = 1; j <= m; j++)
        pre[i][j] = pre[i-1][j] + pre[i][j-1] - pre[i-1][j-1] + a[i][j];
// 子矩阵 (x1,y1)-(x2,y2) 的和
ll sum = pre[x2][y2] - pre[x1-1][y2] - pre[x2][y1-1] + pre[x1-1][y1-1];

// 二阶差分（对 [l,r] 加等差数列 a+d, a+2d, ..., a+(r-l+1)*d）
// d2[l] += a, d2[l+1] += d - a, d2[r+1] -= (r-l+1)*d + a, d2[r+2] += (r-l)*d
// 简化版：若对 [l,mid] 加 +1 递增，对 [mid,r] 加 -1 递减（三角形修改）
// diff2[l] += sign; diff2[mid] -= 2*sign; diff2[r] += sign;
// 两次前缀和还原

// 差分+离散化（处理大范围区间修改）
// 先收集所有端点离散化，再在离散化后的数组上做差分
```

**相关题目**：
- [远昼](http://scpc.fun/problem/9223)（第8题，差分求操作次数）
- [等差数列](http://scpc.fun/problem/10426)（第100题，二阶差分维护等差数列）
- [安排房间](http://scpc.fun/problem/10476)（第113题，差分+离散化）
- [骗人的枫落落](http://scpc.fun/problem/SCPC-1066B)（第126题，差分+前缀和）
- [枝江一梦](http://scpc.fun/problem/SCPC-1165K)（第133题，二阶差分）
- [Marshmallow Tree](http://scpc.fun/problem/SCPC-1208G)（第143题，差分维护时间区间）
- [学长分糖果](http://scpc.fun/problem/%E5%8D%93%E8%B6%8A-044)（第145题，差分区间加法）
- [量子态反转](http://scpc.fun/problem/%E5%8D%93%E8%B6%8A-049)（第146题，异或差分）
- [外星人](http://scpc.fun/problem/%E5%8D%93%E8%B6%8A-064)（第149题，差分模拟）
- [棱形前缀和](http://scpc.fun/problem/10466)（第48题，坐标变换+二维差分）
- [领地选择](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P2004)（第70题，二维前缀和）
- [区间和恰好为 k](http://scpc.fun/problem/10478)（第114题，前缀和+哈希）
- [山卡西](http://scpc.fun/problem/10477)（第52题，前缀和+哈希）
- [更难的异或](http://scpc.fun/problem/SCPC-1026C)（第124题，前缀和+预处理）
- [Small Orange的后花园](http://scpc.fun/problem/SCPC-1183H)（第134题，前缀和+预处理）
- [and、or、xor？](http://scpc.fun/problem/SCPC-1186M)（第139题，前缀和+位运算）
- [只删一个](http://scpc.fun/problem/10481)（第116题，前后缀GCD）
- [接雨水](http://scpc.fun/problem/10482)（第117题，前后缀+双指针）
- [心如止水](http://scpc.fun/problem/SCPC-1116E)（第130题，贪心+前缀和）
- [花童](http://scpc.fun/problem/10480)（第53题，前后缀预处理匹配位置）

---

<a id="sec-1-3-1"></a>

### 1.3.1 树上差分
- **适用场景**：树上的路径修改（路径上所有节点/边权加值）、子树修改，最后通过一次DFS还原。
- **核心公式**：
- **点差分 - 路径 u–v 上所有点加 v**：`diff[u] += v, diff[v] += v, diff[lca] -= v, diff[fa[lca]] -= v`
- **点差分 - 子树 u 加 v**：`diff[u] += v, diff[fa[u]] -= v`（fa[u]=0 时忽略第二项）
- **边差分 - 路径 u–v 上所有边加 v**（边权下沉到深度较大的子节点）：`diff[u] += v, diff[v] += v, diff[lca] -= 2*v`

```cpp
const int MAXN = 100005;
vector<int> g[MAXN];
int fa[MAXN][20], dep[MAXN];
long long diff_node[MAXN], diff_edge[MAXN], val_node[MAXN], val_edge[MAXN];

// ====== 使用前需先做LCA预处理（倍增法，同3.4节） ======
// void bfs_root(int root);    // 预处理fa[][0]、dep[]、fa[][j]倍增表
// int lca(int u, int v);      // 求u和v的最近公共祖先

// 点差分：路径 u-v 上所有点加 val
void add_path_node(int u, int v, long long val) {
    int l = lca(u, v);
    diff_node[u] += val;
    diff_node[v] += val;
    diff_node[l] -= val;
    if (fa[l][0]) diff_node[fa[l][0]] -= val;
}

// 点差分：子树 u 加 val
void add_subtree_node(int u, long long val) {
    diff_node[u] += val;
    if (fa[u][0]) diff_node[fa[u][0]] -= val;
}

// 边差分：路径 u-v 上所有边加 val（边权下沉到深度较大的子节点）
void add_path_edge(int u, int v, long long val) {
    int l = lca(u, v);
    diff_edge[u] += val;
    diff_edge[v] += val;
    diff_edge[l] -= 2 * val;
}

// 还原：后序DFS，把所有子节点贡献累加到父节点
void dfs_restore(int u, int f) {
    val_node[u] = diff_node[u];
    val_edge[u] = diff_edge[u];   // val_edge[u] 表示 fa[u] -> u 这条边的最终值
    for (int v : g[u]) {
        if (v == f) continue;
        dfs_restore(v, u);
        val_node[u] += val_node[v];
        val_edge[u] += val_edge[v];
    }
}
```

**使用说明**：做完所有修改后，调用一次 `dfs_restore(root, 0)` 即可在 `val_node[]` / `val_edge[]` 中得到最终值。

---

<a id="sec-1-4"></a>

### 1.4 离散化
- **适用场景**：值域很大但实际使用的数据很少，将大值域映射到小范围。
- **时间复杂度**：$O(n \log n)$（排序去重）
- **核心思想**：排序去重后，用 `lower_bound` 将原值映射到排名。

```cpp
// 离散化模板
vector<int> vals = a;
sort(vals.begin(), vals.end());
vals.erase(unique(vals.begin(), vals.end()), vals.end());

// 获取离散化后的排名（1-indexed）
auto get_rank = [&](int x) {
    return lower_bound(vals.begin(), vals.end(), x) - vals.begin() + 1;
};

// 使用
int rk = get_rank(a[i]);
```

**相关题目**：
- [三元上升子序列](http://scpc.fun/problem/10107)（第28题，树状数组+离散化）
- [小石的妹子](http://scpc.fun/problem/10201)（第36题，树状数组+离散化）
- [小魂和他的数列](http://scpc.fun/problem/10203)（第38题，树状数组+离散化）
- [逆序对](http://scpc.fun/problem/10106)（第92题，树状数组+离散化）
- [维护元素频率最大值](http://scpc.fun/problem/10200)（第35题，滑动窗口+离散化+频率维护）
- [安排房间](http://scpc.fun/problem/10476)（第113题，差分+离散化）

---

<a id="sec-1-5"></a>

### 1.5 排序
- **适用场景**：需要按特定顺序处理数据。
- **核心思想**：根据问题特点选择合适的排序规则（如按右端点排序、按斜率排序等）。

```cpp
// 自定义排序
sort(a.begin(), a.end(), [](const auto& x, const auto& y) {
    return x.r < y.r;  // 按右端点升序
});
// 二维排序（先按x排序，再按y排序）
sort(a.begin(), a.end(), [](const auto& x, const auto& y) {
    return x.x < y.x || (x.x == y.x && x.y < y.y);
});

// next_permutation（生成全排列）
sort(a.begin(), a.end());  // 先排序确保从最小排列开始
do {
    // 处理当前排列
} while (next_permutation(a.begin(), a.end()));
```

**相关题目**：
- [还没有背景的题](http://scpc.fun/problem/9181)（第76题，排序+哈希+meet in middle）
- [最长不相交区间](http://scpc.fun/problem/10475)（第112题，贪心+排序按右端点）
- [字符串翻转](http://scpc.fun/problem/%E5%8D%93%E8%B6%8A-074)（第152题，排序+偏移技巧）
- [下一个全排列](http://scpc.fun/problem/10088)（第84题，next_permutation）

---

<a id="sec-1-6"></a>

### 1.6 贪心
- **适用场景**：问题具有贪心选择性质（局部最优可以导出全局最优）。
- **核心思想**：每一步选择当前最优的方案，不回溯。常见的贪心策略包括：按右端点排序选区间、差分贪心、优先队列贪心等。

```cpp
// 区间调度问题（选最多不相交区间）
sort(a.begin(), a.end(), [](const auto& x, const auto& y) {
    return x.r < y.r;  // 按右端点排序
});
int ans = 0;
long long last = -1e18;
for (auto& p : a) {
    if (p.l >= last) {
        ans++;
        last = p.r;
    }
}

// 铺设道路贪心（差分贪心）
long long ans = 0, last = 0;
for (int i = 1; i <= n; i++) {
    if (a[i] > last) ans += a[i] - last;
    last = a[i];
}
```

**相关题目**：
- [回文串](http://scpc.fun/problem/9261)（第11题，贪心统计字符频率）
- [河童的黄瓜种植](http://scpc.fun/problem/10028)（第24题，贪心差分）
- [小凡挑巧克力](http://scpc.fun/problem/SCPC-1023J)（第56题，贪心最长连续段）
- [博士，可露希尔有新的推荐](http://scpc.fun/problem/SCPC-1040E)（第58题，贪心购买）
- [Sheyela_Lucid的后花园](http://scpc.fun/problem/SCPC-1217-L)（第65题，贪心+二分+预处理）
- [心如止水2.0](http://scpc.fun/problem/9309)（第83题，贪心枚举约数）
- [铺设道路](http://scpc.fun/problem/10474)（第111题，贪心差分）
- [最长不相交区间](http://scpc.fun/problem/10475)（第112题，贪心+排序）
- [最小化字符串](http://scpc.fun/problem/CF-1675E)（第120题，并查集+贪心）
- [有顶天变](http://scpc.fun/problem/SCPC-1183M)（第135题，贪心差分）
- [猫猫编号](http://scpc.fun/problem/%E5%8D%93%E8%B6%8A-037)（第144题，贪心字符串分割）
- [五星好评](http://scpc.fun/problem/%E5%8D%93%E8%B6%8A-063)（第148题，贪心枚举方案）
- [方向制造快乐](http://scpc.fun/problem/%E5%8D%93%E8%B6%8A-071)（第150题，贪心计算）

---

<a id="sec-1-6-1"></a>

### 1.6.1 反悔贪心
- **适用场景**：贪心过程中可以"撤销"之前的选择以达到更优全局解。常见于：带死线的任务调度收益最大化、买卖股票带冷却、区间问题中临时选取后用更优替换。
- **核心思想**：先按某种规则（如死线/时间）顺序处理，遇到"必须选"的元素先直接选入结果集（同时用优先队列记录，方便后续替换）；若选入后不满足约束（如超出死线），则从已选元素中"反悔"收益最小的那个（弹出堆顶），总收益相应扣减。最终收益即为最优。

```cpp
// 经典模型：带死线的任务收益最大化
// 每个任务 (deadline d_i, profit p_i)：d_i 表示必须在 d_i 时刻（第d_i秒/第d_i天）之前结束
// 每个任务耗时 1 单位时间，求最多能完成的任务总收益最大值
// 时间复杂度：O(n log n)（排序 + 优先队列）

struct Task { int d, p; };
bool cmpDead(const Task& a, const Task& b) { return a.d < b.d; }  // 按死线升序

long long maxProfit(vector<Task>& tasks) {
    sort(tasks.begin(), tasks.end(), cmpDead);
    priority_queue<int, vector<int>, greater<int>> pq;   // 小根堆：存已选任务的收益，top=最小
    long long profit = 0;
    for (auto& t : tasks) {
        pq.push(t.p);
        profit += t.p;
        // 如果已选任务数 > 当前死线（做不完了），反悔：放弃收益最小的那个
        if ((int)pq.size() > t.d) {
            profit -= pq.top();
            pq.pop();
        }
    }
    return profit;
}
```

**使用说明**：
- 死线 `d_i > n` 时可先对 `d_i` 与 `n` 取 min（最多只能做 n 个任务），等价性不变。
- 若求**选择了哪些具体任务**，把堆中元素改为 `(profit, 原下标)`，最后遍历堆内元素即可。
- 推广到更一般问题：任何"选入→冲突→用优先级挑选要撤销的元素"的场景，都可用优先队列+反悔贪心解决。

---

<a id="sec-1-7"></a>

### 1.7 DFS与BFS
- **适用场景**：图/网格上的遍历、搜索、连通性判断。
- **核心思想**：
- DFS（深度优先搜索）：递归遍历，适合连通性判断、排列组合、回溯
- BFS（广度优先搜索）：队列遍历，适合最短路、层序遍历

```cpp
// DFS模板（网格连通性）
int dx[4] = {-1, 0, 1, 0}, dy[4] = {0, 1, 0, -1};
void dfs(int x, int y) {
    vis[x][y] = true;
    for (int i = 0; i < 4; i++) {
        int nx = x + dx[i], ny = y + dy[i];
        if (nx < 0 || nx >= n || ny < 0 || ny >= m) continue;
        if (vis[nx][ny] || grid[nx][ny] == '#') continue;
        dfs(nx, ny);
    }
}

// BFS模板（最短路）
int dx[4] = {-1, 0, 1, 0}, dy[4] = {0, 1, 0, -1};
int dist[N][N];
memset(dist, -1, sizeof(dist));
queue<pair<int,int>> q;
q.push({sx, sy});
dist[sx][sy] = 0;
while (!q.empty()) {
    auto [x, y] = q.front(); q.pop();
    for (int i = 0; i < 4; i++) {
        int nx = x + dx[i], ny = y + dy[i];
        if (nx < 0 || nx >= n || ny < 0 || ny >= m) continue;
        if (dist[nx][ny] != -1) continue;
        dist[nx][ny] = dist[x][y] + 1;
        q.push({nx, ny});
    }
}

// DFS排列组合（选k个）
void dfs(int start, int cnt, int sum) {
    if (cnt == k) {
        // 处理结果
        return;
    }
    for (int i = start; i <= n; i++) {
        dfs(i + 1, cnt + 1, sum + a[i]);
    }
}

// BFS 8方向（切比雪夫距离）
int dx8[8] = {-1,-1,-1,0,0,1,1,1};
int dy8[8] = {-1,0,1,-1,1,-1,0,1};

// N皇后（DFS+对角线标记）
void dfs(int i) {
    if (i > n) { ans++; return; }
    for (int j = 1; j <= n; j++) {
        if (col[j] || diag1[i+j] || diag2[i-j+n]) continue;
        col[j] = diag1[i+j] = diag2[i-j+n] = 1;
        dfs(i + 1);
        col[j] = diag1[i+j] = diag2[i-j+n] = 0;
    }
}
```

**相关题目**：
- [残霞璀月的后花园](http://scpc.fun/problem/9355)（第19题，DFS决策）
- [油田问题](http://scpc.fun/problem/10096)（第26题，DFS 8方向连通块）
- [素数环](http://scpc.fun/problem/10097)（第27题，DFS排列+剪枝）
- [树的深度](http://scpc.fun/problem/9229)（第80题，DFS遍历树）
- [骰子||](http://scpc.fun/problem/9230)（第81题，DFS枚举）
- [N皇后](http://scpc.fun/problem/10089)（第85题，DFS+对角线标记）
- [选数](http://scpc.fun/problem/10093)（第87题，DFS组合+素数判定）
- [迷宫寻路](http://scpc.fun/problem/10094)（第88题，DFS路径）
- [数细胞](http://scpc.fun/problem/%E8%AE%A1%E5%8D%8F-014)（第153题，DFS连通块）
- [最大棱形](http://scpc.fun/problem/10462)（第44题，BFS+曼哈顿距离）
- [最大正方形](http://scpc.fun/problem/10463)（第45题，BFS+切比雪夫距离）
- [花空烟水流](http://scpc.fun/problem/SCPC-1049C)（第59题，BFS+哈希）
- [矩阵染色](http://scpc.fun/problem/SCPC-1217-H)（第64题，BFS+0-1 BFS）
- [勇闯地下城](http://scpc.fun/problem/SCPC-1186H)（第137题，DFS+状态）
- [元旦将近](http://scpc.fun/problem/SCPC-1188K)（第140题，DFS+图论）
- [连连看](http://scpc.fun/problem/10095)（第25题，BFS+转弯次数限制状态搜索）
- [拼多多](http://scpc.fun/problem/SCPC-1199F)（第63题，DFS组合枚举）

---

<a id="sec-1-8"></a>

### 1.8 滑动窗口与双指针
- **适用场景**：维护一个滑动区间，满足某种性质。
- **时间复杂度**：$O(n)$
- **核心思想**：维护左右指针 $l, r$，右指针扩展窗口，左指针收缩窗口。

```cpp
// 滑动窗口模板
int l = 0;
for (int r = 0; r < n; r++) {
    // 加入 a[r]
    while (窗口不满足条件) {
        // 移除 a[l]
        l++;
    }
    // 更新答案
}

// 双指针（接雨水模板）
long long ans = 0;
int l = 0, r = n - 1;
int left_max = 0, right_max = 0;
while (l < r) {
    if (h[l] < h[r]) {
        if (h[l] >= left_max) left_max = h[l];
        else ans += left_max - h[l];
        l++;
    } else {
        if (h[r] >= right_max) right_max = h[r];
        else ans += right_max - h[r];
        r--;
    }
}
```

**相关题目**：
- [维护元素频率最大值](http://scpc.fun/problem/10200)（第35题，滑动窗口+离散化+频率维护）
- [或的子段和](http://scpc.fun/problem/SCPC-1077H)（第61题，滑动窗口+位运算）
- [接雨水](http://scpc.fun/problem/10482)（第117题，前后缀+双指针）

---

<a id="sec-1-9"></a>

### 1.9 ST表
- **适用场景**：静态区间查询（最大值、最小值、GCD、AND等满足结合律且可重复贡献的运算）。
- **时间复杂度**：预处理 $O(n \log n)$，查询 $O(1)$。
- **核心思想**：预处理 $st[j][i]$ 表示从 $i$ 开始长度为 $2^j$ 的区间信息，查询时用两个重叠区间覆盖查询区间。

```cpp
// ST表模板（以区间最大值为例）
int n;
int a[MAXN];
int st[MAXN][LOG];  // st[i][j]: 从i开始长度2^j的区间
int lg2[MAXN];

void build() {
    lg2[1] = 0;
    for (int i = 2; i <= n; i++) lg2[i] = lg2[i / 2] + 1;
    for (int i = 1; i <= n; i++) st[i][0] = a[i];
    for (int j = 1; (1 << j) <= n; j++)
        for (int i = 1; i + (1 << j) - 1 <= n; i++)
            st[i][j] = max(st[i][j-1], st[i + (1 << (j-1))][j-1]);
}

int query(int l, int r) {
    int k = lg2[r - l + 1];
    return max(st[l][k], st[r - (1 << k) + 1][k]);
}

// 适配不同运算的版本
// GCD: st[i][j] = gcd(st[i][j-1], st[i+(1<<(j-1))][j-1])
// AND: st[i][j] = st[i][j-1] & st[i+(1<<(j-1))][j-1]
// MIN: st[i][j] = min(st[i][j-1], st[i+(1<<(j-1))][j-1])
```

**相关题目**：
- [位运算](http://scpc.fun/problem/10461)（第43题，ST表+二分答案，区间AND）
- [Iva & Pav](http://scpc.fun/problem/CF-1878E)（第54题，ST表+二分答案）
- [或的子段和](http://scpc.fun/problem/SCPC-1077H)（第61题，ST表+二分，区间OR）
- [理想的正方形](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P2216)（第71题，二维ST表/单调队列）
- [米欧欧是笨蛋吗？](http://scpc.fun/problem/10446)（第103题，ST表区间AND）
- [萌萌哒](http://scpc.fun/problem/10447)（第104题，并查集+ST表）
- [Friends and Subsequences](http://scpc.fun/problem/CF-689-D)（第121题，二分+ST表）
- [gcd区间](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P1890)（第159题，ST表/线段树区间GCD）

---

<a id="sec-1-10"></a>

### 1.10 位运算
- **适用场景**：利用二进制位进行高效计算，如异或、与、或运算的性质。

**核心技巧**：
- $x \oplus x = 0$，$x \oplus 0 = x$
- $x \text{ AND } y + x \text{ OR } y = x + y$（不进位加法 + 进位 = 和）
- $x \text{ XOR } y = (x \text{ OR } y) \text{ AND NOT}(x \text{ AND } y)$

```cpp
// 位运算基本技巧
int lowbit(int x) { return x & (-x); }       // 最低位的1
int popcount(int x) { return __builtin_popcount(x); }  // 1的个数
bool is_pow2(int x) { return x > 0 && (x & (x-1)) == 0; }  // 是否为2的幂

// 前缀异或
// preXor[i] = a[1] ^ a[2] ^ ... ^ a[i]
// 区间异或 [l,r] = preXor[r] ^ preXor[l-1]

// 前缀和 + 位运算（分别处理每一位）
for (int i = 1; i <= n; i++) {
    pre1[i] = pre1[i-1] + (s[i-1] == '1');       // 1的个数
    preXor[i] = preXor[i-1] ^ (s[i-1] == '1');   // 异或前缀
}
```

**相关题目**：
- [magic of bitmask](http://scpc.fun/problem/9282)（第12题，AND/OR推导XOR）
- [我最爱位运算矩阵啦！](http://scpc.fun/problem/9291)（第13题，位运算矩阵）
- [前天是光棍节，可谁又是单身狗呢，我不说](http://scpc.fun/problem/9258)（第82题，异或找唯一数）
- [and、or、xor？](http://scpc.fun/problem/SCPC-1186M)（第139题，前缀和+位运算）

---

<a id="sec-1-11"></a>

### 1.11 CDQ分治
- **适用场景**：解决**三维偏序**问题（如"统计满足 $a_i \le a_j, b_i \le b_j, c_i \le c_j$ 的点对数量"），以及优化某些一维DP（消除后效性）。本质是把"在线数据结构"换成"离线分治"以降低常数和代码难度。
- **核心思想**：分治三步走：
1. **divide**：按 mid 把序列分成左右两半
2. **conquer**：递归处理左半 + 递归处理右半（左半的下标必然 < 右半的下标，第一维已满足）
3. **combine**：合并时把左右两半按第二维排序，用**树状数组**统计第三维的贡献
- **时间复杂度**：$O(n \log^2 n)$（排序 + 树状数组各 $O(\log n)$）

```cpp
// 经典三维偏序：统计点对 (i,j) 满足 i<j, a[i]<=a[j], b[i]<=b[j], c[i]<=c[j]
// 思路：先按 a 排序（保证第一维），CDQ 分治按原下标，合并时按 b 排序，树状数组维护 c

const int MAXN = 100005;
struct Node { int a, b, c, w; };   // w：同值权值（去重后该值出现次数）
Node arr[MAXN], tmp[MAXN];
int ans[MAXN];
int BIT[MAXN], MAXC;

void bit_add(int i, int v) { for (; i <= MAXC; i += i & -i) BIT[i] += v; }
int bit_query(int i) { int s = 0; for (; i > 0; i -= i & -i) s += BIT[i]; return s; }

// CDQ 分治：处理 [l, r) 范围内的点对贡献
// 前置条件：arr 已按 a 升序排好（同 a 同 b 同 c 的已合并并记 w）
void cdq(int l, int r) {
    if (r - l <= 1) return;
    int mid = (l + r) >> 1;
    cdq(l, mid);
    cdq(mid, r);
    // 合并：按 b 升序归并
    int i = l, j = mid, k = l;
    while (i < mid && j < r) {
        if (arr[i].b <= arr[j].b) { bit_add(arr[i].c, arr[i].w); tmp[k++] = arr[i++]; }
        else                       { ans[j] += bit_query(arr[j].c) * arr[j].w; tmp[k++] = arr[j++]; }  // 左边 b<=右边b 的点都贡献到答案
    }
    while (i < mid) { bit_add(arr[i].c, arr[i].w); tmp[k++] = arr[i++]; }
    while (j < r)   { ans[j] += bit_query(arr[j].c) * arr[j].w; tmp[k++] = arr[j++]; }
    // 清空树状数组（必须按已加入顺序撤销，避免 memset TLE）
    for (int t = l; t < mid; t++) bit_add(arr[t].c, -arr[t].w);
    // 拷回
    for (int t = l; t < r; t++) arr[t] = tmp[t];
}
```

**使用注意**：
- 必须**先按 a 排序**，让第一维天然满足"左 < 右"。
- 树状数组清空**不能 memset**（会 TLE），只能按已加入顺序撤销。
- 若 a, b, c 都相同的多组点要合并为 1 个并记 w，否则贡献会重复。
- CDQ 套 CDQ 可处理 4 维偏序（$O(n \log^3 n)$），但常数大；4 维以上一般用bitset。

---

<a id="sec-1-12"></a>

### 1.12 整体二分
- **适用场景**：多个询问都形如"求第 k 小/大"或"判定性二分"，且这些询问**可以离线**。把每个询问独立二分的 $O(q \cdot \log V \cdot \text{check})$ 优化为 $O((n+q) \cdot \log V \cdot \text{check})$，即所有询问**共享同一棵二分决策树**。
- **核心思想**：递归函数 `solve(ops, L, R)` 表示：当前 ops 中的所有操作（修改+询问）的答案都在值域 [L, R] 内。取 mid = (L+R)/2，把修改按"值 ≤ mid" 分到左支、"> mid" 分到右支；把询问按"当前前缀内 ≤ mid 的修改数 ≥ k" 分到左支、否则分到右支。递归直到 L=R 即得到答案。

```cpp
// 经典模型：动态区间第 k 小（带修改）
// 操作类型：1 i x 表示把 a[i] 改为 x；2 l r k 表示查询 [l,r] 中第 k 小
// 思路：所有修改/查询塞进 ops 数组，整体二分值域

const int MAXN = 100005;
struct Op { int op, l, r, k, id; };   // op: 1=修改 2=查询；修改时 l=位置 r=新值；查询时 l,r=区间 k=第k小 id=询问编号
Op ops[MAXN], tmpL[MAXN], tmpR[MAXN];
int ans[MAXN];                        // 各询问的答案
int BIT[MAXN];

void bit_add(int i, int v) { for (; i < MAXN; i += i & -i) BIT[i] += v; }
int bit_query(int i) { int s = 0; for (; i > 0; i -= i & -i) s += BIT[i]; return s; }

// solve：当前 ops[0..cnt-1] 的答案都在值域 [L, R] 中
void solve(Op* ops, int cnt, int L, int R) {
    if (!cnt) return;
    if (L == R) {
        for (int i = 0; i < cnt; i++)
            if (ops[i].op == 2) ans[ops[i].id] = L;
        return;
    }
    int mid = (L + R) >> 1;
    int cntL = 0, cntR = 0;
    for (int i = 0; i < cnt; i++) {
        if (ops[i].op == 1) {                              // 修改操作
            if (ops[i].r <= mid) { bit_add(ops[i].l, 1); tmpL[cntL++] = ops[i]; }
            else                { bit_add(ops[i].l, -1); tmpR[cntR++] = ops[i]; }
            // 注：上面假设原值已加入树状数组，新值更小则"翻转方向"——
            //   实际写法更复杂，这里给的是简化示意；完整版需记录每个位置的"上一值"
        } else {                                            // 查询操作
            int cnt_le = bit_query(ops[i].r) - bit_query(ops[i].l - 1);
            if (cnt_le >= ops[i].k) tmpL[cntL++] = ops[i];
            else { ops[i].k -= cnt_le; tmpR[cntR++] = ops[i]; }
        }
    }
    // 撤销左支的修改
    for (int i = 0; i < cntL; i++)
        if (tmpL[i].op == 1) bit_add(tmpL[i].l, -1);
    for (int i = 0; i < cntR; i++)
        if (tmpR[i].op == 1) bit_add(tmpR[i].l, 1);   // 把右支修改撤销（之前 add -1）
    // 递归
    solve(tmpL, cntL, L, mid);
    solve(tmpR, cntR, mid + 1, R);
}
```

**使用注意**：
- 整体二分**必须离线**（所有操作预先读入）。
- 树状数组**每次进入分支前必须清空**对应修改，避免污染其他分支。
- 经典应用：静态区间第 k 小（无修改版）、带修区间第 k 小、矩阵第 k 小。

---

<a id="sec-1-13"></a>

### 1.13 Meet-in-the-Middle
- **适用场景**：暴力枚举子集 $O(2^n)$ 在 $n \ge 40$ 时不可行，但 $n \le 40$ 时可拆成两半各 $2^{20}$ 枚举，再合并。经典应用：**超大背包问题**（n=40 时普通 0/1 背包 DP 因重量值域太大失效，只能 Meet-in-the-Middle）。
- **核心思想**：把 n 个物品分成前一半 A 和后一半 B，分别枚举 $2^{n/2}$ 种选取方案，记录每种的 (重量, 价值)；然后对 A 按 weight 排序、做价值前缀最大；遍历 B 中每种方案，在 A 中二分找"重量 ≤ 剩余容量"的最大价值，合并取最大。
- **时间复杂度**：$O(2^{n/2} \cdot n)$（拆分枚举 + 排序 + 二分合并）

```cpp
// 超大背包：n 个物品，每个有 w[i] 和 v[i]，背包容量 W。n ≤ 40，w, v 可达 1e9
long long meet_in_middle_knapsack(int n, vector<long long>& w, vector<long long>& v, long long W) {
    int half = n / 2;
    // 枚举前一半
    vector<pair<long long, long long>> A;   // (weight, value)
    for (int mask = 0; mask < (1 << half); mask++) {
        long long sw = 0, sv = 0;
        for (int i = 0; i < half; i++)
            if (mask >> i & 1) { sw += w[i]; sv += v[i]; }
        if (sw <= W) A.push_back({sw, sv});
    }
    sort(A.begin(), A.end());
    // 把 A 改造成"按 weight 不降，value 严格递增的最大前缀"——筛掉无用解
    for (int i = 1; i < (int)A.size(); i++)
        A[i].second = max(A[i].second, A[i - 1].second);
    // 枚举后一半 + 二分合并
    int other = n - half;
    long long ans = 0;
    for (int mask = 0; mask < (1 << other); mask++) {
        long long sw = 0, sv = 0;
        for (int i = 0; i < other; i++)
            if (mask >> i & 1) { sw += w[half + i]; sv += v[half + i]; }
        if (sw > W) continue;
        long long rem = W - sw;
        // 在 A 中找 weight <= rem 的最大 value
        int lo = 0, hi = (int)A.size() - 1, pos = -1;
        while (lo <= hi) {
            int mid = (lo + hi) >> 1;
            if (A[mid].first <= rem) { pos = mid; lo = mid + 1; }
            else hi = mid - 1;
        }
        if (pos != -1) ans = max(ans, sv + A[pos].second);
        else           ans = max(ans, sv);
    }
    return ans;
}
```

**使用说明**：
- 经典变体：n 个数能否选出若干异或为 0 / 异或最大 → 拆半枚举 + 哈希合并。
- $n = 40$ 时 $2^{20} \approx 10^6$，可过；$n = 50$ 时 $2^{25} \approx 3 \times 10^7$，开始吃紧。

---

<a id="sec-1-14"></a>

### 1.14 模拟退火与爬山
- **适用场景**：NP-hard 问题的近似解，或几何最值/分数规划不会做时"骗分"。常见：求几何图形放置最值、TSP、配送中心选址。
- **核心思想**：
- **爬山**：每步从当前解的邻域中选一个更优的解移动过去；缺点：易陷入局部最优。
- **模拟退火**：每步随机一个新解，若更优则接受；若更差则以概率 $e^{-\Delta E / T}$ 接受（T 是当前温度，逐渐降温）。高温时大胆接受劣解，低温时趋于只接受优解，模拟金属退火过程。

```cpp
// ============= 模拟退火通用模板 =============
// 以"求 f(x) 的最小值"为例（求最大值把 ΔE 取负即可）
// 注意：使用前请用 mt19937 设置好随机种子

mt19937 rng(chrono::steady_clock::now().time_since_epoch().count());
uniform_real_distribution<double> rnd01(0.0, 1.0);

// state 表示一个解；f(state) 返回该解的目标函数值
// 邻域生成函数 neighbor(state)：在当前解附近随机扰动产生新解

double T0 = 1e4;          // 初始温度（根据 f 值量级调整）
double T_end = 1e-6;      // 终止温度
double cooling = 0.995;   // 降温系数（0.99~0.999，越慢越精确）

// 单次退火：从初始状态 cur 出发，返回最优状态
template<typename State, typename F, typename N>
State anneal(State cur, F f, N neighbor) {
    State best = cur;
    double curE = f(cur), bestE = curE;
    for (double T = T0; T > T_end; T *= cooling) {
        State nxt = neighbor(cur);                  // 邻域扰动
        double nxtE = f(nxt);
        double dE = nxtE - curE;                    // ΔE = 新-旧
        if (dE < 0 || rnd01(rng) < exp(-dE / T)) {  // 更优直接接受；更差按概率接受
            cur = nxt; curE = nxtE;
            if (curE < bestE) { best = cur; bestE = curE; }
        }
    }
    return best;
}

// ============= 爬山算法模板（仅接受更优解）=============
template<typename State, typename F, typename N>
State hill_climb(State cur, F f, N neighbor, int max_iter = 100000) {
    State best = cur;
    double bestE = f(cur);
    for (int iter = 0; iter < max_iter; iter++) {
        State nxt = neighbor(cur);
        double nxtE = f(nxt);
        if (nxtE < bestE) { cur = nxt; best = nxt; bestE = nxtE; }
        else break;     // 走不动了（爬山遇局部最优就停）
    }
    return best;
}
```

**使用技巧**：
- **多跑几次**取最优（不同随机种子），避免一次卡局部最优。
- **时限内能跑多少跑多少**：`while ((double)clock() / CLOCKS_PER_SEC < 0.9)` 反复退火。
- 邻域扰动幅度随温度下降而减小（高温大步跳，低温小步精调），通常 `step = base * T / T0`。
- 几何题用模拟退火非常常见：随机扰动点的坐标，比正解快得多且常能AC。

---
<a id="sec-1-15"></a>

### 1.15 单调栈

- **适用场景**：在线性时间内找到每个元素左侧/右侧第一个比它大（或小）的元素。典型应用包括：柱状图中最大矩形、接雨水、求每个位置作为最大值/最小值的区间、笛卡尔树构造。
- **时间复杂度**：$O(n)$（每个元素最多入栈一次、出栈一次）。
- **建议上界**：$n \leq 3 \times 10^6$（1秒时限）。
- **核心思想**：维护一个栈，栈中元素**保持单调递增（或递减）**。从左到右扫一遍，当新元素不满足单调性时，弹栈并在弹栈时记录答案——"被弹出的元素遇到了右侧第一个更大/更小的元素"。

**模板一：求左右第一个更大的元素（柱状图最大矩形）**

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 100010;

int n;
long long h[MAXN];
int L[MAXN], R[MAXN];   // L[i]/R[i]: i 左/右方首个 < h[i] 的下标（不存在取0或n+1）

long long largest_rectangle_area() {
    stack<int> st;
    // 左边界：找左边第一个 < h[i] 的位置
    for (int i = 1; i <= n; i++) {
        while (!st.empty() && h[st.top()] >= h[i]) st.pop();
        L[i] = st.empty() ? 0 : st.top();
        st.push(i);
    }
    while (!st.empty()) st.pop();
    // 右边界：找右边第一个 <= h[i] 的位置（注意一边含等号，避免等高处重复计算）
    for (int i = n; i >= 1; i--) {
        while (!st.empty() && h[st.top()] > h[i]) st.pop();
        R[i] = st.empty() ? (n + 1) : st.top();
        st.push(i);
    }
    long long ans = 0;
    for (int i = 1; i <= n; i++)
        ans = max(ans, h[i] * (long long)(R[i] - L[i] - 1));
    return ans;
}

int main() {
    quick;
    while (cin >> n && n) {
        for (int i = 1; i <= n; i++) cin >> h[i];
        cout << largest_rectangle_area() << endl;
    }
    return 0;
}
```

**模板二：接雨水（统计每个位置上方能存储的水量）**

```cpp
// 给定高度数组 height，求能接住的雨水总量。
// 思路：对每个位置 i，找左边最高 Lmax、右边最高 Rmax，贡献 = min(Lmax, Rmax) - height[i]
long long trap_water(vector<int>& height) {
    int n = height.size();
    vector<int> L(n), R(n);
    stack<int> st;
    for (int i = 0; i < n; i++) {        // 前缀最大值数组（也可用单调栈/直接递推）
        L[i] = i == 0 ? height[i] : max(L[i - 1], height[i]);
    }
    for (int i = n - 1; i >= 0; i--) {
        R[i] = i == n - 1 ? height[i] : max(R[i + 1], height[i]);
    }
    long long ans = 0;
    for (int i = 0; i < n; i++)
        ans += max(0, min(L[i], R[i]) - height[i]);
    return ans;
}
```

- **最大矩形 / 全 1 子矩阵**：逐行做"柱状图最大矩形"，每行 h[j] = 当前列向上连续 1 的个数。
- **子数组最小值之和**：对每个位置 i，找左边第一个 ≤ a[i]、右边第一个 < a[i] 的位置，乘法原理计算以 a[i] 为最小值的子数组个数：`(i-L)*(R-i)`。
- **笛卡尔树**：用单调栈建树（每次插入节点时弹栈直到父亲，然后挂上左/右子树），天然满足"父节点是区间最值"的性质。

---

<a id="sec-1-16"></a>

### 1.16 01BFS（双端队列最短路）

- **适用场景**：图的边权只有 0 或 1（更一般地，边权集合只有两种值，差值较小）时，求从起点出发的单源最短路。
- **时间复杂度**：$O(n + m)$（每个节点/每条边至多被松弛一次，比 Dijkstra 的 $O(m \log n)$ 更优）。
- **建议上界**：$n, m \leq 2 \times 10^6$（1秒时限）。
- **核心思想**：维护一个双端队列 `deque`。当沿边权为 0 的边扩展时，新距离与当前距离相同，放到**队首**；沿边权为 1 的边扩展时，新距离 = 当前距离 + 1，放到**队尾**。这样天然保证了队列中节点的距离是单调不减的——类似 Dijkstra 但省去了优先队列的 log 开销。

- **模板代码：**

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 200010;
const int INF = 0x3f3f3f3f;

int n, m, S;
vector<pair<int,int>> G[MAXN];   // {v, w}，w ∈ {0, 1}
int dist[MAXN];

void bfs01(int S) {
    memset(dist, 0x3f, sizeof(int) * (n + 1));
    dist[S] = 0;
    deque<int> dq;
    dq.push_front(S);
    while (!dq.empty()) {
        int u = dq.front(); dq.pop_front();
        for (auto& [v, w] : G[u]) {
            if (dist[v] > dist[u] + w) {
                dist[v] = dist[u] + w;
                if (w == 0) dq.push_front(v);
                else        dq.push_back(v);
            }
        }
    }
}

int main() {
    quick;
    cin >> n >> m >> S;
    for (int i = 1; i <= n; i++) G[i].clear();
    for (int i = 0; i < m; i++) {
        int u, v, w; cin >> u >> v >> w;
        G[u].emplace_back(v, w);
        // 无向图：G[v].emplace_back(u, w);
    }
    bfs01(S);
    for (int i = 1; i <= n; i++) {
        if (dist[i] >= INF) cout << -1 << ' ';
        else                cout << dist[i] << ' ';
    }
    cout << endl;
    return 0;
}
```

**常见建模场景**：
- **网格图中"翻转代价为1、移动代价为0"**：比如 01 迷宫（0可走、1需炸开），或者推箱子问题。
- **网络切换 / 分层图 0/1 层**：最短路问题中加上"允许一次免费跳跃/传送"的分层技巧，连一条层间 0 边，剩余 1 边。
- **边权为 a 或 b（a, b 相近，都不大）**：可将距离乘以常数 `mod`，用 `Dial算法`（按桶分队列，0/1/2/.../k 桶循环取）；当只有两种边权时 01BFS 可直接套用。
- **易错点**：
- 图中边权必须满足 `w ∈ {0, C}`，其中 C 为固定常数（C=1最常见）。若存在任意正权边，01BFS 正确性不成立，应改回 Dijkstra / SPFA。
- 双端队列入队可以**重复入队**（不需要 inque 标记判重，因为 dist 已经做了松弛判断，但每个点最多被松弛的次数仍保证为常数级）。

---





