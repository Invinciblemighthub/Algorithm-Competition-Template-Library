## [返回目录](http://scpc.fun/discussion-detail/1405)

<style>
a[id], h2, h3, h4 { scroll-margin-top: 140px; }
</style>

---

## 本章目录

### [附录：其他算法模板](#ch9)
- [前缀和+哈希](#app-1) **进阶**
- [质因数分解+DFS](#app-2) **进阶**
- [数学公式](#app-3) **必学**
- [随机化算法](#app-4) **选学**
- [统计数位和](#app-5) **选学**
- [STL优先队列贪心](#app-6) **进阶**
- [哈希查找](#app-7) **进阶**
- [预处理+映射](#app-8) **进阶**
- [快速IO加强：快读快写（大输入必备）](#app-9) **必学**
- [对拍 / 随机数据生成（调题救命工具）](#app-10) **进阶**
- [常用公式速查表（扩展版）](#app-11) **必学**
- [容斥定理](#app-12) **进阶**

### [多组数据怎么清空数组](#mul-data) **必学**
- [链式前向星 / 邻接表](#mul-1)
- [距离 / 代价数组](#mul-2)
- [并查集 / 带权并查集](#mul-3)
- [线段树 / 树状数组 / 主席树](#mul-4)
- [访问标记 / VIS数组](#mul-5)
- [其他高频结构](#mul-6)
- [最稳妥的万能写法](#mul-7)

### [不知道用哪个算法？看这里](#algo-select) **必学**
- [区间 / 序列 类问题](#sel-1)
- [树上问题](#sel-2)
- [图论 / 最短路 / 连通性](#sel-3)
- [字符串](#sel-4)
- [数学 / 数论](#sel-5)
- [数据结构辅助](#sel-6)
- [算法思想类](#sel-7)

### [内置函数/竞赛技巧速查（慎用）](#builtin-guide) **进阶**
- [<algorithm> 常用算法（一）：排序、二分、翻转、旋转、去重](#btn-1)
- [<algorithm> 常用算法（二）：划分、合并、移除、查找、计数](#btn-2)
- [<algorithm> 常用算法（三）：最值、比较、条件检测、遍历](#btn-3)
- [位运算 __builtin 函数（GCC 特有）](#btn-4)
- [更多 __builtin 函数（溢出检测、字节交换、分支预测、预取）](#btn-5)
- [<numeric> 数值辅助（gcd/lcm/iota/accumulate/…）](#btn-6)
- [<cmath> 数学函数补充](#btn-7)
- [<cctype> / <cstring> 字符与C字符串](#btn-8)
- [数据结构与容器补充（bitset/array/stack/queue等）](#btn-9)
- [<tuple> & <utility>：元组、pair、结构化绑定](#btn-10)
- [字符串辅助（to_string/stoi/stoll/类型转换）](#btn-11)
- [高精度辅助：__int128](#btn-12)
- [快速 IO 极致优化（解除同步/fread/fwrite/getchar_unlocked）](#btn-13)
- [随机化与对拍（mt19937/distribution/计时）](#btn-14)
- [内存与初始化辅助（memset/memcpy/fill 等）](#btn-15)
- [GCC 优化咒语（#pragma / __attribute__ / likely/unlikely）](#btn-16)

### [算法复杂度与数据范围速查表](#complexity) **必学**

---

<a id="ch9"></a>

## 附录：其他算法模板

以下算法在题目中也有涉及，但因篇幅原因仅列出模板和题目链接：

<a id="app-1"></a>

### 前缀和+哈希

```cpp
// 前缀和+哈希：求区间和恰好为k的子区间数
unordered_map<long long, long long> mp;
mp[0] = 1;
long long pref = 0, ans = 0;
for (int i = 0; i < n; i++) {
    pref += a[i];
    ans += mp[pref - k];
    mp[pref]++;
}
```

**相关题目**：
- [区间和恰好为 k](http://scpc.fun/problem/10478)（第114题）
- [山卡西](http://scpc.fun/problem/10477)（第52题）

<a id="app-2"></a>

### 质因数分解+DFS

```cpp
// SPF筛 + 质因数分解
int spf[MAXN];
void sieve() {
    for (int i = 2; i < MAXN; i++) spf[i] = i;
    for (int i = 2; i * i < MAXN; i++)
        if (spf[i] == i)
            for (int j = i * i; j < MAXN; j += i)
                if (spf[j] == j) spf[j] = i;
}
```

**相关题目**：
- [天降横祸](http://scpc.fun/problem/SCPC-1126H)（第131题）

<a id="app-3"></a>

### 数学公式

```cpp
// 平方和公式: 1^2 + 2^2 + ... + n^2 = n(n+1)(2n+1)/6
// 等差数列和: a + (a+d) + ... = n*a + n*(n-1)/2*d
// 斐波那契: F(n) = F(n-1) + F(n-2), F(0)=0, F(1)=1
// 威尔逊定理: 若 p 为质数，则 (p-1)! ≡ -1 (mod p)；反之若 (p-1)!≡-1 (mod p) 则 p 是质数
// 约数个数公式: n = ∏ p_i^{a_i}，则约数个数 d(n) = ∏ (a_i + 1)
// 约数和公式:   n = ∏ p_i^{a_i}，则 σ(n) = ∏ (p_i^{a_i+1} - 1) / (p_i - 1)
```

**相关题目**：
- [我佛造经传极乐，观音奉旨上长安](http://scpc.fun/problem/9304)（第16题）
- [AandB](http://scpc.fun/problem/10472)（第110题）
- [二元咖啡馆](http://scpc.fun/problem/%E5%8D%93%E8%B6%8A-061)（第147题）
- [排列和MEX](http://scpc.fun/problem/9224)（第9题，平方和+等差数列求和公式推导）
- [凢凢的烦恼](http://scpc.fun/problem/10010)（第23题，预处理阶乘模+算数基本定理）

<a id="app-4"></a>

### 随机化算法

```cpp
// 随机化判定（概率算法）
mt19937 rng(chrono::steady_clock::now().time_since_epoch().count());
shuffle(idx.begin(), idx.end(), rng);
// 随机选取样本，大概率命中
```

**相关题目**：
- [数点](http://scpc.fun/problem/9300)（第15题）

<a id="app-5"></a>

### 统计数位和

```cpp
// 统计1到n每个数的数位和出现次数
for (int i = 1; i <= n; i++) {
    int x = i, sum = 0;
    while (x) { sum += x % 10; x /= 10; }
    cnt[sum]++;
}
```

**相关题目**：
- [狡兔三窟](http://scpc.fun/problem/SCPC-1075B)（第128题）

<a id="app-6"></a>

### STL优先队列贪心

```cpp
// 优先队列贪心（每次选收益最大的操作）
priority_queue<pair<double,int>> pq;  // {收益, 下标}
while (m-- && !pq.empty()) {
    auto [gain, idx] = pq.top(); pq.pop();
    // 执行操作，更新收益
    // 若新收益>0则重新入队
}
```

**相关题目**：
- [亡羊补牢](http://scpc.fun/problem/9213)（第7题）

<a id="app-7"></a>

### 哈希查找

```cpp
// 哈希表预计算查找
unordered_map<int,int> mp;
for (int i = 1; i <= N; i++) {
    int g = i * (i + 1) / 2;  // 三角数
    mp[g] = 1;
    if (mp.count(n - g)) { flag = 1; break; }
    if (g > n) break;
}
```

**相关题目**：
- [又一一个好数问题](http://scpc.fun/problem/%E5%8D%93%E8%B6%8A-072)（第151题）
- [The most beautiful meeting](http://scpc.fun/problem/9226)（第10题，哈希记录位置+计数对数）

<a id="app-8"></a>

### 预处理+映射

```cpp
// 预处理字符映射：收集出现的字符，按修改规则建立映射后批量替换
map<char,char> mp;
vector<char> v;        // 出现过的字符
int vis[30];
for (char c : s)
    if (!vis[c - 'a']) { vis[c - 'a'] = 1; v.push_back(c); }
for (char c : v) {
    mp[c] = c;
    for (auto& [a, b] : rules) {     // rules 为修改规则
        if (mp[c] == a) mp[c] = b;   // 单向替换
        // else if (mp[c] == b) mp[c] = a;  // 双向替换（如团队改名）
    }
}
for (char& c : s) if (mp.count(c)) c = mp[c];
```

**相关题目**：
- [字符修改](http://scpc.fun/problem/9206)（第79题，预处理字符单向映射）
- [团队改名](http://scpc.fun/problem/SCPC-1199A)（第141题，预处理字符双向映射）

---

<a id="app-9"></a>

### 快速IO加强：快读快写（大输入必备）
- **适用场景**：输入量 ≥ 5×10^5 数字 / ≥ 3×10^6 字符时，`cin + quick` 仍可能超时，此时改用手写 `getchar()` / `putchar()`。

```cpp
// ========== 快读（读入一个整数，支持负数） ==========
inline int qread() {
    int x = 0, f = 1; char c = getchar();
    while (c < '0' || c > '9') { if (c == '-') f = -1; c = getchar(); }
    while (c >= '0' && c <= '9') { x = x * 10 + (c - '0'); c = getchar(); }
    return x * f;
}
// long long 版本
inline long long qread_ll() {
    long long x = 0; int f = 1; char c = getchar();
    while (c < '0' || c > '9') { if (c == '-') f = -1; c = getchar(); }
    while (c >= '0' && c <= '9') { x = x * 10 + (c - '0'); c = getchar(); }
    return x * f;
}

// ========== 快写（写出一个整数，支持负数） ==========
inline void qwrite(int x) {
    if (x < 0) { putchar('-'); x = -x; }
    if (x > 9) qwrite(x / 10);
    putchar(x % 10 + '0');
}
// long long 版本
inline void qwrite_ll(long long x) {
    if (x < 0) { putchar('-'); x = -x; }
    if (x > 9) qwrite_ll(x / 10);
    putchar(x % 10 + '0');
}

// ========== 读整行字符串（含空格） ==========
inline void qreads(char* s) {
    char c = getchar();
    while (c != '\n' && c != ' ') { *s++ = c; c = getchar(); }
    *s = '\0';
}
```

**常见卡常手段**（选1~2条即可，按编译器情况斟酌）：
```cpp
// 1. 常用宏（已在模板中加入）
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

// 2. 关闭同步（写在main首行）
ios::sync_with_stdio(false); cin.tie(nullptr);

// 3. GCC 编译器属性（写在文件最顶部或函数前，少量常数级加速）
#pragma GCC optimize("Ofast,no-stack-protector")
#pragma GCC target("sse,sse2,sse3,ssse3,sse4,popcnt,abm,mmx,avx,avx2,tune=native")
#pragma GCC optimize("unroll-loops")

// 4. register / inline（C++17 起编译器自动inline，但写了更保险）
inline int solve(int n) { /*...*/ }

// 5. 大数组移到全局（避免栈溢出，速度也略快）：不要在 main 里开 int a[1<<20];
```

---

<a id="app-10"></a>

### 对拍 / 随机数据生成（调题救命工具）

> 💡 **对拍流程**：自己写一份`暴力/小范围正确`的程序 `brute.cpp` 和`正解` `std.cpp`，再写 `random_data.cpp` 造随机数据，用 `compare.cpp`（或 shell 脚本）循环比对。若输出不同则立刻打印出错数据，调试只需喂错误样例即可。

---

**随机数据生成器 `random_data.cpp`（通用模板）**

```cpp
#include<bits/stdc++.h>
using namespace std;

mt19937 rng(chrono::steady_clock::now().time_since_epoch().count());

// 随机整数 [l, r]
inline int randint(int l, int r) {
    return uniform_int_distribution<int>(l, r)(rng);
}
// 随机 long long [l, r]
inline long long randll(long long l, long long r) {
    return uniform_int_distribution<long long>(l, r)(rng);
}
// 随机实数 [l, r]
inline long double randreal(long double l, long double r) {
    return uniform_real_distribution<long double>(l, r)(rng);
}

// 生成一棵树（n个节点，带边权[Wmin, Wmax]）
void gen_tree(int n, int Wmin, int Wmax) {
    vector<int> perm(n); iota(perm.begin(), perm.end(), 1);
    shuffle(perm.begin(), perm.end(), rng);
    vector<pair<int,int>> edges;
    for (int i = 1; i < n; i++) {
        int u = perm[i];
        int fa = perm[randint(0, i - 1)];   // 随机向前面的点连边，保证树结构
        edges.emplace_back(u, fa);
    }
    shuffle(edges.begin(), edges.end(), rng);  // 打乱边序，制造无规则输入
    for (auto& [u, v] : edges) {
        if (randint(0, 1)) swap(u, v);         // 随机方向
        int w = randint(Wmin, Wmax);
        cout << u << ' ' << v << ' ' << w << '\n';
    }
}

// 生成一张图（n点m边，无重边无自环）——m ≤ n*(n-1)/2
void gen_graph(int n, int m, int Wmin, int Wmax) {
    set<pair<int,int>> used;
    int gened = 0;
    while (gened < m) {
        int u = randint(1, n), v = randint(1, n);
        if (u == v) continue;
        if (u > v) swap(u, v);
        if (used.count({u, v})) continue;
        used.insert({u, v});
        int w = randint(Wmin, Wmax);
        cout << u << ' ' << v << ' ' << w << '\n';
        gened++;
    }
}

int main() {
    int n = randint(3, 10);     // 小范围对拍，n不要太大
    int m = randint(n, n + 5);  // m要在合理范围内
    int Q = randint(1, 10);
    cout << n << ' ' << m << ' ' << Q << '\n';
    for (int i = 1; i <= n; i++) cout << randint(-10, 10) << " \n"[i == n];
    gen_graph(n, m, -100, 100);
    while (Q--) cout << randint(1, n) << ' ' << randint(1, n) << '\n';
    return 0;
}
```



```

**对拍小技巧**：
1. **先拍小数据**：n ≤ 20 先拍 100 组，大概率能拍出来。
2. **拍不过时缩范围**：如果小数据都过了，再把 n 升到 100、1000 去卡边界。
3. **对比时注意输出格式**：多余空格 / 换行顺序不同导致 diff 误判时，用 `sort` 或 `awk` 归一化后再比。
4. **多组输入时要单独定位**：第 i 组错了就用 `sed -n 'L1,L2p'` 把第 i 组摘出来单独喂，不要整段跑。

---

<a id="app-11"></a>

### 常用公式速查表（扩展版）

将高频用到的数学公式集中汇总，做题时可直接翻阅。

#### 一、求和公式

| 公式 | 结果 | 备注 |
|---|---|---|
| $1 + 2 + \cdots + n$ | $\dfrac{n(n+1)}{2}$ | 等差数列求和 |
| $a + (a+d) + (a+2d) + \cdots + (a+(n-1)d)$ | $na + \dfrac{n(n-1)}{2}d$ | 一般等差数列 |
| $a + ar + ar^2 + \cdots + ar^{n-1}$ | $a \cdot \dfrac{1-r^n}{1-r}$（$r \neq 1$）；$na$（$r=1$） | 等比数列求和 |
| $1^2 + 2^2 + 3^2 + \cdots + n^2$ | $\dfrac{n(n+1)(2n+1)}{6}$ | 平方和 |
| $1^3 + 2^3 + \cdots + n^3$ | $\left(\dfrac{n(n+1)}{2}\right)^2 = (1+2+\cdots+n)^2$ | 立方和 |
| $1^2 + 3^2 + 5^2 + \cdots + (2n-1)^2$ | $\dfrac{n(2n-1)(2n+1)}{3}$ | 奇数平方和 |
| $1 \cdot 2 + 2 \cdot 3 + \cdots + n(n+1)$ | $\dfrac{n(n+1)(n+2)}{3}$ | 相邻乘积和 |
| $\sum_{i=1}^n i(i+1)(i+2)$ | $\dfrac{n(n+1)(n+2)(n+3)}{4}$ | 三连乘和 |
| $H_n = 1 + \dfrac12 + \dfrac13 + \cdots + \dfrac1n$ | $\approx \ln n + \gamma + \dfrac{1}{2n}$ | 调和级数近似，$\gamma \approx 0.57721566$ |

#### 二、组合恒等式

| 名称 | 公式 | 典型用途 |
|---|---|---|
| 二项式定理 | $(a+b)^n = \sum\limits_{k=0}^n \binom{n}{k} a^{n-k} b^k$ | 直接展开 |
| 对称式 | $\dbinom{n}{k} = \dbinom{n}{n-k}$ | 快速计算 |
| 吸收恒等式 | $k \dbinom{n}{k} = n \dbinom{n-1}{k-1}$ | 消掉组合数前的 k |
| 递推式（杨辉三角） | $\dbinom{n}{k} = \dbinom{n-1}{k} + \dbinom{n-1}{k-1}$ | 基础递推 |
| **范德蒙德卷积** | $\sum\limits_{k=0}^r \dbinom{n}{k}\dbinom{m}{r-k} = \dbinom{n+m}{r}$ | 最常用的合并技巧 |
| 上指标求和 | $\sum\limits_{k=m}^n \dbinom{k}{m} = \dbinom{n+1}{m+1}$ | "一格一步向右上"的路径计数 |
| 全集和 | $\sum\limits_{k=0}^n \dbinom{n}{k} = 2^n$ | 所有子集数 |
| 交错和 | $\sum\limits_{k=0}^n (-1)^k \dbinom{n}{k} = 0$（$n \ge 1$） | 容斥系数 |

#### 三、特殊数列

| 数列 | 通项 / 递推 | 组合意义 |
|---|---|---|
| 斐波那契 | $F_n = F_{n-1} + F_{n-2}$，$F_0=0, F_1=1$ | $F_{n-1} = \dbinom{n-1}{0}+\dbinom{n-2}{1}+\dbinom{n-3}{2}+\cdots$（不相邻选取数） |
| 卡特兰 | $C_n = \dfrac{1}{n+1}\dbinom{2n}{n} = \dbinom{2n}{n} - \dbinom{2n}{n-1}$ | 合法括号串、出栈序列、凸多边形三角剖分等 |
| 贝尔数 | $B_n = \sum\limits_{k=0}^n S(n, k)$，递推 $B_{n+1} = \sum\limits_{k=0}^n \dbinom{n}{k} B_k$ | n 个元素的集合划分总数 |
| 错排数 | $D_n = (n-1)(D_{n-1}+D_{n-2})$，$D_0=1, D_1=0$；或 $D_n = n!\sum\limits_{k=0}^n \dfrac{(-1)^k}{k!}$ | n 个人重新排队，每人不站原位 |
| 第一类斯特林数（无符号） | $S_u(n, k) = (n-1)S_u(n-1,k) + S_u(n-1,k-1)$ | n 个元素划分 k 个**轮换**的方案数 |
| 第二类斯特林数 | $S(n, k) = k\cdot S(n-1,k) + S(n-1,k-1)$ | n 个不同球放 k 个相同非空盒的方案数 |

#### 四、数论公式速查

| 名称 | 公式 / 计算 | 说明 |
|---|---|---|
| 等差数列项数 | $m = \dfrac{a_n - a_1}{d} + 1$ | 求 n 项末项求总项数 |
| 整除分块 | $\sum\limits_{i=1}^n \left\lfloor \dfrac{n}{i} \right\rfloor$ | $\left\lfloor\dfrac{n}{i}\right\rfloor$ 相同的 i 合并：$[l, n/(n/l)]$ 一段 O(1) 加 |
| 约数个数 | $n = \prod p_i^{a_i} \Rightarrow d(n) = \prod (a_i + 1)$ | 每个质因子可选 0~a_i 次 |
| 约数和 | $\sigma(n) = \prod \dfrac{p_i^{a_i+1} - 1}{p_i - 1}$ | 每个质因子幂次独立求和后相乘 |
| 欧拉函数 | $\varphi(n) = n \prod\limits_{p|n} \left(1-\dfrac1p\right)$ | 若 $p$ 质因子重复只需乘一次 $(1-1/p)$ |
| 扩展欧拉定理降幂 | $b \ge \varphi(m)$ 时，$a^b \equiv a^{b \bmod \varphi(m) + \varphi(m)} \pmod{m}$ | 不要求 $\gcd(a,m)=1$ |
| 裴蜀定理 | $\exists x,y,\, ax+by = \gcd(a,b)$ | 推广 n 个数：线性组合能表示 gcd 的所有倍数 |
| 威尔逊定理 | $p$ 质数 $\Leftrightarrow$ $(p-1)! \equiv -1 \pmod{p}$ | 双向等价 |
| 素数计数近似 | $\pi(n) \approx \dfrac{n}{\ln n}$ | 粗估 n 以内素数个数 |

#### 五、几何常见公式

| 名称 | 公式 | 说明 |
|---|---|---|
| 三角形面积（叉积） | $S = \dfrac12 | (B-A) \times (C-A) |$ | 按逆时针给点时直接为正 |
| 海伦公式 | $p = \dfrac{a+b+c}{2},\, S = \sqrt{p(p-a)(p-b)(p-c)}$ | 三边长求面积 |
| 四边形面积（对角线） | $S = \dfrac12 d_1 d_2 \sin\theta$ | 任意四边形，两对角线长度+夹角 |
| Pick 定理 | $S = I + \dfrac{B}{2} - 1$ | 格点多边形：I=内部格点，B=边界格点 |
| 两点欧氏距离 | $|AB| = \sqrt{(x_1-x_2)^2 + (y_1-y_2)^2}$ | 2D / 3D 类似 |
| 两点曼哈顿距离 | $|x_1-x_2| + |y_1-y_2|$ | 等价于旋转 45° 后的切比雪夫距离 |
| 两点切比雪夫距离 | $\max(|x_1-x_2|, |y_1-y_2|)$ | 坐标变换 $(x+y, x-y)$ 转曼哈顿 |
| 正多边形内角和 | $(n-2) \cdot 180^\circ$ | 每个内角 $\dfrac{n-2}{n} \cdot 180^\circ$ |

---

<a id="app-12"></a>

### 容斥定理


- **并集的容斥**：求有限个集合的并集大小
$$
|A_1 \cup A_2 \cup \cdots \cup A_n| = \sum_{i} |A_i| - \sum_{i<j} |A_i \cap A_j| + \sum_{i<j<k} |A_i \cap A_j \cap A_k| - \cdots + (-1)^{n+1} |A_1 \cap \cdots \cap A_n|
$$

- **补集的容斥**（常用来算"都不满足/不被任一个整除"等问题，用全集减并集）：
$$
|\overline{A_1} \cap \overline{A_2} \cap \cdots \cap \overline{A_n}| = |U| - |A_1 \cup \cdots \cup A_n|
$$

- **一般形式**（对每个非空子集 $S \subseteq \{1..n\}$，取子集 $S$ 中集合的交集大小，按子集大小的奇偶性加/减权重）。
- **实现方法**：枚举 `mask` 从 1 到 $(1 \ll n) - 1$（二进制第 $i$ 位 $=1$ 表示选入 $A_i$），popcount(mask) 为偶数 → 减、奇数 → 加。

```cpp
// ============= 经典例题：求 [1, N] 中不被 {p1, p2, ..., pn} 中任何一个整除的整数个数 =============
// 等价：全集 N - |∪A_i|，其中 A_i 表示 [1,N] 中被 p_i 整除的数集合。|A_i| = N / p_i；|A_i ∩ A_j| = N / lcm(p_i, p_j) …
// 时间复杂度：O(2^n * n)，要求 n ≤ 20 左右（用 long long 才装得下mask）

long long coprime_count(long long N, vector<long long>& primes) {
    int n = primes.size();
    long long ans = 0;                      // 累加 |∪A_i|
    for (int mask = 1; mask < (1 << n); mask++) {
        int bits = __builtin_popcount(mask);
        long long lcm_val = 1;
        bool overflow = false;
        for (int i = 0; i < n; i++) {
            if (mask >> i & 1) {
                long long p = primes[i];
                // 求 lcm(lcm_val, p) = lcm_val / gcd * p
                long long g = gcd(lcm_val, p);
                if (lcm_val > N / (p / g)) { overflow = true; break; }   // lcm 已经超过 N，|交集|=0，直接跳
                lcm_val = lcm_val / g * p;
            }
        }
        if (overflow) continue;
        long long card = N / lcm_val;     // |∩ A_i| = floor(N / lcm)
        if (bits % 2 == 1) ans += card;   // 奇数个集合：加
        else                ans -= card;   // 偶数个集合：减
    }
    return N - ans;                        // 补集 = 不被任一个整除的个数
}
```
- **常见题型对应**：
- **[1, N] 中不被任何给定质数整除** → 上面的模板（容斥）。
- **n 对夫妻围成一圈，夫妻不相邻** 的方案数 → 容斥"至少有 k 对夫妻相邻"，乘以 `(-1)^k`。
- **错排数 D(n)** → 本质容斥：D(n) = n! Σ_{k=0}^n (-1)^k / k!（或递推 D(n)=(n-1)(D(n-1)+D(n-2))）。
- 任何"**至少/至多 k 个满足条件**"的计数都可先求"恰好 k 个"→ 转成容斥 / 二项式反演。

---

<a id="mul-data"></a>

## 多组数据怎么清空数组

[↑返回目录](http://scpc.fun/discussion-detail/1405)

> ⚠️ **比赛中 TLE / WA 的头号杀手！** 多组数据时，每组测试结束后必须重置数据结构。使用本模板库时请对照下列清单逐项检查：

<a id="mul-1"></a>

### 链式前向星 / 邻接表
```cpp
// 写法一：head数组清零，边数计数器tot重置
memset(head, 0, sizeof(int) * (n + 1));  // 或循环 for (int i=1;i<=n;i++) head[i]=0;
tot = 0;
// 写法二（推荐）：vector直接clear
for (int i = 1; i <= n; i++) G[i].clear();
```

<a id="mul-2"></a>

### 距离 / 代价数组
```cpp
memset(dist, 0x3f, sizeof(int) * (n + 1));    // 最短路INF
memset(dp, -1, sizeof(dp));                    // 记忆化搜索重置
memset(f, 0, sizeof(f));                       // 普通DP清零
```

<a id="mul-3"></a>

### 并查集 / 带权并查集
```cpp
for (int i = 1; i <= n; i++) fa[i] = i;        // 并查集父亲数组重置
memset(rk, 0, sizeof(rk));                     // 按秩合并的秩清零
```

<a id="mul-4"></a>

### 线段树 / 树状数组 / 主席树
```cpp
// 线段树：memset整棵树（n ≤ 1e5时安全）
memset(tree, 0, sizeof(tree)); memset(lazy, 0, sizeof(lazy));
// 树状数组：n较大时用循环清零，避免memset整段
for (int i = 1; i <= n; i++) bit[i] = 0;
// 主席树：root[0]=0, cnt=0, 每棵子树根重置
memset(root, 0, sizeof(root)); cnt = 0;
```

<a id="mul-5"></a>

### 访问标记 / VIS数组
```cpp
memset(vis, 0, sizeof(vis));                    // bool/int vis数组清零
memset(inque, 0, sizeof(inque));                // SPFA入队标记清零
```

<a id="mul-6"></a>

### 其他高频结构
```cpp
vector<T>().swap(vec);                           // vector彻底释放（比clear更彻底）
stack<T>().swap(st);                             // 栈清空
queue<T>().swap(q);                              // 队列清空
priority_queue<T>().swap(pq);                    // 优先队列清空
mp.clear();                                      // map / unordered_map清空
st.clear();                                      // set清空
```

<a id="mul-7"></a>

### 最稳妥的万能写法
当单组数据量不大（`sizeof(大数组) < 256MB`）时，直接在 `while (T--)` 的**第一行**把大数组整块清零：
```cpp
int T; cin >> T;
while (T--) {
    static int once; if (once++) {
        memset(dist, 0x3f, sizeof dist);
        memset(head, 0, sizeof head);
        for (int i = 1; i <= n; i++) G[i].clear();
        tot = 0;
    }
    /* ... 读入 + 算法 ... */
}
```

> 💡 **小提示**：若使用 `int T; while (cin >> T)` 读入，**一定要在每次读入 n、m 之后再初始化**，不要在不知道大小的情况下提前 memset。

---

<a id="algo-select"></a>

## 不知道用哪个算法？看这里

[↑返回目录](http://scpc.fun/discussion-detail/1405)

> 💡 **怎么用**：先看左侧"我想做什么"，再看"首选模板"直接定位章节查看。复杂问题（路径/连通性/网络流）建议先建模再选型。

<a id="sel-1"></a>

### 区间 / 序列 类问题

| 应用场景 | 首选模板 | 备选 / 特殊 |
|---|---|---|
| 单点更新 + 区间求和 / 前缀和 | [**5.2 树状数组**](http://scpc.fun/discussion-detail/1410#52-树状数组) | [5.3 线段树](http://scpc.fun/discussion-detail/1410#53-线段树)（树状数组不够灵活时） |
| 区间更新 + 区间求和（或通用线段树操作） | [**5.3 线段树**](http://scpc.fun/discussion-detail/1410#53-线段树)（含lazy标记） | 分块（根号算法，代码短但慢） |
| 区间求最大 / 最小 / GCD（无修改） | [**1.9 ST表**](http://scpc.fun/discussion-detail/1406#19-st表) | [5.3 线段树](http://scpc.fun/discussion-detail/1410#53-线段树)（需修改时） |
| 区间第 k 小（离线，允许整批询问一起处理） | [**1.12 整体二分**](http://scpc.fun/discussion-detail/1406#112-整体二分) | [5.8 主席树](http://scpc.fun/discussion-detail/1410#58-主席树)（在线） |
| 区间第 k 小（在线，每次立即回答） | [**5.8 主席树**](http://scpc.fun/discussion-detail/1410#58-主席树) | 划分树（不常用） |
| 区间数颜色 / 不同元素个数 | [5.8 主席树](http://scpc.fun/discussion-detail/1410#58-主席树)（离线预处理） | 莫队（对时限宽松时用） |
| 区间加等差数列 / 高次多项式更新 | [5.3 线段树](http://scpc.fun/discussion-detail/1410#53-线段树)（多次差分） | — |
| 三维偏序 / 多维数点 | [**1.11 CDQ分治**](http://scpc.fun/discussion-detail/1406#111-cdq分治) | 树状数组套树状数组（复杂度高） |

<a id="sel-2"></a>

### 树上问题

| 应用场景 | 首选模板 | 备选 / 特殊 |
|---|---|---|
| 求两个点的最近公共祖先（多次查询） | [**3.4 LCA（倍增）**](http://scpc.fun/discussion-detail/1408#34-lca最近公共祖先) | ST表+欧拉序（更快） |
| 求树上两点之间路径的权值和 / 最值 | [3.4 LCA](http://scpc.fun/discussion-detail/1408#34-lca最近公共祖先) + 前缀和 | 树链剖分（需修改时） |
| 树的直径 / 重心 | [**3.5 树的直径与重心**](http://scpc.fun/discussion-detail/1408#35-树的直径与重心) | — |
| 每个子树内的统计信息（如：子树内颜色数） | [**3.16 DSU on Tree**](http://scpc.fun/discussion-detail/1408#316-启发式合并--dsu-on-tree树上子树查询)（启发式合并） | [5.8 主席树](http://scpc.fun/discussion-detail/1410#58-主席树)（离线） |
| 树上路径计数 / 所有路径的权值和 | [**3.7 贡献法**](http://scpc.fun/discussion-detail/1408#37-贡献法) | 点分治（一般情形） |
| 两点路径上**最大边权的最小值** / 瓶颈路 | [**3.15 Kruskal重构树**](http://scpc.fun/discussion-detail/1408#315-kruskal-重构树) | Kruskal+并查集（离线） |
| 边权 0/1 的最短路 | [**1.16 01BFS**](http://scpc.fun/discussion-detail/1406#116-01bfs双端队列最短路) | Dijkstra（log 因子） |

<a id="sel-3"></a>

### 图论 / 最短路 / 连通性

| 应用场景 | 首选模板 | 备选 / 特殊 |
|---|---|---|
| 单源最短路（边权非负） | **Dijkstra（堆优化）** | — |
| 单源最短路（有负权） / 判负环 | [**3.1.1 Bellman-Ford**](http://scpc.fun/discussion-detail/1408#311-bellman-ford) / SPFA | — |
| 全源最短路（n ≤ 400 ~ 500） | [**3.1.2 Floyd全源最短路**](http://scpc.fun/discussion-detail/1408#312-floyd全源最短路) | Dijkstra跑n次（稀疏图更快） |
| 最小生成树（稀疏图） | Kruskal | [3.2.1 Prim](http://scpc.fun/discussion-detail/1408#321-prim算法)（稠密图略快） |
| 判断有向图强连通分量 / 缩点 | [**3.12 SCC（Tarjan）**](http://scpc.fun/discussion-detail/1408#312-scc强连通分量) | Kosaraju（两次DFS） |
| 无向图割点 / 割边 / 双连通分量 | [**3.13 割点割边与双连通分量**](http://scpc.fun/discussion-detail/1408#313-割点割边与双连通分量) | — |
| 一笔画问题 / 欧拉路径 / 欧拉回路 | [**3.14 欧拉路径与回路**](http://scpc.fun/discussion-detail/1408#314-欧拉路径与回路) | — |
| 差分约束系统（不等式组求解） | [**3.8 差分约束**](http://scpc.fun/discussion-detail/1408#38-差分约束)（Bellman-Ford/SPFA） | — |
| 2-SAT（布尔变量满足性判定） | [**3.9 2-SAT**](http://scpc.fun/discussion-detail/1408#39-2-sat)（SCC+缩点） | — |
| 最大流 / 最小割 | [**3.11 网络流与最小割（Dinic）**](http://scpc.fun/discussion-detail/1408#311-网络流与最小割) | ISAP（略快但实现复杂） |
| 二分图最大匹配 | [**3.3 匈牙利算法**](http://scpc.fun/discussion-detail/1408#33-二分图匹配) | Dinic（直接建图跑最大流，更通用） |
| 二分图最大权完美匹配 | [**3.3.1 KM算法**](http://scpc.fun/discussion-detail/1408#331-km算法) | 费用流（[3.3.2](http://scpc.fun/discussion-detail/1408#332-km费用流等价实现)，更通用但稍慢） |

<a id="sel-4"></a>

### 字符串

| 应用场景 | 首选模板 | 备选 / 特殊 |
|---|---|---|
| 模式匹配（P 在 T 中的所有出现位置） | [**6.1 KMP匹配**](http://scpc.fun/discussion-detail/1411#61-kmp匹配) | Z函数（扩展KMP，线性求所有后缀的LCP） |
| 求最长回文子串（线性） | [**6.2 Manacher马拉车**](http://scpc.fun/discussion-detail/1411#62-manacher马拉车) | 回文自动机（需回文串计数时） |
| 循环节（最小表示） | [**6.3 最小表示法**](http://scpc.fun/discussion-detail/1411#63-最小表示法) | — |
| 多模式串匹配（多个关键词在文本中查找） | [**6.4 AC自动机**](http://scpc.fun/discussion-detail/1411#64-ac自动机) | — |
| 本质不同子串数 / 子串最长公共后缀 | [**6.5 后缀自动机**](http://scpc.fun/discussion-detail/1411#65-后缀自动机) | [6.6 SA](http://scpc.fun/discussion-detail/1411#66-后缀数组sa)（后缀数组，更通用） |
| 子串排名 / 两后缀 LCP（Height数组） | [**6.6 后缀数组SA**](http://scpc.fun/discussion-detail/1411#66-后缀数组sa) | [6.5 后缀自动机](http://scpc.fun/discussion-detail/1411#65-后缀自动机) |

<a id="sel-5"></a>

### 数学 / 数论

| 应用场景 | 首选模板 | 备选 / 特殊 |
|---|---|---|
| GCD / LCM / 扩展欧几里得 | [**4.1 GCD与LCM**](http://scpc.fun/discussion-detail/1409#41-gcd与lcm) | — |
| 线性求逆元（模质数批量） | [**4.2 乘法逆元**](http://scpc.fun/discussion-detail/1409#42-乘法逆元)（递推） | 费马小定理 + 快速幂（单个） |
| 素数筛（1e7以内） | [**4.3 素数筛法（欧拉筛）**](http://scpc.fun/discussion-detail/1409#43-素数筛法) | — |
| 组合数 nCr 取模（n ≤ 2e6） | [**4.4 组合数**](http://scpc.fun/discussion-detail/1409#44-组合数)（预处理阶乘+逆元） | Lucas（n很大、模数较小质数时） |
| 大指数 a^b mod m（b 是字符串） | [**4.7 扩展欧拉定理**](http://scpc.fun/discussion-detail/1409#47-扩展欧拉定理)（降幂） | — |
| 中国剩余定理（求线性同余方程组） | [**4.9 CRT**](http://scpc.fun/discussion-detail/1409#49-中国剩余定理crt) / [**4.10 exCRT**](http://scpc.fun/discussion-detail/1409#410-扩展中国剩余定理excrt) | — |
| 高斯消元求解线性方程组 | [**4.11 高斯消元法**](http://scpc.fun/discussion-detail/1409#411-高斯消元法) | — |
| 异或最大值 / 线性基 | [**4.11 异或高斯消元（线性基）**](http://scpc.fun/discussion-detail/1409#411-高斯消元法) | — |
| 多项式乘法 / 卷积（值域大） | [**4.12 FFT**](http://scpc.fun/discussion-detail/1409#412-fftnttfwt)（double） | NTT（模数为NTT友好质数时更精确） |
| 位运算卷积（AND / OR / XOR） | [**4.12 FWT**](http://scpc.fun/discussion-detail/1409#412-fftnttfwt) | — |
| 取石子类博弈 / 公平组合游戏胜负 | [**4.13 博弈论SG函数**](http://scpc.fun/discussion-detail/1409#413-博弈论sg函数) | — |
| 求 [1,n] 不被任何给定质数整除的数的个数 | [**容斥定理**](http://scpc.fun/discussion-detail/1414#容斥定理)（枚举mask） | — |

<a id="sel-6"></a>

### 数据结构辅助

| 应用场景 | 首选模板 | 备选 / 特殊 |
|---|---|---|
| 字符串哈希判等 / 子串哈希 | 双哈希（附录常用技巧） | 字典树（[5.6 Trie](http://scpc.fun/discussion-detail/1410#56-字典树trie)，需字典树结构时） |
| 01字典树 / 最大异或对 | [**5.7 01Trie**](http://scpc.fun/discussion-detail/1410#57-01trie) | 线性基（线性代数结构时） |
| 可持久化（查询历史版本） | [**5.8 主席树**](http://scpc.fun/discussion-detail/1410#58-主席树) | 可持久化Trie |
| 可拆分合并的平衡树 | [**5.5 平衡树（FHQ Treap）**](http://scpc.fun/discussion-detail/1410#55-平衡树fhq-treap) | Splay（略复杂） |
| 对顶堆（维护中位数 / 动态第k小） | [**5.9 对顶堆**](http://scpc.fun/discussion-detail/1410#59-对顶堆) | 平衡树（支持更多操作） |

<a id="sel-7"></a>

### 算法思想类

| 应用场景| 首选模板 |
|---|---|
| 单调性 / 条件判定问题 | [**1.2 二分答案**](http://scpc.fun/discussion-detail/1406#12-二分答案与二分查找) |
| 单峰（单谷）函数求极值 | [**1.2.1 三分法**](http://scpc.fun/discussion-detail/1406#121-三分法) |
| 区间更新 / 区间和的差分数组 | [**1.3 差分与前缀和**](http://scpc.fun/discussion-detail/1406#13-差分与前缀和) |
| 树上点 / 边更新 + 最后统计（离线） | [**1.3.1 树上差分**](http://scpc.fun/discussion-detail/1406#131-树上差分) |
| 枚举 n=40 左右所有子集（2^40太大） | [**1.13 Meet-in-the-Middle**](http://scpc.fun/discussion-detail/1406#113-meet-in-the-middle) |
| 搜索空间过大 / NP难问题的近似解 | [**1.14 模拟退火与爬山**](http://scpc.fun/discussion-detail/1406#114-模拟退火与爬山) |
| 元素贡献可独立统计 | [**3.7 贡献法**](http://scpc.fun/discussion-detail/1408#37-贡献法)（逐元素累加贡献） |
| 决策单调性DP / 四边形不等式 | [**2.8 区间DP**](http://scpc.fun/discussion-detail/1407#28-区间dp) 及优化章节 |
| 不等式转移优化 / 凸包 | [**2.9 斜率优化DP**](http://scpc.fun/discussion-detail/1407#29-斜率优化dp) |

---







<a id="builtin-guide"></a>

## 内置函数/竞赛技巧速查（慎用）

> 💡 本文汇总 GCC 编译器与 C++ STL 中竞赛常用的内置函数、卡常技巧与编程模板。
> **"慎用"原因：** 以下功能中不少属于 GCC 扩展或平台特定功能，在 MSVC、Clang 等其他编译器下可能无法编译；部分功能在部分正规赛事（如 NOI 系列）可能被禁用。**提交前务必确认编译器（G++ / GNU++）、OJ支持版本和赛事规则。**

<a id="btn-1"></a>

### <algorithm> 常用算法（一）：排序、二分、翻转、旋转、去重

**1. 排序**

| 函数 | 作用 | 复杂度 |
|---|---|---|
| `sort(first, last, comp)` | 快速排序，`comp` 默认升序 `less<>()`，降序传 `greater<>()` | $O(n \log n)$ |
| `stable_sort(first, last, comp)` | 稳定排序（相等元素相对顺序保持不变） | $O(n \log n)$ |
| `nth_element(first, nth, last)` | O(n) 快速选择，`nth` 位置变为"若全局有序则应当在此"的元素；左右两边分别 ≤ / ≥ 它（左右内部不一定有序）。常用于找第k小、划分数据。 | 平均 $O(n)$ |

```cpp
sort(v.begin(), v.end());                         // 默认升序
sort(v.begin(), v.end(), greater<int>());         // 降序
sort(v.begin(), v.end(), [](int a,int b){return a>b;}); // 自定义lambda
nth_element(a, a+k, a+n);   // 使 a[k] 成为第k小（0-based），左边≤a[k]≤右边，O(n)
```

**2. 二分查找（要求序列已排序）**

| 函数 | 作用 | 返回值 |
|---|---|---|
| `lower_bound(first, last, val)` | 左闭右开区间中，找第一个 **≥ val** 的元素迭代器 | 迭代器 |
| `upper_bound(first, last, val)` | 找第一个 **> val** 的元素迭代器 | 迭代器 |
| `binary_search(first, last, val)` | 区间中是否存在等于val的元素 | bool |

```cpp
int pos = lower_bound(a, a+n, val) - a;  // 数组下标版本（0-based）
auto it = upper_bound(v.begin(), v.end(), val);
int cnt = upper_bound(v.begin(),v.end(),val) - lower_bound(v.begin(),v.end(),val); // val出现次数
```

⚠️ **`set::lower_bound` 与 std::lower_bound 不同**
> 对于 `set / map` 这类**双向迭代器**容器，**必须使用成员函数 `s.lower_bound(val)`**（复杂度 $O(\log n)$）。
> 写成 `lower_bound(s.begin(), s.end(), val)` 会退化成 $O(n)$，直接 TLE。

**3. 翻转 / 旋转 / 去重**

| 函数 | 作用 |
|---|---|
| `reverse(first, last)` | 翻转区间元素顺序 |
| `rotate(first, mid, last)` | 将 `[mid, last)` 循环左移到 `[first, ...)` 前面，`first` 最终指向原 `*mid` |
| `unique(first, last)` | **仅对有序区间**去重，把"不重复的元素"移到前面，返回新尾迭代器。**不实际删除元素！** |

```cpp
reverse(s.begin(), s.end());           // 翻转字符串
rotate(v.begin(), v.begin()+k, v.end()); // 循环左移k位（把后n-k移到前面）
// 去重三部曲：
sort(v.begin(), v.end());
v.erase(unique(v.begin(), v.end()), v.end());
```

<a id="btn-2"></a>

### <algorithm> 常用算法（二）：划分、合并、移除、查找、计数

**4. 划分与合并**

| 函数 | 作用 |
|---|---|
| `partition(first, last, pred)` | 按条件 `pred` 划分（为true的放前面），返回分界点；**不保证稳定性** |
| `stable_partition(first, last, pred)` | 保持元素相对顺序的划分 |
| `merge(f1, l1, f2, l2, out)` | 合并**两个有序**区间，输出到 `out`（归并排序核心） |
| `inplace_merge(first, mid, last)` | 原地合并两段已排序子区间，额外空间 $O(n)$ |

```cpp
auto mid = partition(v.begin(), v.end(), [](int x){return x%2==0;});
// 现在 [begin, mid) 是偶数，[mid, end) 是奇数
vector<int> merged(v1.size()+v2.size());
merge(v1.begin(),v1.end(), v2.begin(),v2.end(), merged.begin());
```

**5. 移除（erase-remove 惯用法，非常重要！）**

| 函数 | 作用 |
|---|---|
| `remove(first, last, val)` | 把所有不等于 val 的元素移到前面，返回新尾 |
| `remove_if(first, last, pred)` | 条件版移除 |
| `replace(first, last, old, new_val)` / `replace_if` | 替换匹配元素 |

⚠️ **`remove/unique` 不实际删除元素，必须配合 `erase`**
> ```cpp
> // ❌ 这样做是错误的，元素还在容器中，只是位置变了
> remove(v.begin(), v.end(), 5);
>
> // ✅ 正确写法：erase-remove 惯用法
> v.erase(remove(v.begin(), v.end(), 5), v.end());
> ```

**6. 线性查找与计数**

| 函数 | 作用 |
|---|---|
| `find(first, last, val)` | 线性找第一个 `== val` 的元素（`vector` 用这个，O(n)） |
| `find_if(first, last, pred)` / `find_if_not` | 条件查找 |
| `count(first, last, val)` / `count_if(first, last, pred)` | 统计满足条件的元素个数 |
| `search(hay, hay_end, needle, needle_end)` | 在大序列中找子序列首次出现位置 |
| `mismatch(f1, l1, f2)` | 找两个序列第一个不同的位置，返回 pair<it1,it2> |

```cpp
if (find(v.begin(),v.end(), x) != v.end()) { /* 找到x */ }
int even_cnt = count_if(v.begin(), v.end(), [](int x){return x%2==0;});
```

<a id="btn-3"></a>

### <algorithm> 常用算法（三）：最值、比较、条件检测、遍历

**7. 最值**

| 函数 | 作用 |
|---|---|
| `max(a, b)` / `min(a, b)` | 两数最值；C++11起支持初始化列表：`max({a, b, c})` |
| `max_element(first, last)` / `min_element(first, last)` | 区间最大/最小值的迭代器 |
| `minmax(a, b)` / `minmax_element(first, last)` | **一次遍历**同时求最大+最小，返回 pair |

```cpp
auto [mn, mx] = minmax({a, b, c, d});           // C++17 结构化绑定
auto [it_mn, it_mx] = minmax_element(v.begin(), v.end());
```

**8. 条件检测**

| 函数 | 作用 |
|---|---|
| `all_of(first, last, pred)` | 区间内**全部**满足 pred？返回 bool |
| `any_of(first, last, pred)` | **至少一个**满足？ |
| `none_of(first, last, pred)` | **没有一个**满足？ |
| `is_sorted(first, last)` / `is_sorted_until(first, last)` | 检测区间是否有序 / 返回第一个破坏有序的迭代器（调试/剪枝用） |

```cpp
if (all_of(v.begin(), v.end(), [](int x){return x>0;})) cout << "全正\n";
cout << is_sorted(v.begin(), v.end()); // 调试时快速打印
```

**9. 遍历与其他**

| 函数 | 作用 |
|---|---|
| `for_each(first, last, func)` | 对区间每个元素执行 func（lambda 方便） |
| `lexicographical_compare(f1,l1,f2,l2)` | 字典序比较两个序列 |
| `next_permutation(first, last)` / `prev_permutation` | 生成下一个/上一个字典序排列，配合 do-while：`do{...}while(next_permutation(...))` |

<a id="btn-4"></a>

### 位运算 __builtin 函数（GCC 特有）

以下函数由 GCC 直接编译为**单条 CPU 指令**，速度碾压手写循环。

⚠️ **仅限 GCC（几乎所有 OJ 都支持），MSVC 不支持。**

| 函数 | 作用（对 unsigned int / int） | ll 版（64位） |
|---|---|---|
| `__builtin_popcount(x)` | 返回二进制中 **1 的个数**（popcount） | `__builtin_popcountll(x)` |
| `__builtin_ctz(x)` | 返回二进制**末尾连续 0 的个数**（Count Trailing Zeros） | `__builtin_ctzll(x)` |
| `__builtin_clz(x)` | 返回二进制**前导 0 的个数**（Count Leading Zeros） | `__builtin_clzll(x)` |
| `__builtin_ffs(x)` | 从低位开始，第一个 1 的位置（**从1开始计数**；x=0 时返回 0） | — |
| `__builtin_parity(x)` | 二进制1的个数的奇偶性（奇=1，偶=0） | — |
| `__lg(x)` | 直接返回 `floor(log2(x))`；等价于 `31 - __builtin_clz(x)` | — |

```cpp
int popcnt = __builtin_popcount(mask);     // 状压DP统计1的个数，O(1)
int lowbit_idx = __builtin_ctz(x & -x);    // lowbit 的末尾0个数（实现树状数组）
int logn = __lg(n);                        // 倍增LCA、ST表预处理 log2(n)
int bit_pos = 31 - __builtin_clz(x);       // x 的最高有效位位置
```

⚠️ **`__builtin_ctz(0)` 与 `__builtin_clz(0)` 是未定义行为！**
> 在 x 可能为 0 的场景下，一定要先判断 `if (x != 0)`，轻则答案错误，重则程序崩溃。

💡 **C++20 标准 <bit> 头文件（如竞赛支持C++20，优先使用更安全）**

| C++20 函数 | 等价 __builtin | 作用 |
|---|---|---|
| `std::popcount(x)` | `__builtin_popcount` | 1的个数 |
| `std::countr_zero(x)` | `__builtin_ctz` | 末尾0 |
| `std::countl_zero(x)` | `__builtin_clz` | 前导0 |
| `std::has_single_bit(x)` | `popcount==1` | 判断x是否为2的幂 |
| `std::bit_width(x)` | `31 - clz + 1` | 最少二进制位数 |
| `std::bit_ceil(x)` / `std::bit_floor(x)` | — | 不小于/不大于x的最小/最大2的幂 |
| `std::rotl(x, s)` / `std::rotr(x, s)` | — | 循环左移/右移 |

<a id="btn-5"></a>

### 更多 __builtin 函数（溢出检测、字节交换、分支预测、预取）

**10. 整数溢出检测（竞赛中极实用，防爆 long long）**

| 函数 | 作用 | 返回值 |
|---|---|---|
| `__builtin_add_overflow(a, b, &res)` | 计算 a+b，若溢出则写入结果并返回 true | bool（true=溢出） |
| `__builtin_sub_overflow(a, b, &res)` | 计算 a-b，同上 | bool |
| `__builtin_mul_overflow(a, b, &res)` | 计算 a×b，同上 | bool |

```cpp
long long res;
if (__builtin_mul_overflow(a, b, &res)) {
    // a*b 溢出了 long long，改用 __int128 或高精度
}
```

**11. 其他 __builtin**

| 函数 | 作用 |
|---|---|
| `__builtin_bswap32(x)` / `__builtin_bswap64(x)` | 32/64位整数的字节序交换（大小端翻转） |
| `__builtin_prefetch(&arr[i])` | 提示 CPU 提前把 `arr[i]` 预取到缓存，超大数组顺序访问可提速（一般不用手动写） |
| `__builtin_constant_p(x)` | 判断 x 是否为编译期常量，写模板时用 |

**12. 分支预测优化**

```cpp
if (__builtin_expect(x > 0, 1)) { ... }   // 告诉编译器：x>0 大概率成立，优化跳转预测
// GCC 13+ 简化写法：
if (__builtin_likely(x > 0)) { ... }      // 大概率成立的分支
if (__builtin_unlikely(x < 0)) { ... }     // 小概率分支（如异常路径、边界情况）
```

> 写对可快 10%~30%，写反了反而更慢，仅用于分支明显不平衡的热路径（例如 99% 走 if 分支、1% 走 else）。竞赛卡最后时限时可以"乱蒙"：if 内部是正常情况就 likely，异常处理就 unlikely。

<a id="btn-6"></a>

### <numeric> 数值辅助（gcd/lcm/iota/accumulate/…）

`<numeric>` 头文件提供数值运算的工具。C++17 起 `gcd`、`lcm` 进入标准。

| 函数 | 作用 |
|---|---|
| `std::gcd(a, b)`（C++17） | 直接求最大公约数 |
| `std::lcm(a, b)`（C++17） | 直接求最小公倍数 |
| `__gcd(a, b)`（GCC旧版） | GCC 非标准 gcd，老 OJ 上可能只有这个 |
| `std::iota(first, last, val)` | 从 val 开始，依次 val, val+1, val+2, … 填充区间。**并查集 parent 数组一键初始化神器** |
| `std::accumulate(first, last, init)` | 区间求和（init 是初值） |
| `std::reduce(first, last, init)`（C++17） | 并行友好的求和，不保证顺序但通常更快 |
| `std::partial_sum(first, last, result)` | 求前缀和（result[0]=a[0], result[1]=a[0]+a[1], …） |
| `std::adjacent_difference(first, last, result)` | 求差分数组（result[0]=a[0], result[i]=a[i]-a[i-1]） |
| `std::inner_product(f1,l1,f2,init)` | 计算内积 / 加权和 init + Σ(aᵢ·bᵢ) |
| `std::transform_reduce(...)`（C++17） | 通用变换+归约 |

```cpp
// 并查集初始化：
int fa[MAXN];
iota(fa, fa+MAXN, 0);   // fa[0]=0, fa[1]=1, ..., fa[MAXN-1]=MAXN-1，一行搞定

// 前缀和（第i位存 1..i 的和）：
vector<int> pre(n+1, 0);
partial_sum(a.begin(), a.end(), pre.begin()+1);

long long sum = accumulate(v.begin(), v.end(), 0LL);  // 注意初值类型 0LL
```

<a id="btn-7"></a>

### <cmath> 数学函数补充

| 函数 | 作用 |
|---|---|
| `hypot(dx, dy)` | 计算欧几里得距离 √(dx²+dy²)，精度高但略慢于手写 |
| `cbrt(x)`（C++11） | 立方根 ∛x |
| `ceil(x) / floor(x) / round(x) / trunc(x)` | 上取整 / 下取整 / 四舍五入 / 向零取整 |
| `fmod(x, y)` | 浮点取模（整数的 % 不能直接用于浮点） |
| `copysign(x, y)` | 把 y 的符号复制给 x，如 copysign(3.0, -2.0) = -3.0 |
| `lerp(a, b, t)`（C++20） | 线性插值 a + t·(b-a)，精度更高 |
| `bit_cast<T>(x)`（C++20） | 安全类型双关，比 memcpy 优雅，例如 float↔uint32_t 看二进制表示 |
| `frexp(x, &exp)` / `ldexp(x, exp)` | 分解/合成浮点数（尾数 × 2^exp） |

⚠️ **`ceil/floor` 返回 double，想转 int 时记得强转**，或者直接手写。

<a id="btn-8"></a>

### <cctype> / <cstring> 字符与C字符串

**字符分类（快读模板必备）**

| 函数 | 判断 | 对应反向 |
|---|---|---|
| `isdigit(c)` | c 是否为数字字符 '0'~'9' | — |
| `isalpha(c)` | 是否为字母 | — |
| `isalnum(c)` | 数字或字母 | — |
| `islower(c)` / `isupper(c)` | 小写 / 大写字母 | — |
| `isspace(c)` | 空白字符（空格/\t/\n/\r/...） | — |
| `tolower(c)` / `toupper(c)` | 大小写转换 | — |

```cpp
inline int read() {  // 经典快读模板
    int x = 0, f = 1;
    char c = getchar();
    while (!isdigit(c)) { if (c=='-') f = -1; c = getchar(); }
    while (isdigit(c)) { x = x*10 + (c-'0'); c = getchar(); }
    return x * f;
}
```

**C 字符串查找（有时比 string::find 更快一点）**

| 函数 | 作用 |
|---|---|
| `strstr(s1, s2)` | 在 s1 中找 s2 首次出现位置（子串查找） |
| `strchr(s, c)` | 字符 c 在 s 中首次出现位置 |
| `strrchr(s, c)` | 字符 c 在 s 中最后一次出现位置 |

<a id="btn-9"></a>

### 数据结构与容器补充（bitset/array/stack/queue等）

**1. bitset<N> —— 固定大小位集（状压DP / 布尔集合加速神器）**

```cpp
bitset<100005> bs;       // 10万个bit仅约 12.5KB（比bool数组节省8倍空间）
bs.set(5);               // 第5位置1
bs.reset(5);             // 第5位置0
bs.flip();               // 全部取反
bs.count();              // O(N / 64) 统计1的个数
int first_one = bs._Find_first();    // GCC扩展：找第一个1的位置
int next_one  = bs._Find_next(10);   // GCC扩展：找位置10之后第一个1
bs &= other; bs |= other; bs ^= other; // 按位与/或/异或，整体 O(N/64)
```

⚠️ **`bitset<N>` 的 N 必须是编译期常量**，不能用变量！如果需要运行时动态大小，改用 `vector<bool>`（动态位集，省内存但随机访问略慢）。

⚠️ **`_Find_first/_Find_next` 是 GCC 扩展**，非标准但所有竞赛 OJ 都能用。

**2. 其他容器速查**

| 容器 | 常用操作 / 适用场景 | 复杂度 |
|---|---|---|
| `array<T, N>` | 固定大小安全数组，支持 `.size()`、`.fill()`、迭代器；**不会退化为指针** | 随机访问 O(1) |
| `deque<T>` | 双端队列，支持 O(1) 前后插入删除；`01BFS` 的标准容器 | 随机访问 O(1)（但实际比vector慢） |
| `stack<T>` | 栈，适配器（基于deque）；`push/pop/top` 均 O(1) | O(1) |
| `queue<T>` | 队列，适配器（基于deque）；BFS 标准写法 | O(1) |
| `priority_queue<T>` | 优先队列，**默认大根堆** | 插入/堆顶 O(log n) |

⚠️ **`priority_queue` 默认大根堆，要小根堆必须写 `greater<T>`**
> ```cpp
> priority_queue<int> pq;                            // ❗ 大根堆（最大的在最上面）
> priority_queue<int, vector<int>, greater<int>> pq; // ✅ 小根堆，Dijkstra 必备
> // pair<int,int> 小根堆：
> priority_queue<pair<int,int>, vector<pair<int,int>>, greater<pair<int,int>>> pq;
> ```

**3. set / map 成员函数 lower_bound 再提醒**
> ❌ `lower_bound(s.begin(), s.end(), val)` —— 退化为 O(n)，会 TLE
> ✅ `s.lower_bound(val)` —— 成员函数，O(log n)

<a id="btn-10"></a>

### <tuple> & <utility>：元组、pair、结构化绑定

**pair（竞赛最常用）**
```cpp
pair<int,int> p = {3, 4};
auto p2 = make_pair(3, "abc");
// pair 默认排序规则：先比 first，first 相同再比 second（升序）
vector<pair<int,int>> v; sort(v.begin(), v.end()); // 免费排序
```

**tuple（任意数量元素的组合）**
```cpp
tuple<int,string,double> t = {1, "hi", 3.14};
int    a = get<0>(t);
string b = get<1>(t);
// tie 解包到已有变量：
int x; string y; double z;
tie(x, y, z) = t;
// tuple_cat 拼接两个 tuple：
auto t3 = tuple_cat(t, make_tuple(true, 100));
```

**C++17 结构化绑定（解包神器，强烈推荐）**
```cpp
auto [a, b] = make_pair(3, 4);               // pair 解包
auto [x, y, z] = make_tuple(1, 2, 3);        // tuple 解包
// map / unordered_map 遍历（极大提升可读性）：
for (auto& [key, value] : mp) {
    cout << key << " -> " << value << '\n';
}
// minmax_element 返回的 pair<迭代器, 迭代器> 直接解包：
auto [it_mn, it_mx] = minmax_element(v.begin(), v.end());
```

<a id="btn-11"></a>

### 字符串辅助（to_string/stoi/stoll/类型转换）

| 函数 | 作用 |
|---|---|
| `to_string(val)` | 数字 → 字符串（支持 int / long long / double 等） |
| `stoi(s)` | 字符串 → int |
| `stoll(s)` | 字符串 → long long |
| `stod(s)` | 字符串 → double |
| `string s = to_string(12345);` | `"12345"` |
| `long long x = stoll("123456789012");` | 123456789012LL |

⚠️ **`stoi/stoll/stod` 遇到无法转换的字符串会抛出异常**，竞赛中如果输入不可信可能 RE，建议自定义安全的转换函数或用 sscanf。

<a id="btn-12"></a>

### 高精度辅助：__int128

`__int128` 是 GCC 扩展的**128位有符号整数**，支持 `unsigned __int128`。

| 类型 | 范围 | 字节 |
|---|---|---|
| `__int128` | 约 ±1.7×10³⁸ | 16 |
| `unsigned __int128` | 约 0 ~ 3.4×10³⁸ | 16 |

**适用场景：** `long long`（9×10¹⁸）不够用但 `__int128` 够得到的情况，例如：
- 乘法中间结果防溢出（模乘、两 long long 相乘后再取模）
- 高精度乘法的内层运算（用 __int128 暂存，减少取模次数）

⚠️ **__int128 不能直接用 cin / cout / scanf / printf 读写**
> 只能作为**中间计算**变量使用，输入输出需要手动写快读/快写模板，或者先强转 `long long`（如果值在范围内）。
> ```cpp
> // 模乘避免溢出的 __int128 写法：
> inline long long mul_mod(long long a, long long b, long long p) {
>     return (__int128)a * b % p;   // 编译成单条 mul+div，飞快
> }
> ```

<a id="btn-13"></a>

### 快速 IO 极致优化（解除同步/fread/fwrite/getchar_unlocked）

**基础版（99% 的题够用）**
```cpp
ios::sync_with_stdio(false);   // 解除 C/C++ IO 绑定，cin/cout 提速 3~10 倍
cin.tie(nullptr);              // 解除 cin 和 cout 的绑定
cout.tie(nullptr);
```

⚠️ **以上三行启用后，绝对不能混用 scanf/printf 和 cin/cout！** 否则会出现顺序错乱、读入失败等灵异问题。

**进阶版 fread + fwrite（输入输出 ≥ 1e6 时再上）**
```cpp
// fread 整块读（比 getchar 再快 2~5 倍）
char ibuf[1<<20], *ip1=ibuf, *ip2=ibuf;
#define gc() (ip1==ip2 && (ip2=(ip1=ibuf)+fread(ibuf,1,1<<20,stdin),ip1==ip2)?EOF:*ip1++)

// fwrite 整块写
char obuf[1<<20], *op=obuf;
#define pc(c) (op-obuf<(1<<20)?*op++=c:(fwrite(obuf,1,op-obuf,stdout),op=obuf,*op++=c))
// 程序结束前：fwrite(obuf, 1, op-obuf, stdout);   // 必须！否则缓冲区内容不输出
```

**getchar_unlocked()（Linux/GCC 下极快，POSIX 线程不安全但竞赛无妨）**
```cpp
inline int read() {
    int x=0, f=1; char c=getchar_unlocked();
    while (c<'0'||c>'9') { if(c=='-') f=-1; c=getchar_unlocked(); }
    while (c>='0'&&c<='9') { x=x*10+c-'0'; c=getchar_unlocked(); }
    return x*f;
}
```

<a id="btn-14"></a>

### 随机化与对拍（mt19937/distribution/计时）

**高质量随机数（推荐 mt19937，取代低质量的 rand()）**
```cpp
#include <chrono>
#include <random>
mt19937 rng(chrono::steady_clock::now().time_since_epoch().count());
// 均匀分布：
uniform_int_distribution<int> dist(1, 1000000);   // [1, 1e6] 整数均匀
uniform_real_distribution<double> rdist(0.0, 1.0); // [0,1) 浮点均匀
int x = dist(rng);
double y = rdist(rng);
```

⚠️ **`rand()` + `srand(time(0))` 随机数质量低、周期短、分布不均匀**，对拍/模拟退火/随机化算法请用 mt19937。

**计时（卡常 / 对拍速度比较）**
```cpp
// 方法一：clock()（CPU 时间，单位 clocks；CLOCKS_PER_SEC ≈ 1e6）
clock_t t0 = clock();
solve();
cerr << 1.0 * (clock() - t0) / CLOCKS_PER_SEC << "s\n";

// 方法二：chrono 高精度（墙钟时间）
auto t1 = chrono::high_resolution_clock::now();
solve();
auto t2 = chrono::high_resolution_clock::now();
cerr << chrono::duration<double>(t2-t1).count() << "s\n";
```

<a id="btn-15"></a>

### 内存与初始化辅助（memset/memcpy/fill 等）

| 函数 / 写法 | 作用 | 适用范围 / 注意 |
|---|---|---|
| `memset(arr, byte, sizeof(arr))` | **按字节**赋值整块内存 | 只能可靠写 **0** 或 **-1**（因为每字节相同）；写 1 就错！int 各字节全1 才等于 -1 |
| `memcpy(dest, src, bytes)` | 快速内存拷贝（整块复制数组） | 比 for 循环快得多，但不处理重叠区域 |
| `memmove(dest, src, bytes)` | 允许 src 和 dest 重叠的安全 memcpy | 重叠区域必须用它而非 memcpy |
| `fill(first, last, val)` / `fill_n(first, n, val)` | 按**元素**赋值，任何类型/值都正确 | DP数组初始化为 INF/−INF 请用这个而非 memset！ |
| `copy(first, last, out)` / `copy_if` | 按元素复制/条件复制 | 类型安全，比 memcpy 更通用 |

⚠️ **`memset(a, 1, sizeof(a))` 是经典错题！**
> memset 是按字节写的。对于 32 位 int 写 value=1，每个字节=0x01，最终 int = 0x01010101 = 16843009，**不是 1**！
> memset 只能：
> ```cpp
> memset(a, 0, sizeof a);      // ✅ 全置 0
> memset(a, -1, sizeof a);     // ✅ 全置 -1（每字节 0xFF）
> // 其他值请用 fill：
> fill(a, a+n, INF);           // ✅ 正确写任意值
> ```

**其他小工具**

| 函数 | 作用 |
|---|---|
| `std::swap(a, b)` | 交换两变量，不用手写 tmp |
| `std::exchange(x, newval)`（C++14） | 一步完成 `tmp=x; x=newval; return tmp;`（链表/状态转移好用） |
| `std::clamp(x, lo, hi)`（C++17） | 把 x 限制在 [lo, hi] 区间内，溢出就截断 |
| `std::midpoint(a, b)`（C++20） | 安全计算中点 `(a+b)/2`，永不溢出 |

<a id="btn-16"></a>

### GCC 优化咒语（#pragma / __attribute__ / likely/unlikely）

⚠️ **在部分正规赛事（如 NOI 系列）中，此类非标准指令可能被禁用。** 提交前务必确认。

**编译期开启最高级优化**（放在代码最开头即可）
```cpp
#pragma GCC optimize("O3,unroll-loops")
#pragma GCC target("avx2,bmi,bmi2,lzcnt,popcnt")   // 启用特定 CPU 指令集
#pragma GCC target("popcnt")   // 单独启用 popcnt，__builtin_popcount 再加速
```

**函数级属性（直接写在函数声明前面）**
```cpp
__attribute__((always_inline)) inline int lowbit(int x) { return x & -x; }
__attribute__((noinline)) void big_function(...) { ... }  // 禁止内联（避免二进制膨胀）
__attribute__((optimize("O3,unroll-loops"))) void solve() { ... }
__attribute__((packed)) struct Node { ... };   // 结构体紧凑打包，不留 padding（省内存）
```

**不可达代码提示（帮助编译器优化）**
```cpp
// C++23：std::unreachable();
// GCC 通用：
switch (x) {
  case 1: ...; break;
  case 2: ...; break;
  default: __builtin_unreachable();  // 告诉编译器：x 不可能到这里，编译器激进优化
}
```

⚠️ **`__builtin_unreachable()` 执行到真会触发未定义行为**，只能用在你100%确定不会走到的分支。

---

<a id="complexity"></a>

## 算法复杂度与数据范围速查表

> 💡 快速参考：在 1 秒时限、C++ 普通实现（无卡常）下，各算法可通过的推荐数据范围。
> 上限并非硬标准，仅供预估。实际应结合常数、多组数据总规模灵活调整。

| 算法 / 数据结构 | 时间复杂度 | 推荐 n ≤ | 备注 |
|---|---|---|---|
| 快速幂（模乘） | $O(\log b)$ | $b \leq 10^{18}$ | 乘法注意溢出 long long，必要用 __int128 |
| 二分答案 | $O(N \log X)$ | $N \leq 2 \times 10^5$ | check函数跑 $O(N)$ 时 |
| 三分法（浮点数） | $O(\log \frac{r-l}{EPS})$ | 迭代 50~100 次 | EPS 选 $10^{-10}$ 够用 |
| 排序（sort） | $O(n \log n)$ | $n \leq 5 \times 10^5$ | sort 约 5e6/s |
| 离散化 | $O(n \log n)$ | $n \leq 2 \times 10^5$ | sort + unique + lower_bound |
| 前缀和 / 差分 | $O(n) / O(1)$ 修改 | $n \leq 2 \times 10^6$ | 二维注意溢出 int |
| 树上差分 | $O(n)$ DFS + 回溯 | $n \leq 5 \times 10^5$ | 边差分 vs 点差分公式别混 |
| 贪心 / 反悔贪心 | $O(n \log n)$（堆） | $n \leq 3 \times 10^5$ | 反悔贪心常配合 multiset |
| DFS / BFS（搜索） | $O(V+E)$ | $V \leq 2 \times 10^6$ | BFS 爆栈时改手写队列 |
| 滑动窗口 / 双指针 | $O(n)$ | $n \leq 2 \times 10^6$ | 窗口指针不能回退 |
| ST表（稀疏表） | 预处理 $O(n \log n)$，查询 $O(1)$ | $n \leq 2 \times 10^5$ | log表打 int 即可 |
| 位运算枚举子集 | $O(3^n)$ / $O(2^n \times n)$ | $n \leq 20$（3^20≈3.5e9太大，n≤16较稳） | 子集卷积 n≤20 靠 FWT |
| CDQ分治 | $O(n \log^2 n)$ | $n \leq 10^5$ | 常数略大，1e5稳过 |
| 整体二分 | $O((n+Q) \log N \log n)$ | $n, Q \leq 10^5$ | 带修第k小再乘一个 log |
| Meet-in-the-Middle | $O(2^{n/2} \cdot n/2)$ | $n \leq 40$（2^20≈1e6，2^25≈3350万要卡） | n=42~44 需极限卡常 |
| 模拟退火 / 爬山 | 自定义迭代 | — | 玄学调参：T0、降温系数 |
| 单调栈 | $O(n)$ | $n \leq 3 \times 10^6$ | 左右边界等号只加一边防重复 |
| 01BFS | $O(n+m)$ | $n, m \leq 2 \times 10^6$ | 边权只能 {0, C} |
| 线性DP（一维/二维） | $O(n)$ / $O(n^2)$ | 1D: $\leq 10^6$，2D: $\leq 5000 \times 5000$ | 二维压滚动数组 |
| 背包DP（01/完全/多重） | $O(nm)$ / $O(nm \log m)$ | $nm \leq 10^7$（01/完全），$\leq 2 \times 10^6$（多重单调队列优化） | 滚动数组只开 w |
| 树形DP | $O(n)$（普通）/ $O(n^2)$（换根/子树合并） | O(n) ≤ 2e5，O(n²) ≤ 2000~3000 | 二次扫描换根 $O(n)$ |
| 单调队列优化DP | $O(n)$ | $n \leq 5 \times 10^5$ | 要注意单调增/减的方向 |
| 状压DP | $O(2^n \cdot n)$（朴素）/ $O(2^n \cdot n^2)$（复杂） | $n \leq 20$ / $n \leq 15$ | 子集卷积用FWT |
| 记忆化搜索 | 等价对应DP | 同DP对应 | 记得初始化为 -1 而非 0 |
| 数位DP | $O(10 \cdot len \cdot state)$ | len ≤ 20（十进制），state ≤ 20种 | 注意前导零 lead 标记 |
| 区间DP | $O(n^3)$（基础）/ $O(n^2)$（四边形优化） | n ≤ 500 / n ≤ 3000 | 四边形不等式满足决策单调 |
| 斜率优化DP | $O(n)$（单调队列）/ $O(n \log n)$（凸包+二分） | n ≤ 3e5 / n ≤ 2e5 | 斜率严格递增用单调队列，否则用二分/李超树 |
| 概率 / 期望DP | $O(状态数)$ | 线性 ≤ 1e6，二维 ≤ 2000×2000 | 有环用高斯消元或 E[u]=A·E[fa]+B 待定系数 |
| Dijkstra（堆优化） | $O(m \log n)$ | $n, m \leq 5 \times 10^5$ | 负权不能用 |
| Bellman-Ford / SPFA | $O(nm)$（最坏）/ 约 $O(2m)$（随机） | m ≤ 2e5（随机图），判负环必用SPFA | 卡网格图会T，改用01BFS |
| Floyd 全源最短路 | $O(n^3)$ | $n \leq 400$（512³ 勉强，300较稳） | 数组开 MAXN×MAXN，注意内存 |
| 最小环（Floyd版） | $O(n^3)$ | $n \leq 400$ | 检查 ans ≥ INF 输出 No solution |
| Kruskal 最小生成树 | $O(m \log m)$ | $m \leq 5 \times 10^5$ | 排序为瓶颈 |
| Prim 最小生成树 | 朴素 $O(n^2)$，堆 $O(m \log n)$ | 稠密图 n ≤ 3000 用朴素版 | 考试 Kruskal 一般更通用 |
| Kruskal 重构树 | $O(m \log m + n \log n)$（建+LCA） | $n, m \leq 2 \times 10^5$ | 节点数=2n-1，数组翻倍 |
| 二分图最大匹配（匈牙利） | $O(nm)$ / Dinic $O(m\sqrt{n})$ | n, m ≤ 2e5 推荐 Dinic 建模 | 匈牙利写简单但慢 |
| KM算法（完美匹配） | $O(n^3)$（BFS版） | n ≤ 500（300很稳，500略卡） | 稠密图才用KM，稀疏图改费用流 |
| 网络流 Dinic | $O(n^2 m)$（理论）/ 约 $O(m \sqrt{n})$（单位网络） | 比赛经验：点数 n≤300, 边数 m≤1e4 **几乎稳过**；更大规模看建图技巧 | 加当前弧优化 + 分层bfs，开4倍边数组 |
| 费用流（SSP/Primal-Dual） | 约 $O(F \cdot m \log n)$，F为流量 | F ≤ 5000, m ≤ 2e4 常用；大规模需zkw等优化 | 负权边先消势再跑Dijkstra |
| 差分约束 | $O(nm)$ SPFA / 约 $O(m)$ 实际 | n ≤ 5000, m ≤ 1e4（SPFA有负环时慢） | 不等式方向别写反，先想最长/最短 |
| 2-SAT | $O(n+m)$ SCC缩点 | 布尔变量数 ≤ 2e5 | 每个约束连两条边（含逆否） |
| LCA（倍增） | 预处理 $O(n \log n)$，查询 $O(\log n)$ | $n \leq 5 \times 10^5$，查询 ≤ 5e5 | LOG=20 够 n=1e6 |
| 树的直径 / 重心 | $O(n)$ | $n \leq 2 \times 10^6$ | 两遍DFS直径不能有负权 |
| 拓扑排序 | $O(n+m)$ | $n, m \leq 2 \times 10^6$ | 入度为零入队，输出长度≠n即有环 |
| SCC（Tarjan） | $O(n+m)$ | $n, m \leq 2 \times 10^6$ | 缩点后反向拓扑即可DAG DP |
| 割点 / 割边 / 双连通分量 | $O(n+m)$ | $n, m \leq 5 \times 10^5$ | 桥判定：low[v] > dfn[u]（无等号） |
| 欧拉路径 / 回路 | $O(n+m)$ Hierholzer | $m \leq 2 \times 10^6$ | 无向图路径恰好两个奇度点，回路全偶 |
| DSU on Tree | $O(n \log n)$ | $n \leq 5 \times 10^5$ | 全局变量 add/remove 对称写对 |
| 启发式合并（并查集/集合） | $O(\log n)$ 每次均摊 | 总操作 ≤ 2e5 | 永远把小数组合并到大集合里 |
| GCD / exGCD | $O(\log \min(a,b))$ | a,b ≤ 1e18 随便 | 数据类型用 long long |
| 线性逆元递推 | $O(p)$（p为模数） | 模数为质数，p ≤ 2e6 | 递推式 `inv[i] = (mod - mod/i) * inv[mod%i] % mod` |
| 埃氏筛 | $O(n \log \log n)$ | n ≤ 1e7（1秒约800万） | 判质数够，求积性函数用欧拉筛 |
| 欧拉筛（线性筛） | $O(n)$ | n ≤ 2e7（略紧），n≤1e7稳 | 积性函数 φ、μ、d、σ 都能线性求 |
| 组合数（预处理阶乘+逆元） | 预处理 $O(n)$，查询 $O(1)$ | n ≤ 2e6（模数为素数） | n≥模数用 Lucas 定理 |
| 卡特兰数 | $O(\log mod)$ 单次 | n ≤ 1e6（直接公式） | 除法要乘逆元 |
| 斯特林数 | 递推 $O(nm)$，NTT卷积 $O(n \log n)$ | 递推版 n,m ≤ 5000；卷积 n ≤ 2e5 | 容斥公式也可用 |
| 莫比乌斯反演 | $O(n \log n)$ 预处理 + 整除分块 | n ≤ 1e6（线筛μ），查询 ≤ 数论分块 √n | 交换求和顺序技巧 |
| 整除分块 | $O(\sqrt{n})$ | n ≤ 1e18（只要lld范围） | 区间 [l, n/(n/l)] 同值一段 |
| 欧拉函数（单点） | $O(\sqrt{n})$ 试除 | n ≤ 1e12 | 分解质因子套公式即可 |
| 扩展欧拉定理降幂 | $O(|b|)$ 字符串 + $O(\sqrt{m})$ | b长度 ≤ 1e6 | b ≥ φ(m) 才加 φ(m) |
| 矩阵快速幂 | $O(k^3 \log n)$（k为矩阵阶） | k ≤ 100（100³=1e6 × log ≈ OK） | 线性递推专用 BM+线性递推更快 |
| CRT / exCRT | $O(n \log n)$（n条同余式） | n ≤ 1e5（大数取模也能跑） | exCRT 合并时注意溢出用__int128 |
| 高斯消元（浮点） | $O(n^3)$ | n ≤ 500（double，400稳） | 选主元减小误差 |
| 高斯消元（模素数） | $O(n^3)$ | n ≤ 500 | 求逆元代替除法 |
| 行列式 / 矩阵求逆 | $O(n^3)$ | n ≤ 500（行列式），n ≤ 300（求逆） | 求逆增广矩阵高斯约旦消元 |
| 异或线性基 | 插入 / 查询 $O(\log V)$（V为值域） | V ≤ 2^60，n ≤ 1e5 | 异或最值 / 第k小异或 / 线性无关 |
| FFT（double） | $O(n \log n)$ | n ≤ 2e5（多项式乘法标准） | 值域大且对精度敏感改用NTT |
| NTT | $O(n \log n)$ | n ≤ 1e6 | 模数要NTT友好（如 998244353, g=3） |
| FWT | $O(n \log n)$ | n ≤ 2^20（约1e6） | AND/OR/XOR三种位运算卷积 |
| SG函数 / 博弈论 | 求每个局面SG: $O(局面 × 转移)$ | 小局面 ≤ 1e4；nim异或结论可直接用大n | 每堆 SG 值异或非零即先手胜 |
| 并查集 / 带权并查集 | 均摊 $O(\alpha(n)) ≈ O(1)$ | n ≤ 2e6 | 路径压缩 + 按秩合并 |
| 树状数组 | 修改/查询 $O(\log n)$ | n ≤ 2e6 | 不能做区间最大值（线段树/STL做） |
| 线段树 | 修改/查询 $O(\log n)$ | n ≤ 5e5（单点/区间lazy） | 开4倍空间，注意push_down / push_up |
| 二维前缀和 / 二维ST表 | 预处理 $O(nm)$，查询 $O(1)$ | 二维 n,m ≤ 1000；二维ST ≤ 500×500 | 超范围换二维线段树（常数大） |
| FHQ Treap（平衡树） | 插入/删除 $O(\log n)$ | n ≤ 2e5 | split/merge 写对无旋即可 |
| Trie / 01Trie | $O(n × 字符串长度)$ / $O(n × 32)$ | n ≤ 2e5（总字符 ≤ 5e6） | 字符串Trie内存按字符数×26开 |
| 主席树（可持久化线段树） | 修改/查询 $O(\log n)$ | n ≤ 2e5，内存开 20~40 × n | 离散化 + 按版本新建节点 |
| 对顶堆（维护中位数） | push/pop $O(\log n)$ | n ≤ 2e5 | 两堆 size 相差≤1 |
| KMP 匹配 | $O(n+m)$ | 文本串n ≤ 2e6，模式串m ≤ 2e6 | 失配函数 next 数组别用 next 当变量名（和std冲突） |
| Manacher | $O(n)$ | n ≤ 1e6 | 插入分隔符后的字符串长度=2n+1 |
| 最小表示法 | $O(n)$ | n ≤ 1e6 | 双指针比较循环同构最小串 |
| AC自动机 | 文本长度+Σ模式串长度 | 总字符 ≤ 2e6 | 最后fail树DP求子串出现次数 |
| 后缀自动机（SAM） | $O(n)$ 构造，状态数 ≤ 2n | n ≤ 2e5 | link/len/next数组要开 2×n |
| 后缀数组（SA） | 倍增 $O(n \log n)$ | n ≤ 2e5（快版） | Height数组配合ST表求LCP |
| 高精度加 / 减 | $O(len)$ | len ≤ 1e4 轻松 | 数组倒序存；减法借位处理好负数 |
| 高精度乘 / 除 | $O(len_a \cdot len_b)$ / $O(len_a \cdot len_b)$ | len ≤ 2000 普通FFT版；1e4+需NTT加速 | 除法用减法竖式模拟 |
| 平面最近点对 | $O(n \log n)$ 分治 | n ≤ 2e5 | 分治后仅检查相邻的6个点 |
| 向量 / 点线 / 叉积 | $O(1)$ 单操作 | 点数 ≤ 1e5 | 叉积符号判定共线/左右/面积 |
| 线段相交（规范+非规范） | 每次 $O(1)$ | 线段数 ≤ 2000 暴力枚举 | 大规模用扫描线 $O((n+k)\log n)$ |
| 凸包（Andrew） | $O(n \log n)$（排序） | n ≤ 2e5 | 旋转卡壳直径 O(n) 配合 |
| 点在多边形内（射线法） | 每次 $O(n)$ 暴力 | 多边形顶点 ≤ 1e4 暴力；更大用三角剖分+树 | 射线交点奇内偶外 |
| 容斥（mask枚举） | $O(2^n \cdot n)$ | n ≤ 20（2^20=1,048,576 OK） | 2^25 ≈ 3.3e7 卡常极限 |


