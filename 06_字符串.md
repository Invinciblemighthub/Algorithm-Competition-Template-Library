## [返回目录](http://scpc.fun/discussion-detail/1405)

<style>
a[id], h2, h3, h4 { scroll-margin-top: 140px; }
</style>

---

## 本章目录

- [6.1 KMP匹配](#sec-6-1) **必学**
- [6.2 Manacher马拉车](#sec-6-2) **进阶**
- [6.3 最小表示法](#sec-6-3) **选学**
- [6.4 AC自动机](#sec-6-4) **进阶**
- [6.5 后缀自动机](#sec-6-5) **选学**
- [6.6 后缀数组SA](#sec-6-6) **选学**

---

<a id="ch6"></a>

## 六、字符串

本板块涵盖字符串匹配与处理算法，包括KMP、Manacher、最小表示法、AC自动机和后缀自动机。



<a id="sec-6-1"></a>

### 6.1 KMP匹配
- **适用场景**：在文本串中查找模式串的所有出现位置。
- **时间复杂度**：$O(n + m)$
- **核心思想**：预处理失败函数（next/pi数组），利用已匹配信息避免回溯。

```cpp
// KMP模板
vector<int> kmp(const string& text, const string& pattern) {
    vector<int> res;
    int n = text.size(), m = pattern.size();
    if (m == 0) return res;
    
    // 构建pi数组（失败函数）
    vector<int> pi(m, 0);
    for (int i = 1; i < m; i++) {
        int j = pi[i - 1];
        while (j > 0 && pattern[i] != pattern[j]) j = pi[j - 1];
        if (pattern[i] == pattern[j]) j++;
        pi[i] = j;
    }
    
    // 匹配
    int j = 0;
    for (int i = 0; i < n; i++) {
        while (j > 0 && text[i] != pattern[j]) j = pi[j - 1];
        if (text[i] == pattern[j]) j++;
        if (j == m) {
            res.push_back(i - m + 1);  // 匹配位置
            j = pi[j - 1];
        }
    }
    return res;
}
```

**相关题目**：
- [Accepted 自动机](http://scpc.fun/problem/SCPC-1066H)（第127题，KMP匹配+区间DP）

---

<a id="sec-6-2"></a>

### 6.2 Manacher马拉车
- **适用场景**：求字符串中的最长回文子串。
- **时间复杂度**：$O(n)$
- **核心思想**：利用回文的对称性，用已计算的回文半径加速后续计算。

```cpp
// Manacher模板
vector<int> manacher(const string& s) {
    // 预处理：插入特殊字符，如 "abc" -> "^#a#b#c#$"
    string t = "^#";
    for (char c : s) { t += c; t += '#'; }
    t += '$';
    
    int n = t.size();
    vector<int> p(n, 0);  // p[i] = 以i为中心的最长回文半径
    int C = 0, R = 0;  // 当前最右回文的中心和右边界
    for (int i = 1; i < n - 1; i++) {
        int mirr = 2 * C - i;  // i关于C的对称点
        if (i < R) p[i] = min(R - i, p[mirr]);
        // 扩展
        while (t[i + p[i] + 1] == t[i - p[i] - 1]) p[i]++;
        // 更新最右回文
        if (i + p[i] > R) {
            C = i;
            R = i + p[i];
        }
    }
    return p;
    // 最长回文子串长度 = max(p[i])
    // 原串中回文中心 = (i - p[i]) / 2
}

// Manacher变体（在压缩后的串上做，处理带价值的回文）
// 先将连续相同字符合并，记录每段长度和字符
// 再在合并后的串上做Manacher
```

**相关题目**：
- [祖玛](http://scpc.fun/problem/SCPC-1165L)（第62题，Manacher在压缩串上求最大价值回文）

---

<a id="sec-6-3"></a>

### 6.3 最小表示法
- **适用场景**：求字符串的循环同构中字典序最小的表示。常用于判断两个字符串是否循环同构。
- **时间复杂度**：$O(n)$

**空间复杂度**：$O(n)$
- **核心思想**：使用两个指针 $i$ 和 $j$ 分别指向不同的起始位置，比较从这两个位置开始的子串。若 $s[i+k] \neq s[j+k]$，则将字典序较大一侧的指针跳过 $k+1$ 个位置。当 $i$ 或 $j$ 超过 $n$ 时结束。

**模板代码**：

```cpp
#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 200005;
char s[MAXN];
int n;

// 最小表示法，返回最小表示的起始位置(0-based)
// 输入：字符串 s 及其长度 n
// 输出：字典序最小的循环同构的起始下标
int minRepresentation(const char *s, int n) {
    int i = 0, j = 1, k = 0;
    while (i < n && j < n && k < n) {
        char a = s[(i + k) % n];
        char b = s[(j + k) % n];
        if (a == b) {
            k++;
        } else {
            // 字典序较大的一侧跳过 k+1 个位置
            if (a > b) i += k + 1;
            else       j += k + 1;
            if (i == j) j++; // 保证两指针不重合
            k = 0;
        }
    }
    return min(i, j);
}

// 判断两个字符串是否循环同构
bool isCyclicEquivalent(const char *a, int na, const char *b, int nb) {
    if (na != nb) return false;
    return minRepresentation(a, na) == minRepresentation(b, nb);
    // 更严谨的做法：分别取最小表示后逐字符比较
}

int main() {
    quick;
    scanf("%s", s);
    n = strlen(s);
    int pos = minRepresentation(s, n);
    // 输出最小表示
    for (int i = 0; i < n; i++) {
        putchar(s[(pos + i) % n]);
    }
    putchar('\n');
    printf("start = %d\n", pos);
    return 0;
}
```

**使用说明**：
- `minRepresentation(s, n)` 返回原串 $s$ 的字典序最小循环同构的起始下标（0-based）。
- 比较两个串是否循环同构时，可分别求出二者最小表示的起始位置，再逐字符比较由该位置开始的长度为 $n$ 的串是否相同。
- 传给函数的串不需要预先倍增拼接，函数内部通过取模访问实现循环。

---

<a id="sec-6-4"></a>

### 6.4 AC自动机
- **适用场景**：多模式串匹配。给定一个文本串和多个模式串，求哪些模式串在文本串中出现及出现次数。
- **时间复杂度**：建树 $O(\sum |pattern|)$，查询 $O(|text| \cdot \frac{|pattern|_{max}}{|\Sigma|})$，均摊接近 $O(|text|)$

**空间复杂度**：$O(\sum |pattern| \cdot |\Sigma|)$
- **核心思想**：将所有模式串插入 Trie 树，然后利用 BFS 构建 fail 指针（类似 KMP 的 next 数组在 Trie 上的扩展）。fail 指针指向当前节点所代表串的最长真后缀对应的 Trie 节点。查询时沿文本串在 Trie 上走，同时沿 fail 链统计匹配。

**模板代码**：

```cpp
#include <cstdio>
#include <cstring>
#include <queue>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 1000010; // 节点总数上限
const int SIGMA = 26;     // 字符集大小

int trie[MAXN][SIGMA]; // Trie 转移
int fail[MAXN];         // fail 指针
int endCnt[MAXN];       // 以该节点为结尾的模式串个数
int tot;                // 节点总数，0 为根

void init() {
    tot = 0;
    memset(trie, 0, sizeof(trie));
    memset(fail, 0, sizeof(fail));
    memset(endCnt, 0, sizeof(endCnt));
}

// 插入一个模式串
void insert(const char *s) {
    int u = 0;
    for (int i = 0; s[i]; i++) {
        int c = s[i] - 'a';
        if (!trie[u][c]) trie[u][c] = ++tot;
        u = trie[u][c];
    }
    endCnt[u]++; // 标记结尾
}

// BFS 构建 fail 指针，同时做路径压缩
void buildFail() {
    queue<int> q;
    for (int c = 0; c < SIGMA; c++) {
        if (trie[0][c]) {
            fail[trie[0][c]] = 0;
            q.push(trie[0][c]);
        }
    }
    while (!q.empty()) {
        int u = q.front(); q.pop();
        for (int c = 0; c < SIGMA; c++) {
            if (trie[u][c]) {
                fail[trie[u][c]] = trie[fail[u]][c];
                q.push(trie[u][c]);
            } else {
                // 路径压缩：不存在该转移则指向 fail 子节点的对应转移
                trie[u][c] = trie[fail[u]][c];
            }
        }
    }
}

// 查询文本串中出现了多少个(种)模式串
// 这里每个模式串只计一次；若需统计总出现次数，去掉 endCnt[j] = -1 的去重逻辑
int query(const char *t) {
    int u = 0, res = 0;
    for (int i = 0; t[i]; i++) {
        int c = t[i] - 'a';
        u = trie[u][c];
        for (int j = u; j && endCnt[j] != -1; j = fail[j]) {
            res += endCnt[j];
            endCnt[j] = -1; // 标记已统计，避免重复计数
        }
    }
    return res;
}

char pat[100];
char txt[1000010];

int main() {
    quick;
    init();
    int m;
    scanf("%d", &m);
    for (int i = 0; i < m; i++) {
        scanf("%s", pat);
        insert(pat);
    }
    buildFail();
    scanf("%s", txt);
    printf("%d\n", query(txt)); // 输出文本串中出现的模式串种数
    return 0;
}
```

**使用说明**：
- 先调用 `init()` 初始化，再对每个模式串调用 `insert` 插入，最后调用 `buildFail()` 构造 fail 指针。
- `query(t)` 返回文本串 $t$ 中出现的模式串**种数**（每个模式串只算一次）。若需要统计每个模式串的**出现总次数**，应保留每个节点对应的模式串编号，并去掉 `endCnt[j] = -1` 的去重逻辑，改为累加。
- 字符集默认为小写字母 `'a'~'z'`，若需扩展为大写或数字，调整 `SIGMA` 与字符映射即可。
- 节点总数上限应不小于所有模式串长度之和。

---

<a id="sec-6-5"></a>

### 6.5 后缀自动机
- **适用场景**：字符串子串相关的各种查询，如不同子串个数、最长公共子串、子串出现次数等。
- **时间复杂度**：构建 $O(n)$，查询根据具体问题

**空间复杂度**：$O(n \cdot |\Sigma|)$
- **核心思想**：后缀自动机 (SAM) 是一个能接受字符串所有后缀的最小 DFA。每个状态代表一组 endpos 等价类（出现位置集合相同的子串）。通过后缀链接 (link) 连接各状态，形成 parent 树。endpos 集合大小可在 parent 树上 DP 求得。

**模板代码**：

```cpp
#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 2000010; // 2 倍串长
const int SIGMA = 26;

struct SAM {
    int len[MAXN];             // 该状态代表的最长子串长度
    int link[MAXN];            // 后缀链接
    int nxt[MAXN][SIGMA];      // 转移(用 nxt 避免与关键字冲突)
    int cnt[MAXN];             // endpos 集合大小(出现次数)
    int tot;                   // 节点总数
    int last;                  // 最后加入的状态

    void init() {
        tot = 1;               // 1 为初始状态(0 不用)
        last = 1;
        memset(nxt, 0, sizeof(nxt));
        memset(link, 0, sizeof(link));
        memset(len, 0, sizeof(len));
        memset(cnt, 0, sizeof(cnt));
    }

    // 逐字符扩展，c 为 0-based 字符编号
    void extend(int c) {
        int cur = ++tot;
        len[cur] = len[last] + 1;
        cnt[cur] = 1;          // 新建的实节点初始 endpos 大小为 1
        int p = last;
        while (p && !nxt[p][c]) {
            nxt[p][c] = cur;
            p = link[p];
        }
        if (!p) {
            link[cur] = 1;     // 链接到初始状态
        } else {
            int q = nxt[p][c];
            if (len[p] + 1 == len[q]) {
                link[cur] = q;
            } else {
                // 分裂节点 q
                int clone = ++tot;
                len[clone] = len[p] + 1;
                memcpy(nxt[clone], nxt[q], sizeof(nxt[q]));
                link[clone] = link[q];
                cnt[clone] = 0; // clone 是虚节点，不对应真实出现位置
                while (p && nxt[p][c] == q) {
                    nxt[p][c] = clone;
                    p = link[p];
                }
                link[q] = link[cur] = clone;
            }
        }
        last = cur;
    }

    // 查询不同子串个数
    // 每个状态 i 贡献 len[i] - len[link[i]] 个不同子串
    long long countDistinct() {
        long long res = 0;
        for (int i = 2; i <= tot; i++) {
            res += len[i] - len[link[i]];
        }
        return res;
    }

    // 按 len 基数排序得到拓扑序，并在 parent 树上 DP 求 endpos 大小
    // n 为原串长度
    void buildCnt(int *order, int *bucket, int n) {
        for (int i = 1; i <= tot; i++) bucket[len[i]]++;
        for (int i = 1; i <= n; i++) bucket[i] += bucket[i - 1];
        for (int i = 1; i <= tot; i++) order[bucket[len[i]]--] = i;
        // 从长到短(叶子到根)累加
        for (int i = tot; i >= 1; i--) {
            cnt[link[order[i]]] += cnt[order[i]];
        }
        // 初始状态 cnt[1] 会被累加为 n，通常不需要使用
    }

    // 查询某个子串的出现次数：在自动机上沿子串走，
    // 走到的状态 u 若存在，则出现次数为 cnt[u]（需先调用 buildCnt）
};

SAM sam;
char s[1000010];
int orderArr[MAXN], bucketArr[MAXN];

int main() {
    quick;
    scanf("%s", s);
    int n = strlen(s);
    sam.init();
    for (int i = 0; i < n; i++) {
        sam.extend(s[i] - 'a');
    }
    // 不同子串个数
    printf("distinct substrings = %lld\n", sam.countDistinct());

    // 计算 endpos 大小(各状态对应子串的出现次数)
    sam.buildCnt(orderArr, bucketArr, n);

    // 示例：查询整个串本身的出现次数
    int u = 1;
    for (int i = 0; i < n; i++) {
        u = sam.nxt[u][s[i] - 'a'];
    }
    printf("occurrences of full string = %d\n", sam.cnt[u]);

    return 0;
}
```

**使用说明**：
- 调用 `init()` 后，逐字符调用 `extend(c)` 构造 SAM，其中 $c$ 为 $0$-based 字符编号（如 `'a'` 对应 $0$）。
- `countDistinct()` 返回原串的不同子串个数，无需预处理 endpos。
- `buildCnt(order, bucket, n)` 通过基数排序得到 parent 树的拓扑序，并求出每个状态的 endpos 集合大小 `cnt[i]`（即该状态所代表子串的出现次数）。之后查询任一子串出现次数时，先沿自动机转移走到对应状态，再读取 `cnt`。
- 注意 `MAXN` 应开到串长的 $2$ 倍，因为分裂节点会使节点总数最多达到 $2n-1$。
- 字符集默认为小写字母，扩展时调整 `SIGMA` 与字符映射；空间紧张时可将转移改用 `map` 或邻接表存储以降到 $O(n)$。

---

<a id="sec-6-6"></a>

### 6.6 后缀数组SA

- **适用场景**：子串排序、LCP（最长公共前缀）、重复子串、多模式匹配等。
- **时间复杂度**：$O(n \log n)$（倍增 + 基数排序）。
- **核心思想**：倍增地比较后缀，第 $k$ 轮比较每个后缀的前 $2^k$ 个字符（由前 $2^{k-1}$ 个字符的排名拼接得到）。配合基数排序保证每轮 $O(n)$，共 $\log n$ 轮。 Height 数组满足 `Height[rank[i]] >= Height[rank[i-1]] - 1`，可线性递推求出。

**关键定义**：
- `sa[i]`：排名为 $i$ 的后缀的起始位置
- `rk[i]`（即 `rank`）：后缀 $i$ 的排名
- `height[i]`：`sa[i]` 与 `sa[i-1]` 的 LCP 长度
- **性质**：后缀 $i$ 与 $j$（$rk[i] < rk[j]$）的 LCP $= \min\{height[rk[i]+1 \ldots rk[j]]\}$（可配合 ST 表 $O(1)$ 查询）

- **模板代码：**

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MAXN = 1000010;

int n, m;
char s[MAXN];
int sa[MAXN], rk[MAXN], height[MAXN];
int x[MAXN], y[MAXN], c[MAXN];     // x: 第一关键字, y: 第二关键字, c: 计数排序桶

// 倍增构造后缀数组
void build_sa() {
    m = 122;                        // 字符集大小 (ASCII)
    // 第一轮: 单字符排序
    for (int i = 0; i <= m; i++) c[i] = 0;
    for (int i = 1; i <= n; i++) c[x[i] = s[i]]++;
    for (int i = 1; i <= m; i++) c[i] += c[i - 1];
    for (int i = n; i >= 1; i--) sa[c[x[i]]--] = i;

    for (int k = 1; k <= n; k <<= 1) {
        int p = 0;
        // 第二关键字: 后 k 位为空的最小
        for (int i = n - k + 1; i <= n; i++) y[++p] = i;
        // 其余按 sa 顺序, 若 sa[i] > k 则 sa[i] - k 作为第二关键字
        for (int i = 1; i <= n; i++)
            if (sa[i] > k) y[++p] = sa[i] - k;

        // 对第一关键字基数排序
        for (int i = 0; i <= m; i++) c[i] = 0;
        for (int i = 1; i <= n; i++) c[x[i]]++;
        for (int i = 1; i <= m; i++) c[i] += c[i - 1];
        for (int i = n; i >= 1; i--) sa[c[x[y[i]]]--] = y[i];

        // 交换 x, y, 重新计算排名
        swap(x, y);
        x[sa[1]] = 1; p = 1;
        for (int i = 2; i <= n; i++)
            x[sa[i]] = (y[sa[i]] == y[sa[i - 1]] && y[sa[i] + k] == y[sa[i - 1] + k]) ? p : ++p;
        m = p;
        if (p == n) break;          // 所有排名互异, 提前结束
    }
}

// 线性递推 height 数组
void build_height() {
    for (int i = 1; i <= n; i++) rk[sa[i]] = i;
    int h = 0;
    for (int i = 1; i <= n; i++) {
        if (rk[i] == 1) { height[1] = 0; h = 0; continue; }
        if (h) h--;                 // height[rk[i]] >= height[rk[i-1]] - 1
        int j = sa[rk[i] - 1];
        while (i + h <= n && j + h <= n && s[i + h] == s[j + h]) h++;
        height[rk[i]] = h;
    }
}

int main() {
    quick;
    cin >> (s + 1);                 // 1-based
    n = strlen(s + 1);
    build_sa();
    build_height();
    // 输出后缀数组
    for (int i = 1; i <= n; i++) cout << sa[i] << " \n"[i == n];
    // 输出 height 数组 (height[1] = 0)
    for (int i = 1; i <= n; i++) cout << height[i] << " \n"[i == n];
    return 0;
}
```

**使用说明：**
- 字符串采用 1-based 存储，`s[n+1]` 须为空字符以保证 `y[sa[i]+k]` 比较安全
- `m` 初始为字符集最大 ASCII 值（小写字母可设 122）
- 提前终止条件 `p == n`：所有后缀排名互异时无需继续倍增
- `height[rk[i]] >= height[rk[i-1]] - 1` 是线性递推的关键性质
- 求 LCP：建 ST 表后 $O(1)$ 查询任意两后缀 LCP
- 求本质不同子串数：$\sum_{i=1}^{n} (n - sa[i] + 1 - height[i])$
- 重复子串问题：在 `height` 数组上二分/单调栈

---


