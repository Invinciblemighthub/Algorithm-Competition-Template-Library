## [返回目录](http://scpc.fun/discussion-detail/1405)

<style>
a[id], h2, h3, h4 { scroll-margin-top: 140px; }
</style>

---

## 本章目录

- [4.1 GCD与LCM](#sec-4-1) **必学**
- [4.2 乘法逆元](#sec-4-2) **必学**
- [4.3 素数筛法](#sec-4-3) **必学**
  - [4.3.1 线性筛求欧拉函数φ](#sec-4-3-1) **进阶**
  - [4.3.2 线性筛求约数和σ](#sec-4-3-2) **选学**
- [4.4 组合数](#sec-4-4) **必学**
  - [4.4.1 卡特兰数](#sec-4-4-1) **进阶**
  - [4.4.2 斯特林数与斯特林反演](#sec-4-4-2) **选学**
- [4.5 莫比乌斯反演](#sec-4-5) **选学**
- [4.6 整除分块](#sec-4-6) **进阶**
- [4.7 扩展欧拉定理](#sec-4-7) **进阶**
  - [4.7.1 欧拉函数单点计算与经典欧拉定理](#sec-4-7-1) **进阶**
- [4.8 矩阵快速幂](#sec-4-8) **进阶**
- [4.9 中国剩余定理(CRT)](#sec-4-9) **进阶**
- [4.10 扩展中国剩余定理(exCRT)](#sec-4-10) **进阶**
- [4.11 高斯消元法](#sec-4-11) **进阶**
- [4.12 FFT/NTT/FWT](#sec-4-12) **选学**
- [4.13 博弈论SG函数](#sec-4-13) **进阶**

---

<a id="ch4"></a>

## 四、数论

本板块涵盖数论核心算法，包括GCD、逆元、筛法、组合数、莫比乌斯反演、整除分块、扩展欧拉定理和矩阵快速幂。



<a id="sec-4-1"></a>

### 4.1 GCD与LCM
- **适用场景**：最大公约数和最小公倍数计算。
- **时间复杂度**：$O(\log(\min(a, b)))$

**裴蜀定理**：对于任意整数 $a, b$，存在整数 $x, y$ 使得 $ax + by = \gcd(a, b)$；反之，若 $d \mid a$ 且 $d \mid b$，则 $d \mid (ax + by)$。当且仅当 $\gcd(a, b) \mid c$ 时，方程 $ax + by = c$ 有整数解。

```cpp
// GCD模板
long long gcd(long long a, long long b) {
    return b ? gcd(b, a % b) : a;
}

// LCM
long long lcm(long long a, long long b) {
    return a / gcd(a, b) * b;
}

// 扩展欧几里得算法（求 ax + by = gcd(a,b) 的解）—— 即裴蜀定理的构造性证明
long long exgcd(long long a, long long b, long long& x, long long& y) {
    if (b == 0) { x = 1; y = 0; return a; }
    long long x1, y1;
    long long g = exgcd(b, a % b, x1, y1);
    x = y1;
    y = x1 - (a / b) * y1;
    return g;
}
```

**相关题目**：
- [捡拾糖果](http://scpc.fun/problem/SCPC-1165B)（第132题，GCD+方向归一化）
- [The dance of LCM](http://scpc.fun/problem/SCPC-1026K)（第57题，数论+二分，LCM分解）
- [斗地主之癞子](http://scpc.fun/problem/10003)（第21题，约数枚举求最大公约数）

---

<a id="sec-4-2"></a>

### 4.2 乘法逆元
- **适用场景**：在模意义下做除法，$a / b \equiv a \cdot b^{-1} \pmod{p}$。

**方法**：
- **费马小定理**：若 $p$ 为质数且 $p \nmid a$，则 $a^{p-1} \equiv 1 \pmod{p}$。推论：$a^{-1} \equiv a^{p-2} \pmod{p}$（$p$ 为质数，$p \nmid a$）。
- 线性求逆元：$inv[i] = -\lfloor p/i \rfloor \cdot inv[p \% i] \pmod{p}$

```cpp
// 费马小定理求逆元
long long modinv(long long a, long long p) {
    return quickpow(a, p - 2, p);
}

// 线性求逆元（1到n的逆元）
long long inv[MAXN];
void get_inv(int n, long long p) {
    inv[1] = 1;
    for (int i = 2; i <= n; i++)
        inv[i] = (p - p / i) * inv[p % i] % p;
}
```

**相关题目**：
- [枫落和节能的小裙子](http://scpc.fun/problem/9205)（第6题，数论+逆元）
- [兔飞猛进](http://scpc.fun/problem/SCPC-1075F)（第129题，矩阵快速幂+逆元）

---

<a id="sec-4-3"></a>

### 4.3 素数筛法
- **适用场景**：筛出区间内的所有质数。
- **时间复杂度**：埃氏筛 $O(n \log \log n)$，线性筛 $O(n)$

```cpp
// 埃氏筛
vector<bool> isPrime(MAXN + 1, true);
isPrime[0] = isPrime[1] = false;
for (int i = 2; 1LL * i * i <= MAXN; i++)
    if (isPrime[i])
        for (long long j = 1LL * i * i; j <= MAXN; j += i)
            isPrime[j] = false;

// 线性筛（欧拉筛）
vector<int> primes;
vector<bool> isPrime(MAXN + 1, true);
isPrime[0] = isPrime[1] = false;
for (int i = 2; i <= MAXN; i++) {
    if (isPrime[i]) primes.push_back(i);
    for (int p : primes) {
        if (1LL * i * p > MAXN) break;
        isPrime[i * p] = false;
        if (i % p == 0) break;
    }
}

// 最小质因子筛（SPF，用于质因数分解）
int spf[MAXN];
void sieve_spf() {
    for (int i = 2; i < MAXN; i++) spf[i] = i;
    for (int i = 2; i * i < MAXN; i++) {
        if (spf[i] == i) {
            for (int j = i * i; j < MAXN; j += i)
                if (spf[j] == j) spf[j] = i;
        }
    }
}
// 质因数分解
vector<int> factorize(int n) {
    vector<int> factors;
    while (n > 1) {
        factors.push_back(spf[n]);
        n /= spf[n];
    }
    return factors;
}
```

**相关题目**：
- [找质数](http://scpc.fun/problem/SCPC-1072G)（第60题，埃氏筛+指针）
- [天降横祸](http://scpc.fun/problem/SCPC-1126H)（第131题，最小质因子筛+DFS）

---

<a id="sec-4-3-1"></a>

### 4.3.1 线性筛求欧拉函数φ

**定义**：$\varphi(n)$ 表示 $[1, n]$ 中与 $n$ 互质的整数个数。
**积性性质**：若 $\gcd(a,b)=1$，则 $\varphi(ab)=\varphi(a)\varphi(b)$。
**单点公式**：若 $n = \prod p_i^{a_i}$，则 $\varphi(n) = n \cdot \prod \frac{p_i - 1}{p_i}$。

```cpp
// 在欧拉筛（线性筛）过程中顺便计算 phi[i]
// phi[1..n]：1到n所有整数的欧拉函数值
void phi_sieve(int n, vector<int>& phi, vector<int>& primes, vector<bool>& isP) {
    phi.assign(n + 1, 0);
    isP.assign(n + 1, true);
    isP[0] = isP[1] = false;
    phi[1] = 1;
    primes.clear();
    for (int i = 2; i <= n; i++) {
        if (isP[i]) { primes.push_back(i); phi[i] = i - 1; }   // i是质数：phi[i] = i-1
        for (int p : primes) {
            if (1LL * i * p > n) break;
            isP[i * p] = false;
            if (i % p == 0) {
                phi[i * p] = phi[i] * p;                      // i含p：phi(i*p) = phi(i)*p
                break;
            } else {
                phi[i * p] = phi[i] * (p - 1);                // i不含p：phi(i*p) = phi(i)*(p-1) （积性）
            }
        }
    }
}
```
- **时间复杂度**：$O(n)$（与欧拉筛完全相同）。

---

<a id="sec-4-3-2"></a>

### 4.3.2 线性筛求约数和σ

**定义**：
- $d(n)$ / $\tau(n)$：$n$ 的正约数个数。若 $n = \prod p_i^{a_i}$，则 $d(n) = \prod (a_i + 1)$。
- $\sigma(n)$：$n$ 的所有正约数之和。公式：$\sigma(n) = \prod \frac{p_i^{a_i+1} - 1}{p_i - 1}$。

两者均为积性函数，可在欧拉筛过程中同步维护。

```cpp
// 线性筛求 d[n]（约数个数）+ sigma[n]（约数和）
// 辅助数组 cnt[i]：i 的最小质因子 p 在 i 中出现的幂次（即 i 中 p^cnt[i]）
void divisor_sieve(int n,
                   vector<int>& d, vector<long long>& sigma,
                   vector<int>& cnt,    // cnt[i]：i最小质因子的指数
                   vector<int>& lowp,   // lowp[i]：i的最小质因子幂次 p^cnt[i]
                   vector<int>& primes, vector<bool>& isP) {
    d.assign(n + 1, 0);     sigma.assign(n + 1, 0);
    cnt.assign(n + 1, 0);   lowp.assign(n + 1, 0);
    isP.assign(n + 1, true);
    isP[0] = isP[1] = false;
    d[1] = 1; sigma[1] = 1; cnt[1] = 0; lowp[1] = 1;
    primes.clear();
    for (int i = 2; i <= n; i++) {
        if (isP[i]) {
            primes.push_back(i);
            cnt[i]   = 1;                          // 质数 i^1
            lowp[i]  = i;                          // 最小质因子幂 = i^1
            d[i]     = 2;                          // 质数的约数：1 和 i 共 2 个
            sigma[i] = 1 + i;                      // 质数的约数和：1 + i
        }
        for (int p : primes) {
            if (1LL * i * p > n) break;
            isP[i * p] = false;
            if (i % p == 0) {
                lowp [i * p] = lowp[i] * p;
                cnt  [i * p] = cnt[i]  + 1;
                // d(i*p) = d(i / lowp[i]) * (cnt[i]+2)  （把 p 的指数从 a 改成 a+1，因子乘积变化）
                d    [i * p] = d[i / lowp[i]] * (cnt[i] + 2);
                // sigma(i*p) = sigma(i / lowp[i]) * ( (p^{cnt+2}-1) / (p-1) )
                // 等价递推：因为 sigma(i) = s_prev * (p^{a+1}-1)/(p-1)，多乘一个p就是 (p^{a+2}-1)/(p-1)
                sigma[i * p] = sigma[i / lowp[i]] * (1LL * lowp[i] * p * p - 1) / (p - 1);
                break;
            } else {
                lowp [i * p] = p;                   // 新因子首次出现，幂次1
                cnt  [i * p] = 1;
                d    [i * p] = d[i]     * 2;        // 积性，多了一个 (指数1→1+1=2) 因子
                sigma[i * p] = sigma[i] * (1 + p);  // 积性，新质因子贡献 (1 + p^1)
            }
        }
    }
}
```

**使用说明**：
- 若题目**只需要 1..n 约数和 sigma**，无需实现上面复杂的 sigma筛，可用"倍数枚举"更简单：外层 $d=1..n$，内层对 $d$ 的所有倍数位置 `sigma[kd] += d`，复杂度 $O(n \log n)$，大多数 n≤1e6 的场景都够用。
- 上面代码中 `sigma[i*p]` 用 `(p^{cnt+2}-1)/(p-1)`，因为 sigma 乘的是等比数列和，**在筛法递推中可直接用公式推得**，实际 OI 题如需约数和也可打表倍数枚举。

---

<a id="sec-4-4"></a>

### 4.4 组合数
- **适用场景**：计算 $C_n^k \bmod p$。
- **时间复杂度**：预处理 $O(n)$，单次查询 $O(1)$

```cpp
// 组合数模板（预处理阶乘+逆元）
const int MOD = 1e9 + 7;
const int MAXN = 200005;
long long fac[MAXN], invfac[MAXN];

long long modpow(long long a, long long e) {
    long long r = 1;
    while (e) {
        if (e & 1) r = r * a % MOD;
        a = a * a % MOD;
        e >>= 1;
    }
    return r;
}

void init() {
    fac[0] = 1;
    for (int i = 1; i < MAXN; i++) fac[i] = fac[i-1] * i % MOD;
    invfac[MAXN-1] = modpow(fac[MAXN-1], MOD - 2);
    for (int i = MAXN - 2; i >= 0; i--) invfac[i] = invfac[i+1] * (i+1) % MOD;
}

long long C(int n, int k) {
    if (k < 0 || k > n) return 0;
    return fac[n] * invfac[k] % MOD * invfac[n-k] % MOD;
}

// Lucas定理（p较小时）
long long lucas(long long n, long long k, long long p) {
    if (k == 0) return 1;
    return C(n % p, k % p) * lucas(n / p, k / p, p) % p;
}
```

**相关题目**：
- [差值和](http://scpc.fun/problem/10006)（第22题，数论+组合数）
- [KAKAZL在写信](http://scpc.fun/problem/SCPC-1186G)（第136题，组合数学+排列）

---

<a id="sec-4-4-1"></a>

### 4.4.1 卡特兰数

**卡特兰数常用场景**（任何满足"入栈/出栈合法""合法括号""二叉树形态""不穿过对角线的路径数"计数的都是卡特兰数）：
- 长度 2n 的合法括号序列数
- n 个节点组成的不同形态的二叉树数量
- 凸 n+2 边形的三角剖分数
- n 对元素进栈出栈后得到的不同出栈序列数

**通项公式**：
$$C_n = \frac{1}{n+1}\binom{2n}{n} = \binom{2n}{n} - \binom{2n}{n-1}$$

**递推公式**：$C_0 = 1,\quad C_{n+1} = \sum_{i=0}^n C_i C_{n-i}$

```cpp
// 预处理 1..2n 阶乘 + 逆元，然后 O(1) 求卡特兰数 C_n mod p
// 要求 p 为质数（通常 1e9+7 / 998244353）

const int MOD = 1e9 + 7;

long long qpow(long long a, long long b) {
    long long res = 1;
    while (b) {
        if (b & 1) res = res * a % MOD;
        a = a * a % MOD;
        b >>= 1;
    }
    return res;
}

vector<long long> fac, ifac;
void init_factorial(int N) {
    fac.resize(N + 1); ifac.resize(N + 1);
    fac[0] = 1;
    for (int i = 1; i <= N; i++) fac[i] = fac[i - 1] * i % MOD;
    ifac[N] = qpow(fac[N], MOD - 2);
    for (int i = N - 1; i >= 0; i--) ifac[i] = ifac[i + 1] * (i + 1) % MOD;
}
long long C(int n, int k) {
    if (k < 0 || k > n) return 0;
    return fac[n] * ifac[k] % MOD * ifac[n - k] % MOD;
}

// 卡特兰数 C_n（第 n 项，n 从 0 起）
long long catalan(int n) {
    if (n < 0) return 0;
    long long ans = (C(2 * n, n) - C(2 * n, n - 1) + MOD) % MOD;  // 两种写法等价
    // long long ans = C(2*n, n) * qpow(n+1, MOD-2) % MOD;          // 另一种写法
    return ans;
}

// 递推版（不依赖逆元，但 n ≤ 几千时可用；大数会溢出，需配套高精度或取模）
// C[0]=1; for(int i=1; i<=n; i++) C[i]=0; for(int j=0; j<i; j++) C[i]+=C[j]*C[i-1-j];
```

**注意事项**：
- 卡特兰数前 7 项：1, 1, 2, 5, 14, 42, 132（C₀ 到 C₆），记熟可秒判很多题。
- 若题目是"合法括号但可以多出右括号仍合法"等**非严格卡特兰**变体，要用反射原理单独推公式（卡特兰本质就是反射原理应用）。

---

<a id="sec-4-4-2"></a>

### 4.4.2 斯特林数与斯特林反演

#### 第二类斯特林数 $S(n, k)$ / $\genfrac\{\}{0pt}{}n k$
**含义**：把 $n$ 个**不同**的元素分成 $k$ 个**非空**的、不计顺序的**集合**（划分子集），共有多少种分法。

**递推公式**：$S(n, k) = S(n-1, k-1) + k \cdot S(n-1, k)$，边界 $S(0,0)=1$，$S(n,0)=0$（$n>0$）。
- 组合含义：第 n 个元素要么单独一个子集 `S(n-1,k-1)`，要么塞进已有 k 个子集的任意一个里 `k·S(n-1,k)`。

#### 第一类斯特林数 $s(n, k)$ / $\genfrac[]{0pt}{}n k$
**含义**：把 $n$ 个元素划成 $k$ 个**非空轮换**（环排列，不计旋转同构）的方案数；带符号第一类斯特林数记为 $(-1)^{n-k}s(n,k)$。

- 无符号递推：$s(n,k) = s(n-1,k-1) + (n-1)\cdot s(n-1,k)$，边界同上。
- 组合含义：第 n 个元素要么单独一个环 `s(n-1,k-1)`，要么插入到已有 n-1 个元素的任意一个"身后"（顺时针方向），共 n-1 个插入点。

#### 斯特林反演
若 $f(n) = \sum_{k=0}^n S(n, k) g(k)$，则 $g(n) = \sum_{k=0}^n (-1)^{n-k} s(n, k) f(k)$。（反之用带符号/无符号也成立，细节看具体推导）
**用途**：计数问题的公式推导（在"恰好k个"与"最多/至少k个"之间转换）。**反演没有通用代码模板**，均为比赛现场推公式后用第二类斯特林数/阶乘等工具计算。

```cpp
// ============= 第二类斯特林数 S(n, k) 递推（DP） =============
// 时间复杂度 O(nk)
vector<vector<long long>> stirling2(int n, int k, long long mod) {
    vector<vector<long long>> S(n + 1, vector<long long>(k + 1, 0));
    S[0][0] = 1;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= min(i, k); j++) {
            S[i][j] = (S[i - 1][j - 1] + 1LL * j * S[i - 1][j]) % mod;
        }
    }
    return S;   // S[n][k] 即为答案
}

// ============= 第一类斯特林数 s(n, k)（无符号版本）递推 =============
vector<vector<long long>> stirling1(int n, int k, long long mod) {
    vector<vector<long long>> s(n + 1, vector<long long>(k + 1, 0));
    s[0][0] = 1;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= min(i, k); j++) {
            s[i][j] = (s[i - 1][j - 1] + 1LL * (i - 1) * s[i - 1][j]) % mod;
        }
    }
    return s;
}

// ============= 第二类斯特林数 通项公式（单组查询 O(k) 容斥计算） =============
// S(n, k) = (1/k!) * Σ_{i=0}^k (-1)^{k-i} * C(k,i) * i^n
long long stirling2_single(int n, int k, long long mod) {
    if (k < 0 || k > n) return 0;
    // 需要已预处理阶乘 fac[0..k]、逆元阶乘 ifac[0..k]
    long long res = 0;
    for (int i = 0; i <= k; i++) {
        long long c = fac[k] * ifac[i] % mod * ifac[k - i] % mod;     // C(k,i)
        long long term = c * qpow(i, n) % mod;
        if ((k - i) & 1) term = (mod - term) % mod;                   // (-1)^{k-i}
        res = (res + term) % mod;
    }
    res = res * ifac[k] % mod;
    return res;
}
```

---

<a id="sec-4-5"></a>

### 4.5 莫比乌斯反演
- **适用场景**：处理数论函数的求和问题。
- **核心公式**：$F(n) = \sum_{d|n} f(d) \Rightarrow f(n) = \sum_{d|n} \mu(d) F(n/d)$

```cpp
// 线性筛求莫比乌斯函数
int mu[MAXN];
vector<bool> isPrime(MAXN, true);
vector<int> primes;

void sieve_mu() {
    mu[1] = 1;
    for (int i = 2; i < MAXN; i++) {
        if (isPrime[i]) {
            primes.push_back(i);
            mu[i] = -1;
        }
        for (int p : primes) {
            if (1LL * i * p >= MAXN) break;
            isPrime[i * p] = false;
            if (i % p == 0) { mu[i * p] = 0; break; }
            else mu[i * p] = -mu[i];
        }
    }
}

// 莫比乌斯反演：F(n) = sum_{d|n} f(d), f(n) = sum_{d|n} mu(d)*F(n/d)
```

**相关题目**：
- [禁忌の魔法](http://scpc.fun/problem/10023)（第74题，莫比乌斯反演+数论分块）

---

<a id="sec-4-6"></a>

### 4.6 整除分块
- **适用场景**：计算 $\sum_{i=1}^n f(\lfloor n/i \rfloor)$，利用 $\lfloor n/i \rfloor$ 只有 $O(\sqrt{n})$ 种取值的性质。
- **时间复杂度**：$O(\sqrt{n})$

```cpp
// 整除分块模板
// 计算 sum_{i=1}^{n} floor(n/i)
long long sum_floor(int n) {
    long long ans = 0;
    for (int l = 1, r; l <= n; l = r + 1) {
        int k = n / l;
        r = n / k;
        ans += 1LL * k * (r - l + 1);
    }
    return ans;
}

// 带莫比乌斯函数的整除分块
// sum_{i=1}^{n} mu(i) * f(n/i)
// 用前缀和预处理mu的前缀和
long long solve(int n) {
    long long ans = 0;
    for (int l = 1, r; l <= n; l = r + 1) {
        int k = n / l;
        r = n / k;
        // mu_sum = prefix_mu[r] - prefix_mu[l-1]
        // ans += mu_sum * f(k)
    }
    return ans;
}
```

**相关题目**：
- [【模板】整除分块](http://scpc.fun/problem/10000)（第20题，整除分块模板）
- [几番烟雾，只有花难护](http://scpc.fun/problem/SCPC-1049G)（第3题，数论+整除分块+平方和公式）

---

<a id="sec-4-7"></a>

### 4.7 扩展欧拉定理

- **适用场景**：当指数 $b$ 极大（如以字符串形式给出，长度可达 $10^6$）时，利用扩展欧拉定理降幂计算 $a^b \bmod m$。
- **时间复杂度**：$O(\sqrt{m})$ 求 $\varphi(m)$ + $O(|b|)$ 降幂 + $O(\log m)$ 快速幂。
- **公式表述**：

对任意正整数 $a, m, b$，当 $b \geq \varphi(m)$ 时：

$$a^b \equiv a^{b \bmod \varphi(m) + \varphi(m)} \pmod{m}$$

与经典欧拉定理（要求 $\gcd(a, m) = 1$）不同，扩展欧拉定理对 $\gcd(a, m) \neq 1$ 的情况同样适用。

- **降幂代码模板：**

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

// 求 phi(m)
long long euler_phi(long long m) {
    long long res = m;
    for (long long i = 2; i * i <= m; i++) {
        if (m % i == 0) {
            while (m % i == 0) m /= i;
            res = res / i * (i - 1);
        }
    }
    if (m > 1) res = res / m * (m - 1);
    return res;
}

// 快速幂
long long quickpow(long long a, long long b, long long mod) {
    long long r = 1; a %= mod; if (a < 0) a += mod;
    for (; b > 0; b >>= 1, a = a * a % mod)
        if (b & 1) r = r * a % mod;
    return r;
}

// 读入大指数（字符串形式），利用扩展欧拉定理降幂
// 返回实际指数：b < phi(m) 时返回 b，否则返回 b mod phi(m) + phi(m)
long long read_and_reduce(long long phi) {
    string s; cin >> s;
    long long b = 0;
    bool ge = false;                 // 标记 b 是否 >= phi(m)
    for (char c : s) {
        b = b * 10 + (c - '0');
        if (b >= phi) { ge = true; b %= phi; }
    }
    if (ge) b += phi;               // 扩展欧拉定理降幂
    return b;
}

int main() {
    quick;
    long long a, m;
    cin >> a >> m;
    long long phi = euler_phi(m);
    long long exp = read_and_reduce(phi);
    cout << quickpow(a, exp, m) << endl;
    return 0;
}
```

**典型应用场景：**
- **大指数降幂**：$b$ 以字符串形式给出（长度可达 $10^6$），直接读入并逐位降幂
- **幂塔问题**：求 $a^{a^{a^{\cdots}}} \bmod m$，利用 $\varphi$ 的递减性质（$\varphi(\varphi(\cdots\varphi(m)))$ 快速降到 1）
- **$\gcd(a, m) \neq 1$ 时降幂**：经典欧拉定理不适用，必须用扩展欧拉定理

---

<a id="sec-4-7-1"></a>

### 4.7.1 欧拉函数单点计算与经典欧拉定理

#### 经典欧拉定理
若 $\gcd(a, m) = 1$，则 $a^{\varphi(m)} \equiv 1 \pmod{m}$。
推论：当 $\gcd(a,m)=1$ 时，$a^b \equiv a^{b \bmod \varphi(m)} \pmod{m}$（指数可对 $\varphi(m)$ 取模）。

> **注**：这是经典版本；**扩展欧拉定理**去掉了 $\gcd(a,m)=1$ 的约束，只需要 $b \ge \varphi(m)$ 即可写成 $a^b \equiv a^{b \bmod \varphi(m) + \varphi(m)} \pmod{m}$（见下方 4.7 主模板）。

#### 欧拉函数单点计算
对单个 n 求 φ(n)（无需筛法），利用质因数分解公式：
$$\varphi(n) = n \prod_{p \mid n} \frac{p-1}{p}$$

```cpp
// 欧拉函数单点计算：O(√n)，对 n 分解质因数后代公式
long long phi_single(long long n) {
    long long res = n;
    for (long long p = 2; p * p <= n; p++) {
        if (n % p == 0) {
            while (n % p == 0) n /= p;
            res = res / p * (p - 1);     // 先除以 p 再乘 (p-1)，避免溢出
        }
    }
    if (n > 1) res = res / n * (n - 1);  // 剩余的 n 是最后一个质因子
    return res;
}
```

**使用说明**：
- 若需要 1..n 所有 φ 值，优先用 [4.3.1 线性筛求φ](http://scpc.fun/discussion-detail/1409#431-线性筛求欧拉函数φ)（O(n)）。
- 若只需要少量几个数的 φ 值，用本函数 O(√n) 更省空间。

---

- **核心公式**（扩展欧拉定理）：$a^b \equiv a^{b \bmod \varphi(p) + \varphi(p)} \pmod{p}$（当 $b \geq \varphi(p)$ 时）

```cpp
// 扩展欧拉定理模板
// 欧拉函数
long long euler_phi(long long n) {
    long long res = n;
    for (long long p = 2; p * p <= n; p++) {
        if (n % p == 0) {
            while (n % p == 0) n /= p;
            res -= res / p;
        }
    }
    if (n > 1) res -= res / n;
    return res;
}

// 大数取模（字符串表示的大数对mod取模）
long long mod_str(const string& s, long long mod) {
    long long r = 0;
    for (char c : s) r = (r * 10 + (c - '0')) % mod;
    return r;
}

// 比较 string 与 long long 大小
int cmp_str_ll(const string& s, long long x) {
    string xs = to_string(x);
    if (s.size() != xs.size()) return s.size() > xs.size() ? 1 : -1;
    for (size_t i = 0; i < s.size(); i++)
        if (s[i] != xs[i]) return s[i] > xs[i] ? 1 : -1;
    return 0;
}

// 用法：若指数 b >= phi(p)，则 a^b mod p = a^(b mod phi(p) + phi(p)) mod p
```

**相关题目**：
- [陈光蕊赴任逢灾，江流僧复仇报本](http://scpc.fun/problem/9305)（第17题，扩展欧拉定理+大数取模）

---

<a id="sec-4-8"></a>

### 4.8 矩阵快速幂
- **适用场景**：加速线性递推（如斐波那契数列），将 $O(n)$ 递推优化到 $O(\log n)$。
- **时间复杂度**：$O(k^3 \log n)$，$k$ 为矩阵大小

```cpp
// 矩阵快速幂模板
struct Matrix {
    int n;
    vector<vector<long long>> a;
    Matrix(int n = 2, bool identity = false) : n(n), a(n, vector<long long>(n, 0)) {
        if (identity) for (int i = 0; i < n; i++) a[i][i] = 1;
    }
};

Matrix multiply(const Matrix& x, const Matrix& y, long long mod) {
    int n = x.n;
    Matrix z(n);
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++) {
            z.a[i][j] = 0;
            for (int k = 0; k < n; k++)
                z.a[i][j] = (z.a[i][j] + x.a[i][k] * y.a[k][j]) % mod;
        }
    return z;
}

Matrix mat_pow(Matrix base, long long exp, long long mod) {
    Matrix res(base.n, true);  // 单位矩阵
    while (exp) {
        if (exp & 1) res = multiply(res, base, mod);
        base = multiply(base, base, mod);
        exp >>= 1;
    }
    return res;
}

// 斐波那契数列：[[0,1],[1,1]]^k 得到 F(k), F(k+1)
// F(k) = mat.a[0][1], F(k+1) = mat.a[1][1]
```

**相关题目**：
- [兔飞猛进](http://scpc.fun/problem/SCPC-1075F)（第129题，矩阵快速幂+逆元，斐波那契递推）

---

<a id="sec-4-9"></a>

### 4.9 中国剩余定理(CRT)
- **适用场景**：给定 k 组同余方程，模数两两互质，求满足方程组的解 x。
$$\begin{cases} x \equiv a_1 \pmod{m_1} \\ x \equiv a_2 \pmod{m_2} \\ \cdots \\ x \equiv a_k \pmod{m_k} \end{cases}$$
**条件**：$m_1, m_2, \ldots, m_k$ 必须**两两互质**。
- **核心思想**：设 $M = \prod m_i$，$M_i = M / m_i$，则 $x = \sum a_i \cdot M_i \cdot \text{inv}(M_i, m_i) \pmod{M}$，其中 $\text{inv}(M_i, m_i)$ 表示 $M_i$ 在模 $m_i$ 下的逆元（exgcd/费马均可）。

```cpp
// 中国剩余定理 CRT
// x ≡ a_i mod m_i   (m_i 两两互质)
// 返回 pair<x, M>：x 是 [0, M) 范围内的最小非负解，M = ∏m_i（解周期为 M）
// 若不满足两两互质（或方程组无解），请使用 4.10 exCRT

pair<long long, long long> crt(vector<long long>& a, vector<long long>& m) {
    long long M = 1, x = 0;
    int k = a.size();
    for (int i = 0; i < k; i++) M *= m[i];
    for (int i = 0; i < k; i++) {
        long long Mi = M / m[i];
        long long inv_Mi, y;
        // 求 Mi 在 mod m_i 下的逆元：Mi * inv_Mi ≡ 1 (mod m_i)
        long long g = exgcd(Mi, m[i], inv_Mi, y);
        (void)g;   // 因 m_i 互质，gcd(Mi, m_i)=1，必然有逆
        inv_Mi = (inv_Mi % m[i] + m[i]) % m[i];
        long long term = (__int128)a[i] * Mi % M;
        term = (__int128)term * inv_Mi % M;
        x = (x + term) % M;
    }
    x = (x % M + M) % M;
    return {x, M};
}
```

**注意**：CRT 只适用于模数两两互质。实际上竞赛绝大多数题可用通用版 **4.10 exCRT**（不需要互质条件、能判无解）替代。

---

<a id="sec-4-10"></a>

### 4.10 扩展中国剩余定理(exCRT)
- **适用场景**：与 CRT 相同——合并若干同余方程，但**不要求模数两两互质**；能正确判断无解。
- **核心思想**：两两迭代合并。假设当前已合并到 `x ≡ a1 mod m1`，下一条方程是 `x ≡ a2 mod m2`，则等价于解 `a1 + m1·t ≡ a2 mod m2`，即 `m1·t ≡ (a2 - a1) mod m2`，用 exgcd 解线性同余方程求出 t；若 $\gcd(m1, m2) \nmid (a2-a1)$ 则无解。合并后得到新的 `a' = a1 + m1·t`，`m' = lcm(m1, m2) = m1 / gcd * m2`。

```cpp
// 扩展中国剩余定理 exCRT（不需要模数互质，能判无解）
// 返回 pair<x, M>：x 是 [0, M) 的最小非负解，M 为最终模数（即所有 m_i 的 LCM）
// 无解返回 {-1, 0}
pair<long long, long long> excrt(vector<long long>& a, vector<long long>& m) {
    long long A = 0, M = 1;          // 已合并的方程组：x ≡ A mod M
    int k = a.size();
    for (int i = 0; i < k; i++) {
        long long a2 = a[i], m2 = m[i];
        // 解：A + M*t ≡ a2 (mod m2)   =>   M*t ≡ (a2 - A) mod m2
        long long g = gcd(M, m2);
        long long c = (a2 - A % m2 + m2) % m2;
        if (c % g != 0) return {-1, 0};     // 无解
        // 用 exgcd 求 (M/g) * t ≡ (c/g) mod (m2/g) 的最小正整数解 t
        long long t0, y;
        exgcd(M / g, m2 / g, t0, y);
        long long mod_new = m2 / g;
        t0 = (__int128)t0 % mod_new * ((c / g) % mod_new) % mod_new;
        if (t0 < 0) t0 += mod_new;
        // 合并后
        long long lcm = M / g * m2;                         // 注意：M*m2 可能爆long long，需按题目范围加 __int128
        A = (A + (__int128)M * t0) % lcm;
        M = lcm;
        A = (A % M + M) % M;
    }
    return {A, M};
}
```

**使用注意**：
- 若题目**保证**模数两两互质，exCRT 依然能正确工作（性能与 CRT 基本一致），但代码更通用，**建议比赛直接默写 exCRT**，不用背两个版本。
- 当 $m_i$ 的 `LCM` 极大（如 `1e18` 量级）时，乘法步骤必须用 `__int128` 或手写慢速乘避免 long long 溢出，模板中已用 `__int128` 做中间乘法。

---

<a id="sec-4-11"></a>

### 4.11 高斯消元法
- **适用场景**：
- 解线性方程组 $Ax = b$（实数 / 模素数域）
- 求矩阵行列式 / 矩阵的秩 / 矩阵求逆
- 线性基（处理异或最值、第k小异或、异或和可达性等问题）

---

**① 浮点数高斯消元 —— 解实数线性方程组**

列主元消元法（选当前列绝对值最大的行换到当前行做 pivoting，减小舍入误差）。解判定：若出现 `0 = 非0` → 无解；若有效方程个数 < 未知数个数 → 无穷多解。
- **时间复杂度**：$O(n^3)$

```cpp
// 浮点数高斯消元：解 n 个 n 元一次方程组 a[n*n] * x = b[n]，结果写入 x
// 返回值：0 = 无解，1 = 唯一解，2 = 无穷多解
int gauss_double(int n, vector<vector<double>>& a, vector<double>& b, vector<double>& x) {
    const double EPS = 1e-8;
    for (int col = 0, row = 0; col < n && row < n; col++, row++) {
        // 1. 列主元：选绝对值最大的行
        int mx = row;
        for (int i = row + 1; i < n; i++)
            if (fabs(a[i][col]) > fabs(a[mx][col])) mx = i;
        swap(a[row], a[mx]); swap(b[row], b[mx]);
        if (fabs(a[row][col]) < EPS) { row--; continue; }   // 该列全0，跳过，行号不变
        // 2. 消元：把当前列下方所有系数变成 0
        for (int i = row + 1; i < n; i++) {
            if (fabs(a[i][col]) < EPS) continue;
            double factor = a[i][col] / a[row][col];
            for (int j = col; j < n; j++) a[i][j] -= factor * a[row][j];
            b[i] -= factor * b[row];
        }
    }
    x.assign(n, 0);
    // 3. 回代：从最后一行开始
    for (int i = n - 1; i >= 0; i--) {
        double sum = b[i];
        int first_non_zero = -1;
        for (int j = 0; j < n; j++)
            if (fabs(a[i][j]) > EPS) { first_non_zero = j; break; }
        if (first_non_zero == -1) {
            if (fabs(b[i]) > EPS) return 0;          // 0 = 非0：无解
            else continue;                            // 0 = 0：无穷多解的一行
        }
        for (int j = first_non_zero + 1; j < n; j++) sum -= a[i][j] * x[j];
        x[first_non_zero] = sum / a[i][first_non_zero];
    }
    // 若回代过程中存在自由变量（上面跳过了一些 row），就是无穷解
    int free_vars = 0;
    for (int i = 0; i < n; i++) {
        bool all_zero = true;
        for (int j = 0; j < n; j++) if (fabs(a[i][j]) > EPS) { all_zero = false; break; }
        if (all_zero && fabs(b[i]) <= EPS) free_vars++;
    }
    return free_vars > 0 ? 2 : 1;
}
```

**使用注意**：
- 当题目系数都是整数且要求精确分数解时，推荐用"② mod prime 版"更稳定（浮点有误差风险）。

---

**② 模素数高斯消元 —— 解 $A x \equiv b \pmod{p}$**

与浮点版流程完全相同，但所有"除法"用乘**逆元**代替。`p` 必须是质数（才能保证非0系数都有逆元）。

```cpp
// mod prime 高斯消元（p 必须是质数）
int gauss_mod(int n, vector<vector<long long>>& a, vector<long long>& b,
              vector<long long>& x, long long p) {
    auto qp = [&](long long A, long long B) { long long r = 1; A %= p;
        while (B) { if (B & 1) r = r * A % p; A = A * A % p; B >>= 1; } return r; };
    auto inv = [&](long long x) { return qp(x, p - 2); };

    int rank = 0;
    for (int col = 0, row = 0; col < n && row < n; col++, row++) {
        int mx = row;
        for (int i = row; i < n; i++) if (a[i][col]) { mx = i; break; }   // 选第一个非0行（模下无需列主元）
        if (!a[mx][col]) { row--; continue; }
        swap(a[row], a[mx]); swap(b[row], b[mx]);
        rank++;
        long long inv_piv = inv(a[row][col]);
        // 消去所有其他行（包括上方行）
        for (int i = 0; i < n; i++) {
            if (i == row || !a[i][col]) continue;
            long long factor = a[i][col] * inv_piv % p;
            for (int j = col; j < n; j++)
                a[i][j] = (a[i][j] - factor * a[row][j] % p + p) % p;
            b[i] = (b[i] - factor * b[row] % p + p) % p;
        }
    }
    x.assign(n, 0);
    // 上面用的是"约旦消元"（化对角矩阵），直接读即可
    for (int i = 0; i < n; i++) {
        bool row_zero = true;
        for (int j = 0; j < n; j++) if (a[i][j]) { row_zero = false; break; }
        if (row_zero) { if (b[i]) return 0; continue; }     // 无解
        for (int j = 0; j < n; j++)
            if (a[i][j]) { x[j] = b[i] * qp(a[i][j], p - 2) % p; break; }
    }
    return rank == n ? 1 : 2;   // 1=唯一解，2=无穷多解
}
```

---

**③ 模素数行列式 —— 高斯消元求 $|A| \bmod p$**

化上三角矩阵，对角线乘积就是行列式；每次换行要给行列式乘 `-1`。

```cpp
long long det_mod(int n, vector<vector<long long>> a, long long p) {
    auto qp = [&](long long A, long long B) { long long r = 1; A %= p;
        while (B) { if (B & 1) r = r * A % p; A = A * A % p; B >>= 1; } return r; };
    long long det = 1;
    for (int col = 0; col < n; col++) {
        int pivot = -1;
        for (int i = col; i < n; i++) if (a[i][col]) { pivot = i; break; }
        if (pivot == -1) return 0;
        if (pivot != col) { swap(a[col], a[pivot]); det = (p - det) % p; }   // 换行：乘 -1
        det = det * a[col][col] % p;
        long long inv_piv = qp(a[col][col], p - 2);
        for (int i = col + 1; i < n; i++) {
            if (!a[i][col]) continue;
            long long factor = a[i][col] * inv_piv % p;
            for (int j = col; j < n; j++)
                a[i][j] = (a[i][j] - factor * a[col][j] % p + p) % p;
        }
    }
    return (det % p + p) % p;
}
```

---

**④ Gauss-Jordan 秩与矩阵求逆**

给定 $n \times n$ 矩阵 $A$，求它的**秩**和**逆矩阵** $A^{-1}$（使得 $A \cdot A^{-1} = I$）。方法：在 $A$ 右侧挂一个同样大小的单位矩阵 $I$ 组成 $n \times 2n$ 增广矩阵，对增广矩阵用 Gauss-Jordan（约旦消元：每列选主元后**全列清零除主元行外**）。若最终左侧能化成单位阵，则右侧即 $A^{-1}$；左侧非零行数 = 秩。

```cpp
// 求 A 的秩 rank + 逆矩阵 inv（mod prime版，p必须为质数）
// 若不可逆：inv 返回空矩阵，rank 为实际秩
int gauss_jordan(int n, vector<vector<long long>> A, long long p,
                 vector<vector<long long>>& inv) {
    auto qp = [&](long long a, long long b) { long long r = 1; a %= p;
        while (b) { if (b & 1) r = r * a % p; a = a * a % p; b >>= 1; } return r; };
    // 构造增广矩阵 n*2n：左侧=A，右侧=单位阵
    vector<vector<long long>> M(n, vector<long long>(2 * n, 0));
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) M[i][j] = A[i][j] % p;
        M[i][i + n] = 1;
    }
    int rank = 0;
    for (int col = 0, row = 0; col < n && row < n; col++, row++) {
        int pivot = -1;
        for (int i = row; i < n; i++) if (M[i][col]) { pivot = i; break; }
        if (pivot == -1) { row--; continue; }
        swap(M[row], M[pivot]);
        rank++;
        long long inv_piv = qp(M[row][col], p - 2);
        // 主元行整行归一化
        for (int j = 0; j < 2 * n; j++) M[row][j] = M[row][j] * inv_piv % p;
        // 全列清零（除主元行）
        for (int i = 0; i < n; i++) {
            if (i == row || !M[i][col]) continue;
            long long factor = M[i][col];
            for (int j = 0; j < 2 * n; j++)
                M[i][j] = (M[i][j] - factor * M[row][j] % p + p) % p;
        }
    }
    // 判断是否可逆：必须 rank == n 且左侧 = 单位阵
    if (rank != n) { inv.clear(); return rank; }
    inv.assign(n, vector<long long>(n));
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
            inv[i][j] = M[i][j + n];
    return rank;
}
```

---

**⑤ 异或线性基（XOR Basis / XOR高斯消元）**

用于处理"数组选若干个数异或起来能得到的最大值/最小值/第k小值/可达性判断"。是高斯消元在 GF(2)（0-1异或域）的特殊情形。
- **核心思想**：维护 $b[i]$ = "最高有效位为 i" 的一个"基向量"，插入数 $x$ 时从高位到低位消去已有基，若最终不为 0 则把它放入对应位置的基里。任何能被原数组异或表示的数，都能唯一表示为基的子集异或。

```cpp
// ============= 异或线性基（XOR Basis）=============
// 通常取 60 位或 31 位；long long 版用 60 位足够

struct XorBasis {
    static const int BITS = 60;
    long long b[BITS + 1];   // b[i]：最高位在第 i 位（0-based 最低位0）的基元素

    XorBasis() { for (int i = 0; i <= BITS; i++) b[i] = 0; }

    // 插入一个数 x 到基中
    void insert(long long x) {
        for (int i = BITS; i >= 0; i--) {
            if ((x >> i) & 1) {
                if (!b[i]) { b[i] = x; return; }
                x ^= b[i];
            }
        }
    }

    // 判断：x 能否被当前基表示（能被异或出来）
    bool can_rep(long long x) {
        for (int i = BITS; i >= 0; i--)
            if ((x >> i) & 1) { if (!b[i]) return false; x ^= b[i]; }
        return true;
    }

    // 求：从基中选若干异或能得到的**最大值**
    long long max_xor() {
        long long res = 0;
        for (int i = BITS; i >= 0; i--)
            if (b[i] && (res ^ b[i]) > res) res ^= b[i];
        return res;
    }

    // 求：基中选若干异或能得到的**非零最小值**
    long long min_xor() {
        for (int i = 0; i <= BITS; i++) if (b[i]) return b[i];
        return 0;   // 基为空
    }

    // 求：异或集合中**第 k 小**（k >= 1，不含 0；如果原集合能异或出 0 则需要特殊处理）
    // 使用前先 call rebuild：把基改造成"行最简形"——每行的最高1位在其他行对应位置都是0
    void rebuild() {
        for (int i = 1; i <= BITS; i++)
            for (int j = 0; j < i; j++)
                if (b[i] && ((b[i] >> j) & 1)) b[i] ^= b[j];
    }
    long long kth_smallest(long long k, bool zero_reachable) {
        // zero_reachable=true：表示原集合存在子集异或=0（即"插入时至少有一个x没能加入基"）
        //   k=1 对应 0；否则 k-- 后再查
        if (zero_reachable) { if (k == 1) return 0; k--; }
        long long res = 0;
        int cnt = 0;
        for (int i = 0; i <= BITS; i++) if (b[i]) cnt++;
        if (k >= (1LL << cnt)) return -1;   // 超出范围
        for (int i = 0, j = 0; i <= BITS; i++) {
            if (!b[i]) continue;
            if (k >> j & 1) res ^= b[i];
            j++;
        }
        return res;
    }

    // 合并两个 XorBasis：把另一个基中所有元素逐个插入当前
    void merge(XorBasis& oth) {
        for (int i = 0; i <= BITS; i++) if (oth.b[i]) insert(oth.b[i]);
    }
};
```

**使用场景速查**：
- "树上路径异或最大值"：对每个点做前缀异或（从根出发），然后问题转化为"两点前缀异或XOR = 路径异或"，通常配合**可持久化Trie**。
- "最大异或和子集"：直接 `max_xor()`。
- "第k小异或值"：必须先 `rebuild()` 再 `kth_smallest()`。

---

<a id="sec-4-12"></a>

### 4.12 FFT/NTT/FWT

- **适用场景**：多项式乘法（FFT/NTT）、位运算卷积（FWT：与/或/异或卷积）。
- **时间复杂度**：$O(n \log n)$（$n$ 为补到 2 的幂后的长度）。
- **核心思想**：
- **FFT**：利用单位根的对称性，分治地将系数表示转化为点值表示（DFT），相乘后再 IDFT 转回系数表示。需用浮点数，存在精度问题。
- **NTT**：用原根 $g$ 替代单位根，在模 $p$ 意义下进行，避免精度问题。常用 $p = 998244353 = 119 \times 2^{23} + 1$，$g = 3$。
- **FWT**：对位运算卷积 $c_k = \sum_{i \oplus j = k} a_i b_j$，按位分治构造可逆变换。

**模板代码（NTT，模 998244353）：**

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

const int MOD = 998244353;     // = 119 * 2^23 + 1, 原根 g = 3
const int G   = 3;

long long pw(long long a, long long b, long long p) {
    long long r = 1; a %= p; if (a < 0) a += p;
    for (; b > 0; b >>= 1, a = a * a % p)
        if (b & 1) r = r * a % p;
    return r;
}

// 迭代式 NTT, flag=1 为 NTT, flag=-1 为 INTT
void ntt(vector<long long>& a, int flag) {
    int n = a.size();
    // 蝶形置换 (位逆序)
    for (int i = 1, j = 0; i < n; i++) {
        int bit = n >> 1;
        for (; j & bit; bit >>= 1) j ^= bit;
        j ^= bit;
        if (i < j) swap(a[i], a[j]);
    }
    for (int len = 2; len <= n; len <<= 1) {
        long long w = flag == 1 ? pw(G, (MOD - 1) / len, MOD)
                                : pw(G, MOD - 1 - (MOD - 1) / len, MOD);
        for (int i = 0; i < n; i += len) {
            long long wn = 1;
            for (int j = 0; j < len / 2; j++) {
                long long u = a[i + j];
                long long v = a[i + j + len / 2] * wn % MOD;
                a[i + j]             = (u + v) % MOD;
                a[i + j + len / 2]   = (u - v + MOD) % MOD;
                wn = wn * w % MOD;
            }
        }
    }
    if (flag == -1) {
        long long inv = pw(n, MOD - 2, MOD);
        for (int i = 0; i < n; i++) a[i] = a[i] * inv % MOD;
    }
}

// 多项式乘法 (模 998244353)
vector<long long> multiply(vector<long long> a, vector<long long> b) {
    int n = 1;
    while (n < (int)(a.size() + b.size())) n <<= 1;   // 补到 2 的幂
    a.resize(n); b.resize(n);
    ntt(a, 1); ntt(b, 1);
    for (int i = 0; i < n; i++) a[i] = a[i] * b[i] % MOD;
    ntt(a, -1);
    return a;
}

int main() {
    quick;
    int n, m; cin >> n >> m;
    vector<long long> a(n + 1), b(m + 1);
    for (auto& x : a) cin >> x;
    for (auto& x : b) cin >> x;
    auto c = multiply(a, b);
    for (int i = 0; i <= n + m; i++) cout << c[i] << ' ';
    return 0;
}
```

**FWT（异或卷积）核心代码：**

```cpp
void fwt_xor(vector<long long>& a, int flag) {
    int n = a.size();
    for (int len = 2; len <= n; len <<= 1)
        for (int i = 0; i < n; i += len)
            for (int j = 0; j < len / 2; j++) {
                long long u = a[i + j], v = a[i + j + len / 2];
                a[i + j]             = (u + v) % MOD;
                a[i + j + len / 2]   = (u - v + MOD) % MOD;
            }
    if (flag == -1) {
        long long inv = pw(n, MOD - 2, MOD);
        for (auto& x : a) x = x * inv % MOD;
    }
}
```

**使用说明：**
- NTT 要求模数 $p$ 满足 $p = k \cdot 2^m + 1$ 且 $2^m \geq n$，常用 `998244353`
- 多项式乘法结果长度为 `a.size() + b.size() - 1`，需将 `n` 补到 $\geq$ 该值的 2 的幂
- FFT 浮点版本将 `long long` 换成 `complex<double>`，单位根用 `cos/sin` 计算
- FWT 与卷积/或卷积变换公式不同，请按需替换

---

<a id="sec-4-13"></a>

### 4.13 博弈论SG函数

- **适用场景**：公平组合游戏（ICG）的胜负判定与最优策略。
- **时间复杂度**：$O(\text{状态数} \times \text{转移数})$。

**核心定理（Sprague-Grundy）**：
- 每个 ICG 状态等价于一个 Nim 堆，堆大小即该状态的 SG 值
- **SG 定理**：多个独立游戏的 SG 异或和 $\neq 0$ 时先手必胜，$= 0$ 时先手必败
- **SG 定义**：$\text{SG}(u) = \text{mex}\{\text{SG}(v) \mid v \text{ 是 } u \text{ 的后继}\}$
- 终止状态 SG = 0（无后继）

**模板代码（一般 ICG 的 SG 函数 + Nim 博弈判定）：**

```cpp
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

// 记忆化搜索求 SG 值
// G: 状态转移图 (u -> 后继状态集合)
// n: 状态总数
vector<int> SG;
vector<bool> vis;

int getSG(int u, const vector<vector<int>>& G) {
    if (SG[u] != -1) return SG[u];
    set<int> s;
    for (int v : G[u]) s.insert(getSG(v, G));
    int g = 0;
    while (s.count(g)) g++;        // mex
    return SG[u] = g;
}

// Nim 博弈: n 堆石子, 每次任取一堆取任意多, 取完者胜
// 结论: 异或和 != 0 先手必胜, 否则必败
bool nim_game(const vector<int>& piles) {
    int xorsum = 0;
    for (int x : piles) xorsum ^= x;
    return xorsum != 0;            // true: 先手必胜
}

int main() {
    quick;
    // Nim 博弈示例
    int n; cin >> n;
    vector<int> a(n);
    for (auto& x : a) cin >> x;
    cout << (nim_game(a) ? "First\n" : "Second\n");

    // 一般 ICG 的 SG 求解示例
    int m; cin >> m;               // 状态数
    vector<vector<int>> G(m + 1);
    SG.assign(m + 1, -1);
    int edges; cin >> edges;
    for (int i = 0; i < edges; i++) {
        int u, v; cin >> u >> v;   // u -> v (v 是 u 的后继)
        G[u].push_back(v);
    }
    // 多个独立游戏合并: 异或和
    int q; cin >> q;
    int xorsum = 0;
    while (q--) {
        int s; cin >> s;
        xorsum ^= getSG(s, G);
    }
    cout << (xorsum != 0 ? "First\n" : "Second\n");
    return 0;
}
```

**经典博弈模型结论：**
- **Nim 博弈**：$n$ 堆石子任取，异或和 $\neq 0$ 先手胜
- **Bash 博弈**：$n$ 颗石子每次取 $[1, m]$，$n \bmod (m+1) \neq 0$ 先手胜
- **Wythoff 博弈**：两堆 $(a, b)$（$a \leq b$），当 $b - a = \lfloor \phi \cdot (b - a) \rfloor$ 即 $(a, b) = (\lfloor k\phi \rfloor, \lfloor k\phi^2 \rfloor)$ 时先手必败，其中 $\phi = \frac{\sqrt{5}+1}{2}$
- **Moore's Nim-k**：每次至多从 $k$ 堆中取，每堆至少取 1，将每堆石子二进制按位求和，所有位都 $\bmod (k+1) = 0$ 时必败
- **斐波那契博弈**：$n$ 颗石子，先手第一次不能取完，之后每次取不超过上一次的 2 倍，$n$ 为斐波那契数时必败

**使用说明：**
- 终止状态（无后继）的 SG = 0
- 多个独立游戏组合的总 SG = 各游戏 SG 的异或和
- 求解一般 ICG 时需建出状态转移图（DAG），SG 值用记忆化搜索 + mex 计算
- 注意区分公平组合游戏（双方操作相同）与非公平游戏（如象棋）

---


