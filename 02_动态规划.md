## [返回目录](http://scpc.fun/discussion-detail/1405)

<style>
a[id], h2, h3, h4 { scroll-margin-top: 140px; }
</style>

---


## 本章目录

- [2.1 线性DP](#sec-2-1) **必学**
- [2.2 背包DP](#sec-2-2) **必学**
- [2.3 树形DP](#sec-2-3) **必学**
- [2.4 单调队列优化DP](#sec-2-4) **进阶**
- [2.5 状压DP](#sec-2-5) **进阶**
- [2.6 记忆化搜索](#sec-2-6) **必学**
- [2.7 数位DP](#sec-2-7) **进阶**
- [2.8 区间DP](#sec-2-8) **进阶**
- [2.9 斜率优化DP](#sec-2-9) **选学**
- [2.10 概率DP / 期望DP](#sec-2-10) **进阶**

---

<a id="ch2"></a>

## 二、动态规划

本板块涵盖动态规划的经典模型，包括线性DP、背包DP、树形DP和单调队列优化DP。


<a id="sec-2-1"></a>

### 2.1 线性DP
- **适用场景**：状态转移在一维或二维上线性推进。
- **时间复杂度**：$O(n)$ 或 $O(nm)$

**核心模型**：
- 斐波那契/爬楼梯：$dp[i] = dp[i-1] + dp[i-2]$
- 最大子段和（Kadane算法）：$dp[i] = \max(a[i], dp[i-1] + a[i])$
- 最长公共子序列：$dp[i][j] = \max(dp[i-1][j], dp[i][j-1], dp[i-1][j-1] + 1)$

```cpp
// 最大子段和（Kadane算法）
long long cur = 0, ans = LLONG_MIN;
for (int i = 0; i < n; i++) {
    cur = max((long long)a[i], cur + a[i]);
    ans = max(ans, cur);
}

// 爬楼梯（每次走1/2/3步）
dp[0] = 1;
for (int i = 1; i <= n; i++) {
    dp[i] = dp[i-1];
    if (i >= 2) dp[i] += dp[i-2];
    if (i >= 3) dp[i] += dp[i-3];
}

// 最大正方形（二维DP）
for (int i = 1; i <= n; i++) {
    for (int j = 1; j <= m; j++) {
        if (a[i][j] == 1) {
            dp[i][j] = min({dp[i-1][j], dp[i][j-1], dp[i-1][j-1]}) + 1;
            ans = max(ans, dp[i][j]);
        }
    }
}
```

**相关题目**：
- [小凡爬楼梯](http://scpc.fun/problem/9203)（第77题，爬楼梯DP）
- [dp入门](http://scpc.fun/problem/9204)（第78题，最大子段和）
- [精准的跳跃](http://scpc.fun/problem/10092)（第86题，跳跃DP）
- [我要成为旮旯男主！](http://scpc.fun/problem/10452)（第106题，最大正方形DP）
- [最大子段和](http://scpc.fun/problem/10479)（第115题，Kadane算法）

<a id="sec-2-2"></a>

### 2.2 背包DP
- **适用场景**：在容量限制下选择物品，最大化价值。
- **时间复杂度**：$O(nW)$

**核心模型**：
- 01背包：每件物品选或不选，倒序遍历容量
- 完全背包：每件物品可选无限次，正序遍历容量
- 分组背包：每组中最多选一件，每组倒序遍历容量

```cpp
// 01背包
for (int i = 0; i < n; i++)
    for (int j = W; j >= w[i]; j--)
        dp[j] = max(dp[j], dp[j - w[i]] + v[i]);

// 完全背包
for (int i = 0; i < n; i++)
    for (int j = w[i]; j <= W; j++)
        dp[j] = max(dp[j], dp[j - w[i]] + v[i]);

// 分组背包（每组选至多一件）
for (int g = 0; g < groups; g++)
    for (int j = W; j >= 0; j--)
        for (auto& item : group[g])
            if (j >= item.w)
                dp[j] = max(dp[j], dp[j - item.w] + item.v);

// 分数规划+背包（二分答案+背包判定）
bool check(double x) {
    vector<double> dp(W + 1, -1e18);
    dp[0] = 0;
    for (int i = 0; i < n; i++) {
        double val = a[i] - x * b[i];
        int w = min(W, b[i]);
        for (int j = W; j >= 0; j--) {
            if (dp[j] == -1e18) continue;
            int nj = min(W, j + w);
            dp[nj] = max(dp[nj], dp[j] + val);
        }
    }
    return dp[W] >= 0;
}
```

**相关题目**：
- [搭配购买](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P1455)（第68题，并查集+分组背包）
- [分数规划 II](http://scpc.fun/problem/10468)（第50题，二分答案+背包判定）

<a id="sec-2-3"></a>

### 2.3 树形DP
- **适用场景**：在树上进行动态规划，通常自底向上递归处理。
- **时间复杂度**：$O(n)$

**核心模型**：
- 没有上司的舞会：$dp[u][0/1]$ 表示 $u$ 不选/选时子树的最大值
- 树形背包：在树上做分组背包

```cpp
// 树形DP模板（没有上司的舞会）
void dfs(int u) {
    dp[u][1] = val[u];  // 选u
    for (int v : g[u]) {
        dfs(v);
        dp[u][0] += max(dp[v][0], dp[v][1]);  // 不选u，子节点可选可不选
        dp[u][1] += dp[v][0];                   // 选u，子节点不能选
    }
}
// 答案：max(dp[root][0], dp[root][1])
```

**相关题目**：
- [没有上司的舞会](http://scpc.fun/problem/%E6%B4%9B%E8%B0%B7-P1352)（第157题，经典树形DP）

<a id="sec-2-4"></a>

### 2.4 单调队列优化DP
- **适用场景**：DP转移形如 $dp[i] = \min_{j \in [i-m, i-1]} dp[j] + cost$，用单调队列维护滑动窗口最值。
- **时间复杂度**：$O(n)$
- **核心思想**：用双端队列维护一个单调递增（或递减）的下标序列，队首即为最优决策点。

```cpp
// 单调队列优化DP模板
deque<int> dq;
dq.push_back(0);  // 初始决策点
for (int i = 1; i <= n; i++) {
    // 移除超出窗口的决策
    while (!dq.empty() && dq.front() < i - m) dq.pop_front();
    // 转移
    dp[i] = a[i] + dp[dq.front()];
    // 维护单调性
    while (!dq.empty() && dp[dq.back()] >= dp[i]) dq.pop_back();
    dq.push_back(i);
}
// 最终答案：min(dp[n-m+1], ..., dp[n])
int ans = INT_MAX;
for (int i = n - m + 1; i <= n; i++) ans = min(ans, dp[i]);
```

**相关题目**：
- [烽火传递](http://scpc.fun/problem/10119)（第96题，单调队列优化DP）

<a id="sec-2-5"></a>

### 2.5 状压DP
- **适用场景**：选取状态可以用二进制位表示（集合选取、排列、路径经过集合、灯的开关状态等），状态数 $O(2^n)$ 一般要求 $n \le 20 \sim 24$。
- **核心思想**：用一个整数 `mask` 的每一位代表"是否选中/是否经过/是否打开"，通过 `mask | (1<<k)` 加元素、`mask ^ (1<<k)` 翻转、`mask & (1<<k)` 判断来转移。

```cpp
// ============= 经典模型 1：TSP 旅行商问题 =============
// dp[mask][u]：经过点集合 mask（二进制第 u 位=1）且停在 u 的最短路径
// 点编号 0..n-1
long long tsp(int n, vector<vector<long long>>& w) {
    int FULL = 1 << n;
    vector<vector<long long>> dp(FULL, vector<long long>(n, 4e18));
    // 任选起点（比如0号点）
    dp[1 << 0][0] = 0;
    for (int mask = 0; mask < FULL; mask++) {
        for (int u = 0; u < n; u++) {
            if (!(mask >> u & 1) || dp[mask][u] == 4e18) continue;
            for (int v = 0; v < n; v++) {
                if (mask >> v & 1) continue;
                int nmask = mask | (1 << v);
                dp[nmask][v] = min(dp[nmask][v], dp[mask][u] + w[u][v]);
            }
        }
    }
    // 回到起点0
    long long ans = 4e18;
    for (int u = 0; u < n; u++) ans = min(ans, dp[FULL-1][u] + w[u][0]);
    return ans;
}

// ============= 经典模型 2：集合覆盖 / 位枚举子集 =============
// 枚举 mask 的所有子集 sub
for (int mask = 0; mask < (1 << n); mask++) {
    for (int sub = mask; ; sub = (sub - 1) & mask) {
        // 使用 sub
        if (sub == 0) break;
    }
}

// ============= 常用位运算速查 =============
// __builtin_popcount(mask)  —— 返回 mask 中 1 的个数（C++自带GCC内建）
// __builtin_ctz(mask)       —— 返回 mask 末尾 0 的个数（最低位 1 的位置）
// lowbit(x) = x & (-x)      —— 取最低位的 1
```

<a id="sec-2-6"></a>

### 2.6 记忆化搜索
- **适用场景**：DP状态转移具有递归结构（子问题多次重复计算），用 DFS + 记忆数组缓存已算过的子问题答案。等价于自顶向下的DP，通常比自底向上迭代写法更直观（尤其是网格、区间、多状态约束）。

**核心步骤**：
1. 设计递归函数参数 = DP 状态
2. 进入函数先判：如果该状态已计算过（memo[state] ≠ 未设值），直接返回缓存值
3. 写递归边界
4. 写递归转移（调用自身），结果存入 memo[state] 再返回

```cpp
// ============= 模型 1：整数拆分 / 爬楼梯变种（记忆化）=============
// f(n)：爬上 n 阶台阶，每次可跨 1/2/3 步，有多少种方法
const int MAXMEM = 1005;
long long memo[MAXMEM];
long long dfs_climb(int n) {
    if (n < 0) return 0;
    if (n == 0) return 1;
    if (memo[n] != -1) return memo[n];
    long long res = dfs_climb(n - 1) + dfs_climb(n - 2) + dfs_climb(n - 3);
    return memo[n] = res;
}
// 使用：memset(memo, -1, sizeof memo); cout << dfs_climb(n);

// ============= 模型 2：网格 DP 记忆化（从左上到右下）=============
// grid[i][j]：格子权值；f(i,j)：从 (0,0) 走到 (i,j) 的最大路径和（只能右/下走）
int R, C;
vector<vector<int>> grid;
vector<vector<long long>> memog;
long long dfs_grid(int i, int j) {
    if (i >= R || j >= C) return -4e18;
    if (i == R - 1 && j == C - 1) return grid[i][j];
    if (memog[i][j] != -4611686018427387903LL) return memog[i][j];  // 若缓存过
    long long right = dfs_grid(i, j + 1);
    long long down  = dfs_grid(i + 1, j);
    return memog[i][j] = grid[i][j] + max(right, down);
}
// 使用：memog.assign(R, vector<long long>(C, -4611686018427387903LL)); dfs_grid(0,0);

// ============= 模型 3：区间DP记忆化（石子合并变种）=============
// sum[i][j] 为区间 [i..j] 数的和；f(l,r)：合并区间 [l,r] 的最小代价
int n;
long long s[MAXMEM], sum[MAXMEM][MAXMEM];
long long memo_int[MAXMEM][MAXMEM];
long long dfs_interval(int l, int r) {
    if (l == r) return 0;
    if (memo_int[l][r] != -1) return memo_int[l][r];
    long long res = 4e18;
    for (int k = l; k < r; k++) {
        res = min(res, dfs_interval(l, k) + dfs_interval(k + 1, r) + sum[l][r]);
    }
    return memo_int[l][r] = res;
}
```

**注意事项**：
- 缓存初始化必须用"不可能出现的值"（不能是 0，因为 0 可能是合法答案）；整数拆分/区间DP常用 -1，网格最值常用 ±∞ 哨兵。
- 若模数为质数，缓存的是取模后结果，初始化仍需设为 -1。
- 当状态太多数组装不下时，改用 `map/unordered_map<Tuple, T>` 做稀疏缓存。

<a id="sec-2-7"></a>

### 2.7 数位DP
- **适用场景**：统计区间 $[L, R]$ 中满足某数位限制的整数个数（如"不含前导零且数位和恰好为 k"、"相邻两位差≥2 的数"、"包含子串 13 的数"等）。几乎所有"数位限制 + 区间计数"都是数位DP。
- **核心思想**：用记忆化搜索，从高位到低位逐位填数；状态包含：当前处理到第几位、前几位填的数是否"贴着上界 limit"、"是否已经过了前导零 lead"、以及题意所需的状态（数位和、上一位填的数等）。

```cpp
// 经典例题：统计 [L, R] 中不含前导零、相邻两位差 ≥ 2 的数的个数（"windy数"）
// 状态：dfs(pos, prev_digit, limit, lead)
//   pos：当前处理第 pos 位（从高到低，0-indexed）
//   prev_digit：上一位填的数字（用于限制 |cur - prev| >= 2）；未填则用 -2 表示
//   limit：前几位是否"贴上界"（贴上界时当前位不能超过对应位的上界数字）
//   lead：是否处于前导零状态（前导零时下一位填 0 不算作"真0"）

const int MAXN = 15;     // 数字最多位数
int digit[MAXN];         // 上界R的各位数字（高位在前）
int dp[MAXN][15][2][2];  // 记忆数组：pos, prev_digit, limit, lead（注意 limit/lead 为 1 的状态不可缓存）
int N;                    // 数字总位数

// dfs 返回：从第 pos 位开始填，前面已填上一位为 prev_digit，状态 (limit, lead) 时的合法方案数
int dfs(int pos, int prev, bool limit, bool lead) {
    if (pos == N) return 1;       // 填完了，得到一个合法数
    if (!limit && !lead && dp[pos][prev + 2][limit][lead] != -1)
        return dp[pos][prev + 2][limit][lead];   // 仅"非限制态"可缓存
    int up = limit ? digit[pos] : 9;              // 当前位上界
    int res = 0;
    for (int d = 0; d <= up; d++) {
        if (!lead && abs(d - prev) < 2) continue; // 非前导零状态：相邻两位差必须 >= 2
        // 前导零状态（lead=true）：填 0 仍是前导零；填非 0 才进入"真数字"状态
        res += dfs(pos + 1,
                   (lead && d == 0) ? -2 : d,      // 仍是前导零 → prev=-2；否则更新为 d
                   limit && (d == up),              // 之前贴上界且本位也贴 → 下一位仍贴上界
                   lead && (d == 0));               // 之前前导零且本位填 0 → 仍前导零
    }
    if (!limit && !lead) dp[pos][prev + 2][limit][lead] = res;
    return res;
}

// 计算 [1, x] 中满足条件的数个数；[L, R] = solve(R) - solve(L-1)
int count_windy(long long x) {
    if (x <= 0) return 0;
    N = 0;
    while (x) { digit[N++] = x % 10; x /= 10; }
    reverse(digit, digit + N);
    memset(dp, -1, sizeof dp);
    return dfs(0, -2, true, true);
}
```

**使用说明**：
- 状态数通常很少（位数 ≤ 20，每维最多 10），可以放心开数组。
- `limit=true` 或 `lead=true` 的状态**不可缓存**（依赖上界），必须实时算。
- 不同题改 `prev` 维度和转移条件即可：如"数位和=k"→ 加一维 `sum`；"包含 13"→ 加一维 `has13`；"数位 % p == 0"→ 加 `mod` 维。
- 求区间 $[L, R]$ → `solve(R) - solve(L-1)`，是数位DP通用套路。

<a id="sec-2-8"></a>

### 2.8 区间DP
- **适用场景**：合并相邻元素的代价问题（石子合并、矩阵链乘、括号匹配、回文分割等）。模型：把序列分成若干段，合并相邻两段产生代价，求最小/最大总代价。
- **核心思想**：$dp[l][r]$ 表示合并区间 $[l, r]$ 的最优解；转移时枚举断点 $k$：$dp[l][r] = \min_k \{ dp[l][k] + dp[k+1][r] \} + \text{cost}(l, r)$，其中 $\text{cost}(l, r)$ 通常是 $[l, r]$ 区间和（用前缀和 $O(1)$ 算）。
- **时间复杂度**：$O(n^3)$；可用四边形不等式优化到 $O(n^2)$。

```cpp
// 经典模型：石子合并
// n 堆石子排成一排，每次合并相邻两堆代价为两堆之和，求合并成一堆的最小总代价
// 注意：必须"相邻"才能合并！如果可以任意选则直接哈夫曼贪心（用优先队列）。
const int MAXN = 305;
long long a[MAXN], pre[MAXN];     // a：石子重量；pre：前缀和
long long dp[MAXN][MAXN];         // dp[l][r]：合并 [l, r] 的最小代价
int   opt[MAXN][MAXN];            // opt[l][r]：最优断点位置（四边形不等式优化用）

long long stone_merge(int n, vector<long long>& w) {
    for (int i = 1; i <= n; i++) a[i] = w[i - 1];
    for (int i = 1; i <= n; i++) pre[i] = pre[i - 1] + a[i];
    // 区间DP标准写法（O(n^3)）
    for (int len = 2; len <= n; len++) {           // 枚举区间长度
        for (int l = 1; l + len - 1 <= n; l++) {   // 枚举左端点
            int r = l + len - 1;
            dp[l][r] = LLONG_MAX;
            for (int k = l; k < r; k++) {           // 枚举断点
                long long cost = dp[l][k] + dp[k + 1][r];
                if (cost < dp[l][r]) { dp[l][r] = cost; opt[l][r] = k; }
            }
            dp[l][r] += pre[r] - pre[l - 1];       // 加上合并 [l,r] 的代价 = 区间和
        }
    }
    return dp[1][n];
}

// ============= 四边形不等式优化版（O(n^2)）=============
// 利用最优断点单调性：opt[l][r-1] <= opt[l][r] <= opt[l+1][r]
long long stone_merge_quadrangle(int n, vector<long long>& w) {
    for (int i = 1; i <= n; i++) a[i] = w[i - 1];
    for (int i = 1; i <= n; i++) pre[i] = pre[i - 1] + a[i];
    for (int i = 1; i <= n; i++) dp[i][i] = 0, opt[i][i] = i;
    for (int len = 2; len <= n; len++) {
        for (int l = 1; l + len - 1 <= n; l++) {
            int r = l + len - 1;
            dp[l][r] = LLONG_MAX;
            int kl = opt[l][r - 1], kr = opt[l + 1][r];   // 利用单调性
            for (int k = kl; k <= kr; k++) {
                long long cost = dp[l][k] + dp[k + 1][r];
                if (cost < dp[l][r]) { dp[l][r] = cost; opt[l][r] = k; }
            }
            dp[l][r] += pre[r] - pre[l - 1];
        }
    }
    return dp[1][n];
}
```

**使用说明**：
- **环形石子合并**：把数组复制一份接在末尾，对长 $2n$ 的数组做区间DP，最后答案为 $\min_{i=1..n} dp[i][i+n-1]$。
- **四边形不等式优化**需满足"代价函数满足四边形不等式"（区间和、绝对值差等都满足）；不满足时不能优化。
- 经典变体：矩阵链乘、戳气球（反向DP）、回文串分割、括号匹配方案数。

<a id="sec-2-9"></a>

### 2.9 斜率优化DP
- **适用场景**：DP 转移形如 $dp[i] = \min_j \{ dp[j] + \text{cost}(j, i) \}$，且 $\text{cost}(j, i)$ 可写成形如 $a[i] \cdot X[j] + Y[j] + C[i]$ 的形式（即"代价是关于 $j$ 的一次线性组合"）。常见于：任务调度、序列分割、仓库选址等。
- **核心思想**：把每个决策点 $j$ 看作二维平面上的点 $(X[j], Y[j])$，转移 $dp[i] = \min_j \{ a[i] \cdot X[j] + Y[j] \} + C[i]$ 等价于"在凸包上找斜率 $-a[i]$ 的切线"。维护下凸包（求 min）或上凸包（求 max），用单调队列维护凸包点。

**判定能否斜率优化**：$\text{cost}(j, i)$ 能拆成 $a[i] \cdot X[j] + Y[j] + C[i]$（$a, X, Y, C$ 仅依赖 $i$ 或 $j$）。

```cpp
// 经典模型：序列分割（[HNOI2008] 玩具装箱 TOY）
// n 个物品，第 i 个长度 c[i]；把它们分成若干段，每段 [j+1, i] 的代价为 (sum[i]-sum[j]+i-j-1-L)^2
// 求 dp[i] = min_{j<i} { dp[j] + (sum[i]-sum[j]+i-j-1-L)^2 }
// 设 S[i] = sum[i] + i；则代价 = (S[i] - S[j] - 1 - L)^2 = (S[i] - (S[j]+1+L))^2
// 令 X[j] = S[j]+1+L, Y[j] = dp[j]，则 dp[i] = min_j { (S[i] - X[j])^2 + Y[j] }
// 这是标准斜率优化形式：维护 (X[j], Y[j]) 的下凸包

long long c[MAXN], S[MAXN], dp[MAXN];
int n; long long L;

// 函数：判断点 b 是否在凸包上有用（即 b 是否"在 a,c 形成的下凸包外侧"）
// 用叉积判断：(b - a) × (c - b) >= 0 ? 无用 : 有用
bool useless(int a, int b, int c) {
    // (X[b]-X[a])*(Y[c]-Y[a]) - (Y[b]-Y[a])*(X[c]-X[a]) >= 0 ? 无用 : 有用
    long long xa = S[a] + 1 + L, ya = dp[a];
    long long xb = S[b] + 1 + L, yb = dp[b];
    long long xc = S[c] + 1 + L, yc = dp[c];
    // 注意：为了防爆long long，用 __int128 或 double 都可以；这里用 cross >= 0 表示"右转"→无用
    return (__int128)(xb - xa) * (yc - ya) - (__int128)(yb - ya) * (xc - xa) >= 0;
}

long long slope_opt_dp() {
    for (int i = 1; i <= n; i++) S[i] = S[i - 1] + c[i] + 1;   // S[i] = sum + i（c数组已含原长度）
    // 队列维护凸包下标
    static int q[MAXN]; int head = 0, tail = 0;
    q[tail++] = 0;                  // 初始：j=0 是一个候选决策点
    dp[0] = 0;
    for (int i = 1; i <= n; i++) {
        // 1. 队首：弹出斜率比 (S[i]) 还小的点（已经不会被选为最优）
        while (tail - head >= 2) {
            int j1 = q[head], j2 = q[head + 1];
            // 比较 (Y[j2]-Y[j1]) / (X[j2]-X[j1]) 和 S[i]
            // 即 (Y[j2]-Y[j1]) <= S[i] * (X[j2]-X[j1]) 时弹出 j1（因斜率单调）
            long long dy = dp[j2] - dp[j1];
            long long dx = (S[j2] + 1 + L) - (S[j1] + 1 + L);
            if (dy <= S[i] * dx) head++;     // S[i] 增大且凸包斜率单调递增 → 队首可弹
            else break;
        }
        int j = q[head];
        long long diff = S[i] - (S[j] + 1 + L);
        dp[i] = dp[j] + diff * diff;
        // 2. 队尾：维护凸包，弹出"右转"的无用点
        while (tail - head >= 2 && useless(q[tail - 2], q[tail - 1], i)) tail--;
        q[tail++] = i;
    }
    return dp[n];
}
```

**使用说明**：
- **斜率单调性**是关键：$a[i]$（这里是 $S[i]$）必须单调，否则不能直接用队列维护凸包，要用**二分**找最优切点或**李超树**。
- 叉积判断"是否在凸包上"时小心爆 long long，可用 `__int128` 或 `long double` 比较。
- 决策单调性 + 斜率优化是省选/NOI 高频考点，模板必须背熟。

---

<a id="sec-2-10"></a>

### 2.10 概率DP / 期望DP

- **适用场景**：题目给出"概率"、"期望"、"随机游走"、"抛骰子"、"随机选元素"等关键词，需计数期望/概率的最终答案。
- **时间复杂度**：取决于状态数，通常 $O(n)$ 或 $O(n^2)$，与普通 DP 一致。
- **建议上界**：线性 DP ≤ 2×10^6；二维 DP ≤ 5000×5000（视状态数而定）。
- **核心思想**：
- **概率DP**：`dp[i]` 表示到达状态 `i` 的**概率**，转移按条件概率公式展开：`dp[v] = Σ dp[u] * P(u→v)`。
- **期望DP**：`E[x]` 表示状态 `x` 到终止态的**期望**，常用公式：`E[x] = 1 + Σ P(x→y)·E[y]`（加 1 是因为转移本身消耗一步）。
- 若转移含**环**（如：随机游走、回到起点概率>0），需**从后往前逆推**或**高斯消元**解方程。

**模板一：线性期望（抛骰子到终点）**

```cpp
// 经典：从位置 0 出发，每次抛骰子 1~6 前进相应步数，到达 ≥n 的位置停止
// 求期望走多少步？
//   E[i]: 从 i 出发到停止的期望步数，终止态 E[n..] = 0
//   转移: E[i] = 1 + (E[i+1] + ... + E[i+6]) / 6  (i < n)
#include<bits/stdc++.h>
using namespace std;
#define quick ios::sync_with_stdio(0); cin.tie(0)
#define endl '\n'

int main() {
    quick;
    int n;
    while (cin >> n && n) {
        vector<double> E(n + 7, 0.0);   // E[n..n+6] = 0
        for (int i = n - 1; i >= 0; i--) {
            double sum = 0;
            for (int j = 1; j <= 6; j++) sum += E[i + j];
            E[i] = 1 + sum / 6.0;
        }
        cout << fixed << setprecision(4) << E[0] << endl;
    }
    return 0;
}
```

**模板二：有概率陷阱（经典期望DP + 转移环 → 解方程）**

```cpp
// 一条直线上 N 个点，从位置 i 有 p 的概率到 i+1，有 (1-p) 的概率回到 1。
// 问从 1 出发到达 N 的期望步数？
//   E[i]: 从 i 到 N 的期望；E[N] = 0，E[i] = 1 + p·E[i+1] + (1-p)·E[1]
//   这是一个"自环方程"，用高斯消元或待定系数法求解。
//   此处演示通用方法：高斯消元（小型方程组）。
double expected_steps(int N, double p) {
    // 设 E[i] = A[i] * E[1] + B[i]，代入得递推
    vector<double> A(N + 2, 0), B(N + 2, 0);
    A[N] = 0; B[N] = 0;
    for (int i = N - 1; i >= 1; i--) {
        A[i] = (1 - p);                           // 系数部分
        B[i] = 1 + p * B[i + 1];                  // 常数部分
        if (i == 1) continue;                     // E[1] 用下式解
        // E[i] = 1 + p·E[i+1] + (1-p)·E[1] = A[i]·E[1] + B[i]
        A[i] = (1 - p);                           // (1-p) 乘 E[1]
        B[i] = 1 + p * (A[i + 1] * A[i] /*...*/); // 具体推导见模板三
    }
    // i=1 时代入 E[1] = A[1]*E[1] + B[1]  →  E[1] = B[1] / (1 - A[1])
    double A1 = (1 - p) + p * A[2];
    double B1 = 1 + p * B[2];
    return B1 / (1.0 - A1);
}
```

**模板三：树上随机游走（经典期望逆推）**

```cpp
// 从根 1 出发，每次等概率移动到相邻节点，求走到某个叶子的期望步数。
// 对无向树：设 fa[u] 为父节点，E[u] 为 u 到目标的期望。
//   度数为 deg[u]，则：
//     E[u] = 1 + (E[fa[u]] + Σ E[v]) / deg[u]    (v 为 u 的儿子)
//   同样用 E[u] = A[u] * E[fa[u]] + B[u] 求解
int G[MAXN][MAXN], deg[MAXN];
double A[MAXN], B[MAXN];

void dfs_expect(int u, int fa) {
    if (deg[u] == 1 && u != 1) { A[u] = B[u] = 0; return; }   // 叶子终止态
    double sumA = 0, sumB = 0;
    for (int v : G[u]) {
        if (v == fa) continue;
        dfs_expect(v, u);
        sumA += A[v]; sumB += B[v];
    }
    double d = deg[u];
    double k = 1 - sumA / d;            // E[u] 的系数
    A[u] = (1.0 / d) / k;               // E[u] = A[u] * E[fa[u]] + B[u]
    B[u] = (1 + sumB / d) / k;
}
```
- **常见套路**：
- **抛硬币 / 骰子**：通常直接逆推即可，状态天然无环（骰子问题从后往前推）。
- **随机游走 / 马尔可夫链**：**一定要先判断是否有自环/互环**，有环就用待定系数法 `E[u] = A[u]*E[fa] + B[u]` 或高斯消元。
- **期望的线性性质**：不必枚举所有方案，直接对"每个事件发生的概率 × 其贡献"求和，经常能大幅简化问题。






